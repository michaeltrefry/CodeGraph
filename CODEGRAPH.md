# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-14 18:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-component platform that ingests, indexes, and visualizes software repositories as graph structures, providing LLM-assisted analysis, dependency mapping, semantic search, and an AI assistant interface. The repository is organized into several distinct layers that work together: (1) Language-specific extractors (C#, TypeScript, SQL, ColdFusion, Ansible, Terraform, and Tree-sitter-based multi-language support) parse source files and infrastructure definitions, producing structured ExtractionResult graphs of nodes and edges representing code constructs and their relationships. (2) The indexer host (CodeGraph.Indexer.Host) orchestrates the extraction pipeline, triggering discovery, processing, cross-repository linking, community/cluster detection, and batch analysis via HTTP endpoints and MassTransit message consumers; a typed client library (CodeGraph.Indexer.Client) allows other services to invoke these operations programmatically. (3) The data layer is split between an abstract contract library (CodeGraph.Data defining all persistence interfaces and DTOs) and two concrete backends: MariaDB/MySQL via Entity Framework Core (CodeGraph.Data.MariaDb, handling relational storage, AES-encrypted credentials, and LLM config) and Neo4j via the official driver (CodeGraph.Data.Neo4j, handling graph traversal, Cypher queries, vector embeddings, and sequence management). (4) The core service layer (CodeGraph.Services) implements business logic for repository indexing, LLM-backed code review and analysis (OpenAI, Anthropic, LM Studio), an AI assistant with tool-use, semantic search via ONNX embeddings, wiki/documentation management, memory graph operations, and admin reporting. (5) The API layer (CodeGraph.Api) exposes REST endpoints consumed by an Angular frontend (CodeGraphWeb), covering project management, graph queries, LLM provider configuration, assistant interactions, memory graph operations, wiki content, and admin reporting, while also hosting MassTransit consumers for indexing pipeline events. (6) A dedicated memory subsystem (CodeGraph.Memory.Host and CodeGraph.Memory.Client) provides a knowledge graph service for storing and querying entity/claim/relationship data, with MassTransit-based async ingestion. (7) Background job scheduling (CodeGraph.Jobs) uses cron-based schedules stored in the database and a hosted worker that polls and dispatches named job types (re-indexing, community detection, embedding ingestion, MCP doc regeneration, etc.). (8) Metrics collection (CodeGraph.Metrics) consumes LLM usage and MCP tool invocation events from the message bus and persists them. (9) Shared infrastructure libraries (CodeGraph.Host.Shared, CodeGraph.Models) provide JWT/HMAC internal authentication, distributed tracing activity sources, and the central DTO/message contract schema used across all services. Externally, CodeGraph makes HTTP calls to a service identified as TC.CodeGraphApi (likely a remote or tenant-specific API instance), and is itself consumed by at least two external repositories — codeflow-team and CodeFlow — which make multiple HTTP calls into the CodeGraph API, indicating CodeGraph serves as a backend platform dependency for those systems. Test coverage is provided by CodeGraph.Tests (broad integration and unit tests with in-memory fakes) and CodeGraph.Jobs.Tests (job scheduling and dispatch unit tests).

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a multi-component repository consisting of a .NET backend (services, jobs, data stores, migration utilities) and an Angular frontend (CodeGraphWeb) along with TypeScript extraction tooling. The system analyzes, indexes, and visualizes code repositories as graph structures, providing features such as dependency analysis, LLM-assisted code review, wiki management, memory/knowledge graph storage, cluster detection, security analysis, and an AI assistant interface. The frontend communicates with the backend via a comprehensive API service, while background jobs handle indexing, community detection, batch analysis, and data migration.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core web API layer for the CodeGraph system — a platform that indexes, analyzes, and queries code repositories, database schemas, and wiki content using graph-based representations and LLM-powered assistance. It exposes REST endpoints for project/repository management, code graph querying, LLM provider configuration, AI assistant interactions, memory graph operations, wiki content, and administrative reporting, while also hosting MassTransit message consumers that react to background indexing and analysis pipeline events. It acts as the primary integration point between frontend clients, background workers, and internal services.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all persistence interfaces and entity/data-transfer types used across the system. It declares contracts (interfaces) for every major storage concern — graph nodes/edges, analysis results, code reviews, assistant runs, LLM configuration, wiki pages, memory graphs, vector embeddings, job scheduling, metrics, and more — while leaving concrete implementations to backend-specific projects (MariaDb, Neo4j). The project acts as the shared data contract boundary that service and pipeline layers depend on, keeping storage technology choices decoupled from business logic.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph, analysis, metrics, review, memory, assistant runs, wiki, admin, etc.) using Entity Framework Core with MySqlConnector, raw SQL via Dapper-style parameterized queries, and bulk upsert patterns. The project also handles database migrations, AES encryption of sensitive connection strings and API tokens, LLM configuration persistence, and DI registration for all store services.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It provides concrete implementations of multiple data store interfaces (graph, analysis, metrics, memory, wiki, vector, job scheduling, etc.) using the Neo4j graph database driver, translating between Neo4j nodes/relationships and the domain entities and models used throughout the system. It is responsible for all Cypher query execution, node/edge mapping, batch operations, migrations, and sequence management against a Neo4j instance.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a specialized extraction module that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and meta files) and produces a structured graph representation of their contents. It identifies Ansible-specific constructs such as tasks, modules, roles, handlers, and variables, creates typed graph nodes and edges, and detects cross-repository references. This project implements the ICodeExtractor interface, integrating into the broader CodeGraph repository analysis pipeline.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files and produces a structured code graph from them. It identifies components, functions, SQL queries, HTTP calls, include directives, object creation, and component invocations within ColdFusion markup and CFScript syntax, then emits those findings as an ExtractionResult consumed by the broader CodeGraph analysis pipeline.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to parse and extract structural metadata from C# source files and Visual Studio solutions. It walks syntax trees to identify classes, methods, interfaces, DI registrations, HTTP routes, service bus events, NuGet dependencies, and cross-service calls, producing structured ExtractionResult models consumed by the broader CodeGraph system.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information from them. It implements the ICodeExtractor interface to analyze T-SQL/MySQL syntax, identifying database objects such as tables, views, stored procedures, functions, and indexes, and building a code graph (ExtractionResult) representing their relationships and definitions.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a specialized code analysis component that parses HashiCorp Configuration Language (HCL) files used by Terraform infrastructure-as-code. It extracts graph nodes and edges representing Terraform constructs (resources, modules, data sources, variables, outputs, locals) and their relationships, producing an ExtractionResult that feeds into the broader CodeGraph repository analysis pipeline.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code analysis project that uses the Tree-sitter parsing library to extract structural information (definitions, imports, and relationships) from source files in multiple programming languages. It implements the ICodeExtractor interface, walking syntax trees to identify named entities and their relationships, then producing graph nodes and edges for the broader CodeGraph repository analysis system. The project supports language-specific extraction rules through a pluggable LanguageSpec/LanguageSpecs registry that maps file extensions to parsing strategies.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a long-running Node.js server process to perform TypeScript-specific analysis (including AST extraction and linting), communicates with that server via request/response protocols, and converts the results into graph nodes and edges for the broader CodeGraph system.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library for internal service-to-service authentication within the CodeGraph host ecosystem. It provides JWT/HMAC-based token creation and validation primitives, authentication configuration options, and dependency injection extension methods that other services in the repository can use to establish trusted internal communication channels. It also exposes shared hosting utilities such as activity sources for distributed tracing.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a typed HTTP client library that provides a .NET abstraction for communicating with a CodeGraph indexer service. It defines an interface and concrete implementation for triggering and monitoring indexing operations (repository processing, schema synchronization, community detection, linking, and re-indexing), along with DI registration helpers and exception types for error handling.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web application that serves as the hosting entry point for a code graph indexing system. It exposes HTTP API endpoints for triggering repository indexing operations (discovery, processing, schema sync, cross-repo linking, community detection, batch analysis) and wires up MassTransit message consumers that react to indexing lifecycle events. The project coordinates between a graph store, project/batch analysis services, and a TypeScript sidecar process to build and maintain a code knowledge graph.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (e.g., repository discovery, re-indexing, community detection, embedding ingestion, MCP doc regeneration), a dispatcher that routes job execution requests by type, and a scheduling service that manages cron-based schedules stored in a database. A hosted worker service polls for due schedules and triggers execution, making this project the primary orchestration layer for all async background processing in the system.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the CodeGraph.Jobs scheduling and dispatch infrastructure. It contains test doubles (recording/fake implementations of service interfaces), an in-memory store for job schedules, and test classes that verify job scheduling, command dispatching, and individual job execution behaviors without hitting real external dependencies.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides a typed .NET interface for communicating with a remote memory graph service. It defines the IMemoryClient contract and implements it via HttpMemoryClient, exposing operations for querying, traversing, writing, and deleting knowledge graph data (entities, claims, relationships, subgraphs) on behalf of named users. The project also includes DI registration helpers and supporting types for configuration and error handling.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API host that exposes a knowledge graph memory service. It provides REST endpoints for storing, querying, searching, and managing memory claims and entities in a graph structure, and integrates with MassTransit for asynchronous message-based claim ingestion. It serves as the runtime host for the memory subsystem within the CodeGraph repository, wiring together persistence, message bus consumers, and HTTP controllers.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository that handles in-memory data migration concerns. Based on the project name alone (no graph nodes or source code were provided), it likely manages schema or data transformations that occur in memory, possibly as part of a code analysis or graph database migration pipeline.

### CodeGraph.Metrics

CodeGraph.Metrics is a service responsible for collecting and persisting metrics events within the CodeGraph repository. It consumes messages from a message bus (MassTransit) — specifically LLM usage events and MCP tool invocation events — and records them via a metrics event recorder abstraction. The project is structured as an ASP.NET Core application with persistence (supporting MariaDB), MassTransit integration, and internal service authentication middleware.

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines the DTOs, message types, request/response objects, and domain model classes used across the CodeGraph repository. It serves as the central schema layer consumed by indexer, API, memory, and metrics services, defining the message bus contracts (consumed by external consumers) and the structured data types for memory graph operations, analysis workflows, and wiki/repository processing.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a tool for indexing, analyzing, and querying code repositories as graph structures. It provides services for repository indexing (extracting code into a graph database), LLM-backed code analysis and review (via OpenAI, Anthropic, and LM Studio), an AI assistant with tool-use capabilities, memory management, wiki/documentation management, semantic search via ONNX embeddings, and administrative reporting. It acts as the orchestration layer between the data access layer (CodeGraph.Data), external LLM providers, and the API/host layer.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository, covering unit and integration tests across all major subsystems including code extractors, data stores, API controllers, services, memory graph operations, and indexer pipelines. It provides a comprehensive collection of test doubles (fakes, stubs, recording/spy implementations, and in-memory stores) that implement production interfaces to enable isolated testing without real databases or external services. The project validates correctness of graph storage operations, LLM configuration resolution, security analysis, review services, MCP telemetry middleware, and numerous other components.

### reflect

The 'reflect' project appears to be a reflection or introspection utility within the CodeGraph repository. Based on the project name and its role in the repository structure, it likely provides mechanisms for analyzing, inspecting, or representing code structures programmatically. However, no graph nodes or source code snippets were provided, making a precise technical description difficult.

