# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-18 14:39 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a code intelligence platform that ingests software repositories, parses their source code into graph structures, and exposes querying, analysis, and visualization capabilities over those graphs. The repository is organized as a multi-project .NET solution with an Angular web frontend and a TypeScript extractor sidecar tool.

Architectural layers and their relationships:

1. **Extraction layer** – Four language-specific extractors (CodeGraph.Extractors.CSharp using Roslyn for C#/.NET solutions, CodeGraph.Extractors.TypeScript spawning an HTTP-based TypeScript language server, CodeGraph.Extractors.Sql using TSqlFragment AST visitors for SQL scripts, and CodeGraph.Extractors.TreeSitter using the Tree-sitter library for C, C++, Python, Go, Java, Ruby, Rust, PHP, and Bash) all implement a shared ICodeExtractor interface. Each extractor converts source files into typed graph nodes and directed edges representing definitions, dependencies, calls, imports, HTTP routes, DI registrations, and message bus patterns.

2. **Data model layer** – CodeGraph.Models is a shared DTO/contract library consumed by all other projects. It defines graph node/edge shapes, messaging contracts for the repository processing pipeline (indexing, analysis, synthesis events), HTTP request/response types, and the schema for a knowledge-graph memory subsystem (entities, claims, evidence).

3. **Data access layer** – CodeGraph.Data defines persistence interfaces (IGraphStore, IMemoryGraphStore, IWikiStore, IAnalysisStore, IVectorStore, IJobScheduleStore, etc.) and entity types without binding to a specific backend. CodeGraph.Data.Neo4j provides concrete Neo4j implementations of all those interfaces using Cypher queries via the Neo4j .NET driver, including vector embedding storage. In-memory fakes for all interfaces live in the test projects.

4. **Service layer** – CodeGraph.Services is the central business-logic project. It orchestrates extractors, integrates multiple LLM providers (OpenAI, Anthropic, Gemini, local), performs semantic search via ONNX embeddings, manages a structured memory/knowledge graph, handles repository discovery (GitHub, GitLab, local folders), runs code health and security analysis, generates AI-powered code reviews and summaries, manages wiki content, and links cross-repository dependencies.

5. **API host** – CodeGraph.Api is an ASP.NET Core Web API that exposes REST endpoints for graph querying, project/repository management, analysis orchestration, wiki management, memory graph operations, and AI-assisted code review. It also hosts MassTransit message consumers that react to internal domain events (indexing completed, analysis batch submitted, memory claims stored) to drive post-processing workflows such as cross-repo linking and community detection. It wires together the service layer, message bus, and HTTP surface consumed by the Angular frontend.

6. **Background jobs layer** – CodeGraph.Jobs is a .NET Generic Host worker service that polls a persistent schedule store and dispatches named job types (discover repositories, re-index, batch analysis, cross-repo linking, community detection, MCP doc regeneration) on cron or on-demand schedules. It depends on CodeGraph.Services and CodeGraph.Data.

7. **Frontend and tooling** – CodeGraphWeb is an Angular single-page application for exploring repository structure, health metrics, security findings, reviews, memory/knowledge graphs, clusters, and wiki documentation. tools.ts-extractor is a standalone TypeScript tool that the TypeScript extractor project communicates with over HTTP to delegate parsing and linting work.

8. **Test projects** – CodeGraph.Tests and CodeGraph.Jobs.Tests provide xUnit unit and integration tests, backed by in-memory fakes, covering extractors, graph stores, analysis services, memory services, review services, API controllers, repository providers, job dispatching, and scheduling.

External dependencies include: Neo4j graph database (sole persistence backend), OpenAI/Anthropic/Gemini APIs and a local LLM option (AI analysis), MassTransit message bus, ONNX runtime (semantic embeddings), Roslyn compiler platform (C# extraction), Tree-sitter native library (polyglot extraction), Microsoft TSqlFragment (SQL extraction), and GitHub/GitLab APIs (repository discovery). No cross-repository dependencies to other external repositories were identified in the analysis.

## Projects

### CodeGraph.Services

CodeGraph.Services is the core service layer of the CodeGraph repository, providing the business logic for indexing source code repositories into a graph database, analyzing code health and security, generating AI-powered code reviews and summaries, and exposing graph query capabilities. It orchestrates extractors, multiple LLM provider integrations (OpenAI, Anthropic, Gemini, and local), repository discovery (GitHub, GitLab, folder-based), semantic search via ONNX embeddings, a structured memory graph, wiki management, and cross-repository dependency linking. This project serves as the primary backend implementation layer, consumed by API controllers and background job processors in the broader repository.

### CodeGraph.Data

CodeGraph.Data is the data access layer (DAL) for the CodeGraph repository. It defines all persistence interfaces and entity/DTO types used to store and retrieve code graph data (nodes, edges, repositories, analysis results, metrics, reviews, wiki content, memory graphs, job schedules, and vector embeddings) against a Neo4j graph database backend. It acts as the contract boundary between domain services and storage implementations, with Neo4j implementations living in a sibling project (CodeGraph.Data.Neo4j) and in-memory test fakes in test projects.

### CodeGraph.Models

CodeGraph.Models is a shared data model library for the CodeGraph repository, providing plain data transfer objects (DTOs), message contracts, request/response types, exception types, and serialization configuration used across the system. It defines the schema for a knowledge graph memory subsystem (entities, claims, evidence, subgraph traversal), messaging contracts for repository processing pipelines (indexing, analysis, synthesis), and HTTP API request/response shapes. Other projects in the repository consume these types for message bus communication, API handling, and memory graph operations.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API host for the CodeGraph system, responsible for exposing REST endpoints that cover code graph querying, project/repository management, analysis orchestration, wiki management, memory/knowledge graph operations, and AI-assisted code review. It also hosts a suite of MassTransit message consumers that react to internal domain events (e.g., repository indexing completed, analysis batch submitted, memory claims stored) to drive post-processing workflows such as cross-repo linking, community detection, and batch analysis coordination. The project acts as the central integration point, wiring together domain services, a message bus, and HTTP endpoints that front-end clients and other system components consume.

### CodeGraph.Tests

CodeGraph.Tests is the automated test project for the CodeGraph repository, providing unit and integration tests for all major subsystems including code extractors, graph data stores, analysis services, memory services, review services, API controllers, and repository providers. It contains a large set of in-memory fake and stub implementations of core interfaces (IGraphStore, IMemoryGraphStore, IAnalysisModelProvider, IWikiStore, etc.) used to isolate system under test components without external dependencies. The project follows xUnit conventions and covers behavior ranging from Roslyn/SQL/TreeSitter code extraction to AI batch analysis, security scanning, cross-repo graph linking, and memory retrieval.

### CodeGraph

CodeGraph is a code intelligence platform that analyzes, indexes, and visualizes software repositories as graph structures. The project provides an Angular web frontend (CodeGraphWeb) for exploring repository structure, health, security, reviews, memory/knowledge graphs, clusters, and wiki documentation, backed by backend services that extract, index, and analyze .NET and TypeScript codebases. A TypeScript extractor tool (tools.ts-extractor) parses TypeScript projects into graph nodes and edges, while backend job infrastructure handles batch processing, community detection, and repository indexing.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is an xUnit test project that validates the job scheduling and dispatching infrastructure of the CodeGraph system. It contains unit tests for JobCommandDispatcher, JobScheduleService, and specific job implementations (DiscoverRepositoriesJob, ProcessBatchAnalysisJob), supported by in-memory fakes and recording test doubles for the underlying store and service interfaces.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution service for the CodeGraph system. It defines a set of named job types (discover repositories, re-index, batch analysis, link/detect, community detection, MCP doc regeneration), dispatches them by name with JSON-serialized arguments, and runs them on cron-based or on-demand schedules managed through a persistent store. The project hosts a .NET Generic Host worker process that polls for due schedules and executes them, while also exposing service interfaces for external callers to manage schedules via CRUD operations.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (Microsoft's .NET compiler platform) to extract structural and semantic information from C# source files and .NET solutions. It builds a graph representation of codebases by walking syntax trees to identify types, members, dependencies, HTTP routes, DI registrations, message bus patterns, and cross-service calls. It serves as the C#-specific extraction engine within a larger CodeGraph repository that constructs knowledge graphs of software systems.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction module that uses the Tree-sitter parsing library to analyze source files in multiple programming languages (C, C++, Python, Go, Java, Ruby, Rust, PHP, Bash). It implements the ICodeExtractor interface to parse source files into graph nodes and edges representing code definitions (functions, classes, variables) and their relationships (imports, calls), feeding structured data into a broader code graph analysis system.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j persistence layer for the CodeGraph repository, implementing all data store interfaces defined in the CodeGraph.Data abstraction layer. It provides concrete Neo4j-backed implementations for storing and querying a code knowledge graph including graph nodes/edges representing code structure, AI analysis results, memory entities/claims, vector embeddings, wiki content, job schedules, and repository metadata. All database interactions use the Neo4j .NET driver via a shared session factory, with Cypher queries executed through async sessions.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL-specific code extraction plugin that parses SQL script files and builds a structural graph of database objects (tables, views, procedures, functions, indexes) and their relationships. It implements the ICodeExtractor interface, making it a pluggable extractor within the larger CodeGraph repository. The project uses Microsoft's TSqlFragment AST visitor pattern to traverse parsed SQL and emit graph nodes and edges representing schema definitions and cross-object dependencies.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data (nodes and edges) from TypeScript source files and projects. It operates by spawning and managing a long-running TypeScript language server process (via HTTP), delegating extraction and linting work to that server, and translating the results into the repository's shared graph model (GraphNode, PendingEdge, ExtractionResult). It implements the ICodeExtractor and ITypeScriptAnalyzer service interfaces, integrating TypeScript analysis into the broader CodeGraph pipeline.

