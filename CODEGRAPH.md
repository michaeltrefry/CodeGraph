# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-29 16:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that ingests software repositories, constructs semantic graph representations of codebases, and exposes analysis, search, and AI-assisted features through a layered architecture. The repository is organized into the following cooperating subsystems: (1) A set of language-specific extractors (C#/Roslyn, TypeScript/Node.js, SQL, Terraform/HCL, Ansible/YAML, ColdFusion/regex, C/C++/Tree-sitter) that each implement a shared ICodeExtractor interface and produce normalized graph nodes and edges from source files. (2) An indexing pipeline (CodeGraph.Indexer.Host, CodeGraph.Indexer.Client, CodeGraph.Jobs) that orchestrates extraction, stores results in a graph database, schedules recurring jobs (indexing, community/cluster detection, batch LLM analysis, MCP doc regeneration), and publishes lifecycle events over a MassTransit message bus. (3) A dual-backend persistence layer: CodeGraph.Data defines all storage interfaces and DTOs; CodeGraph.Data.MariaDb implements them using Entity Framework Core against MariaDB/MySQL (relational data, vector store, migrations); CodeGraph.Data.Neo4j implements them using Cypher queries against Neo4j (graph traversal, code nodes/edges, cross-repo relationships). (4) A core services layer (CodeGraph.Services) providing LLM-driven batch analysis, code review, an AI assistant (OpenAI/Anthropic/LM Studio backends), semantic search via ONNX embeddings, memory/knowledge graph management, wiki generation, cross-repository link detection, MCP server integration, and administrative reporting. (5) A REST API host (CodeGraph.Api) exposing controllers for projects, nodes, search, reviews, AI assistant, wiki, admin, and MCP tokens, plus MassTransit consumers reacting to pipeline events. (6) A memory graph subsystem (CodeGraph.Memory.Host, CodeGraph.Memory.Client) providing a standalone service for storing, querying, and managing structured knowledge claims as a graph, consumed by the main API and services. (7) A metrics subsystem (CodeGraph.Metrics) that subscribes to LLM usage and tool invocation events and records them persistently. (8) Shared infrastructure (CodeGraph.Host.Shared, CodeGraph.Models) providing HMAC-based internal service authentication, cross-cutting DI registrations, and canonical DTO/message contract types used across all services. (9) An Angular web frontend (CodeGraphWeb) and a TypeScript extractor tool. (10) Test projects (CodeGraph.Tests, CodeGraph.Jobs.Tests) with extensive in-memory fakes and recording test doubles covering all major interfaces. Externally, the repository makes HTTP calls to a service identified as TC.CodeGraphApi, and is itself called by a system named CodeFlow, indicating CodeGraph acts as a backend dependency for at least one downstream consumer.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code intelligence platform that analyzes software repositories, builds graph-based representations of codebases, and provides features including code search, impact analysis, security review, LLM-assisted chat, wiki documentation, cluster detection, and memory/knowledge management. The repository contains a .NET backend (services, jobs, data stores, migration utilities) and an Angular frontend (CodeGraphWeb) with a TypeScript extractor tool, all working together to index, analyze, and visualize code graphs.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API layer for the CodeGraph platform — a code intelligence system that indexes source repositories, builds a semantic graph of code nodes, runs LLM-based analysis/review, maintains a memory graph, and exposes all of this through a REST API. It hosts controllers covering projects, nodes, search, code reviews, an AI assistant, wiki, admin configuration, and MCP token management, while also running MassTransit message consumers that react to indexer and analysis pipeline events.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining the full set of storage abstractions (interfaces) and entity/DTO classes used throughout the system. It declares contracts for persisting and querying code graph nodes, edges, analysis results, LLM configurations, assistant runs, wiki content, memory graphs, metrics, reviews, job schedules, and administrative data, with concrete implementations living in sibling projects (MariaDb, Neo4j). This project acts as the shared contract boundary between application services and storage backends.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph system. It provides concrete implementations of all data store interfaces (graph store, analysis store, metrics store, review store, memory store, assistant runs, admin, wiki, vector store, etc.) using Entity Framework Core via CodeGraphDbContext and raw MySqlConnector queries. The project also handles database migrations, connection string encryption, LLM configuration persistence, and dependency injection registration for all MariaDB-backed stores.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IMemoryGraphStore, IVectorStore, IWikiStore, IJobScheduleStore, etc.) by translating domain model operations into Cypher queries executed against a Neo4j graph database. Its primary responsibility is storing and querying code graph structures (nodes, edges, repositories, cross-repo edges), AI analysis results, memory/knowledge graph entities, wiki content, job schedules, and vector embeddings, all persisted as Neo4j nodes and relationships.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a project responsible for parsing Ansible YAML files and extracting structured graph data (nodes, edges, relationships) from them. It classifies Ansible file types (playbooks, roles, variables, handlers, etc.), identifies tasks, modules, roles, variables, and cross-repository references, and produces an ExtractionResult that feeds into a larger code graph analysis system. It implements the shared ICodeExtractor interface, acting as the Ansible-specific extraction plugin within the CodeGraph repository.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files and extracts structural code graph elements such as components, functions, SQL queries, HTTP calls, include directives, and object invocations. It implements the shared ICodeExtractor interface, making it a pluggable analyzer within a broader code graph/dependency analysis system. The project uses regex-based parsing to identify and catalog ColdFusion-specific constructs and their relationships.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural metadata from C# solutions and source files. It traverses syntax trees to identify classes, methods, interfaces, routes, dependency injection registrations, messaging patterns, and cross-service calls, producing structured ExtractionResult models consumed by the broader CodeGraph system. It serves as the C#-specific extraction backend, implementing shared extractor interfaces defined in a services layer.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information from them. It identifies SQL objects such as tables, views, stored procedures, functions, and indexes, along with their relationships (e.g., table references within procedures), and produces an ExtractionResult representing the code graph. It implements the ICodeExtractor interface, integrating with the broader CodeGraph repository's extraction pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis component that parses HashiCorp Configuration Language (HCL) Terraform files and extracts structural graph information from them. It identifies Terraform constructs such as resources, modules, data sources, variables, outputs, and locals, then builds a graph representation of their relationships and dependencies. This project implements the ICodeExtractor interface, integrating into a larger CodeGraph system that maps code and infrastructure-as-code relationships.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction project that uses the Tree-sitter parsing library to analyze source files in multiple languages (at minimum C and C++). It traverses syntax trees to extract code definitions (classes, functions, etc.) and import/dependency relationships, producing graph nodes and edges that feed into the broader CodeGraph repository model. It implements the ICodeExtractor interface, integrating with the shared extraction pipeline.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a .NET project responsible for extracting code graph data (nodes and edges) from TypeScript source files and projects. It manages a long-running external Node.js/TypeScript analysis server process, communicates with it to parse TypeScript code and perform linting, and maps the results into the CodeGraph domain model (GraphNode, PendingEdge, ExtractionResult) for consumption by the broader CodeGraph system.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing cross-cutting concerns for host services in the CodeGraph repository. It implements internal service-to-service authentication using HMAC-signed tokens, and exposes DI registration extensions to wire up authentication and shared hosting configuration across multiple services.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP client for communicating with a CodeGraph indexer service. It exposes operations for triggering indexing jobs (repository processing, schema sync, link detection, community detection, discovery, re-indexing) and querying their run status, and integrates with ASP.NET Core's dependency injection via extension methods.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web application that serves as the hosting layer for a code graph indexing service. It exposes HTTP API endpoints for triggering repository indexing, schema synchronization, cross-repository linking, and community detection operations, while also consuming asynchronous messages via MassTransit to orchestrate a multi-stage pipeline that processes repositories, performs batch analysis, stores results in a graph store, and handles lifecycle events like repository removal.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution subsystem for the CodeGraph repository. It defines a set of concrete job implementations (indexing, community detection, batch analysis, MCP doc regeneration, etc.), a dispatcher that routes job-type strings to the correct typed handler, and a cron-based schedule service that persists schedules, computes next-run times, and drives execution via a hosted worker. The project also exposes DI registration extensions and an entry-point, suggesting it can run as a standalone worker process.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the CodeGraph.Jobs scheduling and dispatch infrastructure. It contains test doubles (recording/in-memory fakes) for external service dependencies and test classes that verify job scheduling, lease acquisition, command dispatching, and argument normalization behaviors without requiring real infrastructure.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides a typed .NET interface for interacting with a remote memory graph service. It exposes operations for querying, searching, reading, writing, and deleting graph entities and claims on behalf of named users, along with diagnostics and write-status tracking. The project also includes DI registration extensions, making it easy to wire up the client in ASP.NET Core or other dependency-injection-based hosts.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API host that provides a memory graph service — a system for storing, querying, and managing structured knowledge claims organized as a graph of entities and relationships. It exposes a REST API for operations such as storing memory claims, querying graph neighborhoods, searching entities, rendering summaries, expanding knowledge frontiers, and performing diagnostics/cleanup. It also integrates with a message bus (MassTransit) to consume async claim-store messages, and supports pluggable persistence backends (including MariaDB).

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project related to in-memory data migration within the CodeGraph repository. Based on the project name, it likely handles migration of code graph data structures in memory, possibly transforming or upgrading graph representations between versions or formats. The project's role in the repository is to support data migration operations without requiring persistent storage changes.

### CodeGraph.Metrics

CodeGraph.Metrics is a microservice responsible for collecting and persisting metrics events within the CodeGraph repository. It subscribes to domain messages (such as LLM usage and MCP tool invocations) via MassTransit message consumers, records those events through a metrics recording abstraction, and manages its own persistence layer with database migrations. It exposes an ASP.NET Core web application with internal service authentication.

### CodeGraph.Models

CodeGraph.Models is a shared data model library that defines the DTOs, message contracts, request/response objects, and domain entities used across the CodeGraph repository. It serves as the canonical type definitions for inter-service communication (message bus consumers), API contracts, memory graph operations, and analysis pipeline coordination.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform, a tool for indexing, analyzing, and querying software repositories as graphs. It provides services for code ingestion (via an indexing pipeline that extracts AST-level nodes and edges from multiple languages), LLM-driven analysis (batch analysis, code review, repository vitals), an AI assistant backed by multiple LLM providers (OpenAI, Anthropic, LM Studio), memory/knowledge graph management, cross-repository dependency linking, semantic search via ONNX embeddings, wiki management, MCP (Model Context Protocol) server integration, and administrative reporting — all wired together through a service-oriented architecture with dependency injection.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository, covering unit and integration tests across all major subsystems including API controllers, data stores, code extractors, memory services, LLM configuration, indexing pipelines, review services, and metrics. It provides a rich set of in-memory and recording test doubles (fakes, stubs, recording implementations) for every major interface in the production codebase, enabling isolated testing without real database or external service dependencies. The project serves as both a correctness harness and a living contract specification for all service interfaces.

### reflect

The 'reflect' project appears to be a Java reflection utility or wrapper library within the CodeGraph repository. Based on the project name and typical patterns for such utilities, it likely provides abstractions or helpers for working with Java's reflection API, though the graph contains no node details to analyze further.

