# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-18 23:06 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-component code intelligence and graph analysis platform implemented primarily in .NET (ASP.NET Core) with an Angular frontend. Its core function is to ingest source code repositories across multiple languages (C#, TypeScript, Rust, SQL, ColdFusion, Ansible, Terraform, C/C++) via a suite of language-specific extractors, build graph representations of the code structure (nodes, edges, dependencies, clusters), persist those graphs to either Neo4j or MariaDB/MySQL, and expose the results through REST APIs and an Angular web UI.

The repository is organized as a layered system: (1) Extractor plugins (CodeGraph.Extractors.*) parse source files for each supported language using tools like Roslyn, Tree-sitter, SCIP, and custom YAML/SQL parsers, producing structured ExtractionResult models. (2) The indexer host (CodeGraph.Indexer.Host) orchestrates the indexing pipeline — repository discovery, extraction, graph storage, schema sync, cross-repo linking, and community detection — coordinating work via MassTransit message consumers. (3) The data layer (CodeGraph.Data, CodeGraph.Data.MariaDb, CodeGraph.Data.Neo4j) provides technology-agnostic store interfaces with two concrete backends. (4) The services layer (CodeGraph.Services) implements core business logic including LLM-powered analysis (OpenAI, Anthropic, LM Studio), security scanning, wiki management, AI assistant orchestration, and an MCP (Model Context Protocol) server. (5) The API host (CodeGraph.Api) exposes all capabilities as REST endpoints and hosts async message consumers for post-indexing workflows. (6) Supporting microservices include CodeGraph.Memory.Host (graph-based knowledge/claims store), CodeGraph.Metrics (LLM/MCP usage telemetry), CodeGraph.Jobs (CRON-based job scheduling), and CodeGraph.Mcp.Hub (a policy-enforcing gateway that brokers AI tool calls to MySQL, RabbitMQ, and Shortcut backends). Client libraries (CodeGraph.Indexer.Client, CodeGraph.Memory.Client) provide typed HTTP access to the indexer and memory services respectively. CodeGraph.Models is the shared DTO/message contract library consumed across all components. CodeGraph.Host.Shared provides HMAC-based service-to-service authentication. CodeGraph.Tests is a comprehensive unified test project covering all major subsystems.

Externally, this repository makes HTTP calls to its own API (TC.CodeGraphApi) and is consumed by at least two external repositories — CodeFlow and codeflow-team — both of which make HTTP calls into the CodeGraph API, suggesting CodeGraph serves as a code intelligence backend for those downstream systems.

## Languages

- **C#**: 81 % of non-blank LOC (81,323 LOC, 609 files)
- **TypeScript**: 17 % of non-blank LOC (17,562 LOC, 84 files)

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a multi-component repository that provides code graph analysis, indexing, and visualization capabilities. It includes a backend (.NET) that builds and queries graph representations of codebases (supporting multiple languages like C#, TypeScript, Rust), an Angular frontend (CodeGraphWeb) for exploring repositories, clusters, memory graphs, wikis, and AI assistant features, and tooling (ts-extractor) for extracting TypeScript project structure. The system also integrates LLM-based analysis, MCP (Model Context Protocol) hub functionality, and wiki/documentation management.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API host for the CodeGraph platform — a code intelligence and analysis system. It exposes REST endpoints for querying a code graph (nodes, projects, clusters, schemas), triggering indexing/analysis operations, managing LLM provider configurations, running an AI assistant, conducting code reviews, managing a wiki, and operating an MCP (Model Context Protocol) hub. It also hosts message consumers that react to asynchronous events published via a message bus (MassTransit), coordinating post-indexing workflows such as community detection, batch analysis, memory claim storage, and wiki embedding.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all persistence interfaces (stores/repositories) and their corresponding entity/DTO classes. It provides a technology-agnostic contract layer that is implemented by concrete backends (Neo4j, MariaDB/MySQL) and consumed by services throughout the system. The project covers graph storage, analysis results, LLM configuration, assistant runs, wiki content, MCP hub tooling, metrics, review workflows, job scheduling, and memory graph operations.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph platform. It implements all data store interfaces defined in the CodeGraph.Data abstraction layer using EF Core (via CodeGraphDbContext) and raw MySqlConnector for performance-sensitive bulk operations. The project handles storage and retrieval for code graph nodes/edges, LLM analysis results, AI assistant runs, memory graph structures, metrics, reviews, wiki content, MCP hub configuration, job scheduling, and database migrations.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph nodes/edges, analysis batches, memory graph, vector embeddings, wiki, job scheduling, etc.) by translating between the application's domain models and Neo4j Cypher queries via the Neo4j .NET driver. This project acts as the single integration point between the rest of the codebase and the Neo4j graph database.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a plugin/extractor component that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and extracts a structured graph representation of the Ansible infrastructure code. It implements the ICodeExtractor interface, integrating into a broader CodeGraph system that builds a code/infrastructure knowledge graph from source files.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a specialized extraction module that parses ColdFusion (.cfm/.cfc) source files and produces a structured ExtractionResult representing the code graph elements found within them. It identifies and extracts ColdFusion-specific constructs such as components, functions, cfinclude directives, cfquery SQL blocks (including table references), cfhttp calls, createObject calls, and cfinvoke invocations, feeding these into the broader CodeGraph analysis pipeline via the ICodeExtractor contract.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# static analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic information from C# solutions and source files. It walks syntax trees to identify classes, methods, routes, dependency injection registrations, service bus interactions, NuGet dependencies, and cross-service calls, producing structured ExtractionResult models consumed by the broader CodeGraph system. Its role is to serve as the C#-specific code intelligence backend, implementing shared extractor interfaces defined elsewhere in the repository.

### CodeGraph.Extractors.Rust

CodeGraph.Extractors.Rust is a project responsible for extracting code graph data from Rust projects. It orchestrates the generation or reading of SCIP (Stack graphs Index Protocol) JSON files from Rust codebases using Cargo tooling, then parses and imports that SCIP JSON into the CodeGraph model as nodes, edges, and extraction results.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL script files and extracts structural graph information from them. It identifies SQL objects such as tables, views, stored procedures, functions, and indexes, then builds a code graph representation (ExtractionResult) suitable for use in the broader CodeGraph repository. It handles both standard T-SQL syntax and includes normalization for MySQL-specific syntax.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis plugin that parses Terraform/HCL infrastructure-as-code files and extracts their structural elements (resources, modules, variables, outputs, data sources, locals) into a graph representation. It implements the ICodeExtractor interface from the broader CodeGraph repository, enabling Terraform files to be analyzed alongside other code artifacts by producing GraphNode objects and edges that represent dependencies and cross-references between infrastructure components.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files across multiple programming languages (including C and C++). It implements the ICodeExtractor interface to parse source files into an abstract syntax tree, then walks those trees to extract definitions (classes, functions, etc.) and import/dependency relationships, producing graph nodes and edges for the broader CodeGraph repository analysis system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data (nodes and edges) from TypeScript source files and projects. It manages a long-running Node.js server process to perform TypeScript-specific analysis (parsing, extraction, and linting), and bridges the results into the shared CodeGraph model types used by the rest of the repository.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing authentication and hosting utilities for internal services within the CodeGraph repository. It implements a lightweight token-based authentication mechanism (using HMAC-signed tokens) for service-to-service communication, along with DI registration extensions for configuring these capabilities in ASP.NET Core host applications.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP interface for communicating with a CodeGraph indexer service. It exposes operations for triggering and monitoring indexing pipeline stages such as repository discovery, batch analysis processing, schema synchronization, graph linking, and community detection. The project is intended to be consumed by other projects in the repository (e.g., jobs, tests) via dependency injection.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web host that orchestrates code repository indexing and graph construction. It exposes a REST API for triggering indexing operations (repository processing, schema sync, cross-repo linking, community detection, batch analysis) and uses MassTransit message consumers to coordinate an event-driven pipeline that processes repositories, stores results in a graph store, and performs post-indexing analysis. It also manages a TypeScript sidecar process used during analysis.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project that defines a set of named job types (repository indexing, community detection, embedding ingestion, etc.), dispatches them by type string, and runs them on CRON-based schedules managed via a persistent schedule service. It acts as the job orchestration layer of the CodeGraph repository, wiring together a hosted worker, a command dispatcher, and CRUD schedule management into a self-contained .NET worker-service application.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the job scheduling and dispatching subsystem of the CodeGraph repository. It provides in-memory fakes and recording test doubles for external service dependencies (job store, admin service, indexer client, batch analysis service, etc.) and contains test classes that verify the behavior of job command dispatching, job schedule lifecycle management, and specific job types like discover-repositories and process-batch-analysis.

### CodeGraph.Mcp.Hub

CodeGraph.Mcp.Hub is a Model Context Protocol (MCP) hub/gateway service that brokers AI tool calls to multiple backend providers including MySQL databases, RabbitMQ message brokers, and the Shortcut project management API. It manages tool catalogs, access control via token entitlements, credential storage, audit logging, sensitive column policies for SQL query filtering, and shim-based forwarding to downstream MCP servers — acting as a centralized, policy-enforcing middleware layer between AI clients and data/service providers.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides typed access to a remote memory graph service. It defines an interface and concrete implementation for querying, writing, and managing a knowledge graph of entities, claims, and relationships on behalf of named users. The project also includes DI registration extensions, a custom exception type, and a configuration options class, making it a self-contained client SDK for other services in the CodeGraph repository to consume.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API service that manages a graph-based memory system for storing, querying, and managing knowledge claims and entities. It exposes REST endpoints for memory operations (storing claims, querying graphs, searching, cleanup, diagnostics) and integrates with MassTransit message bus for asynchronous claim ingestion. The service acts as the hosted entry point for a memory graph subsystem, handling both synchronous HTTP requests and asynchronous message consumption.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository that handles in-memory data migration operations. Based on the project name, it likely provides infrastructure for migrating or transforming data structures in memory, potentially as part of a code graph analysis or transformation pipeline. However, no graph nodes or source snippets were provided, limiting detailed analysis.

### CodeGraph.Metrics

CodeGraph.Metrics is a background service/microservice within the CodeGraph repository responsible for collecting and persisting metrics events. It consumes messages from a message bus (MassTransit) — specifically LLM usage and MCP tool invocation events — and records them via a metrics event recorder, likely persisting them to a database (with MariaDB/SQL migration support configured at startup).

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines all DTOs, message types, request/response models, and exception classes used across the CodeGraph system. It serves as the central schema layer consumed by multiple services including the indexer, API, memory, and metrics hosts, enabling decoupled communication via message bus consumers and typed HTTP contracts.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a system that indexes source code repositories into a graph database, performs AI-powered analysis using multiple LLM providers (OpenAI, Anthropic, LM Studio), and exposes the resulting graph through query, review, and assistant services. It orchestrates everything from file discovery and extraction through cross-repo linking, community detection, security scanning, vitals analysis, memory management, wiki management, and an MCP (Model Context Protocol) server for AI assistant tool access.

### CodeGraph.Tests

CodeGraph.Tests is the unified test project for the CodeGraph repository, containing unit and integration tests covering API controllers, service logic, data stores, extractors, memory operations, MCP (Model Context Protocol) infrastructure, and indexer pipelines. It provides a comprehensive suite of test doubles (fakes, recording/spy implementations, stubs, and no-op implementations) for every major interface in the system, plus an in-memory graph store that fully implements the storage contracts without a real database. The project exercises correctness of extractors (Roslyn, SQL, Terraform, Ansible, ColdFusion, Rust SCIP), LLM configuration resolution, review services, security analysis, wiki management, and cross-cutting concerns like telemetry middleware and admin authentication.

### reflect

The 'reflect' project appears to be a reflection or introspection utility library within the CodeGraph repository. Based on its name and position in the repository, it likely provides mechanisms for inspecting, traversing, or analyzing code structure and type information at runtime or statically. The project's role is likely to support the broader CodeGraph system's ability to understand and navigate code relationships.

