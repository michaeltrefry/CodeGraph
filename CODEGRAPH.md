# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-15 10:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-component platform for analyzing, indexing, and navigating software codebases as graph structures. It consists of roughly 20+ projects organized into clear layers: extraction, persistence, services, hosting, and tooling.

Extraction layer: Language-specific extractor projects (CSharp via Roslyn, TypeScript via a Node.js sidecar, SQL/T-SQL, Terraform/HCL, Ansible/YAML, ColdFusion, and generic Tree-sitter-based parsers for C/C++ and others) each implement an ICodeExtractor interface and produce a normalized ExtractionResult containing graph nodes and edges representing code/infrastructure structure and dependencies.

Data layer: CodeGraph.Data defines all storage interfaces and entity/DTO types. Two concrete backends are provided — CodeGraph.Data.Neo4j (Cypher-based graph storage) and CodeGraph.Data.MariaDb (Entity Framework Core + raw MySqlConnector, with AES encryption, migrations, and vector storage). Abstractions cover code graphs, analysis batches, metrics, reviews, assistant runs, MCP hub management, wiki, memory graphs, LLM configuration, and vector embeddings.

Service layer: CodeGraph.Services provides the core business logic — indexing pipelines, LLM-powered code review and analysis (OpenAI, Anthropic, LM Studio), an AI assistant with MCP tool support, memory graph management, embeddings, wiki, metrics publishing, and community detection.

Host/API layer: CodeGraph.Api is the primary ASP.NET Core Web API, wiring together authentication, message-bus consumers (MassTransit), and HTTP endpoints for external clients. CodeGraph.Indexer.Host is a dedicated indexing service exposing indexing endpoints and consuming domain events. CodeGraph.Memory.Host exposes a knowledge graph REST API and consumes memory-claim messages. CodeGraph.Metrics consumes LLM/MCP telemetry events and persists them. CodeGraph.Jobs provides cron-driven background job scheduling and dispatch for tasks like re-indexing and embedding ingestion.

MCP Hub: CodeGraph.Mcp.Hub acts as a gateway/aggregator for Model Context Protocol tool providers (MySQL, RabbitMQ, Shortcut, and shim-based servers), enforcing access control via token entitlements, auditing invocations, and shielding sensitive data — serving as the interface between AI clients and heterogeneous backend tools.

Cross-cutting: CodeGraph.Host.Shared provides HMAC-based internal service authentication and distributed tracing activity sources. CodeGraph.Models is the shared DTO/message contract library. CodeGraph.Indexer.Client and CodeGraph.Memory.Client are typed HTTP client libraries for service-to-service calls.

Frontend: An Angular web application (CodeGraphWeb) provides UI for graph exploration, project health dashboards, security analysis, AI assistant chat, wiki, admin controls, and MCP hub management.

Dependencies: The system depends on Neo4j and MariaDB/MySQL for persistence, MassTransit (likely over RabbitMQ) for messaging, OpenAI/Anthropic/LM Studio APIs for LLM capabilities, Node.js for TypeScript parsing, Roslyn for C# analysis, Tree-sitter for additional language support, and Entity Framework Core for ORM.

External dependents: The repository is called by at least two external repositories — 'CodeFlow' and 'codeflow-team' — both making HTTP calls to the CodeGraph API, suggesting CodeGraph serves as a code intelligence backend for one or more downstream developer tooling or project management systems. CodeGraph itself also makes HTTP calls to a 'TC.CodeGraphApi' endpoint, which may be a remote/tenant instance of the same API.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a multi-component repository that analyzes, indexes, and visualizes software codebases as graph structures. It includes a C#/.NET backend with services for indexing repositories (supporting .NET and TypeScript), migrating graph data between databases (Neo4j to MariaDB), running background jobs, and exposing APIs; an Angular web frontend (CodeGraphWeb) providing UI for graph exploration, project health, security analysis, AI assistant chat, wiki, admin controls, and MCP hub management; and a TypeScript extraction tool that parses TypeScript/JavaScript projects into graph node/edge representations for ingestion.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API host for the CodeGraph platform — a code intelligence system that indexes repositories, builds code graphs, runs LLM-powered analysis and reviews, manages a knowledge memory store, and exposes an MCP (Model Context Protocol) hub. It wires together persistence, authentication, message-bus consumers, and HTTP endpoints, delegating business logic to downstream services while hosting the entry points for all external clients.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all storage abstractions (interfaces) and entity/DTO types used across the system. It provides a comprehensive set of store interfaces covering code graph storage, analysis, review, metrics, assistant runs, MCP hub management, wiki, memory graphs, LLM configuration, vector storage, and more. Concrete implementations (Neo4j, MariaDb/MySQL) live in sibling projects and implement these interfaces, while services in other projects depend on them via injection.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph store, analysis store, metrics store, review store, assistant run store, memory graph store, vector store, wiki store, LLM config, admin stores, MCP hub stores, etc.) using Entity Framework Core via a CodeGraphDbContext and raw MySqlConnector for performance-critical bulk operations. It also contains database migration infrastructure, AES encryption for sensitive fields, and a DI registration extension that wires all stores into the application's service container.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph system, implementing all data store interfaces defined in the CodeGraph.Data abstractions. It provides concrete Neo4j Cypher query implementations for storing and retrieving code graph nodes/edges, analysis batches, memory graphs, wiki content, job schedules, vector embeddings, and project health/security/review data. This project acts as the infrastructure adapter between the application's domain models and a Neo4j graph database.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a specialized extractor project that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, and requirements) and builds a structured graph model of their components and relationships. It implements the ICodeExtractor interface, integrating into a broader CodeGraph system that analyzes and visualizes code/infrastructure dependencies across repositories. The project classifies Ansible file types, extracts nodes (tasks, roles, plays, variables), and identifies cross-repository edges such as role references and module invocations.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion markup and script files (.cfm/.cfc) to extract structural and behavioral metadata — including components, functions, SQL queries, HTTP calls, include directives, and object invocations — and converts them into a standardized ExtractionResult for the CodeGraph repository analysis system.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# static analysis project that extracts structural and semantic metadata from .NET solutions and source files using Roslyn (the .NET compiler platform). It analyzes solution files, restores NuGet packages, parses C# syntax trees, and produces ExtractionResult models capturing code structure including classes, methods, interfaces, routes, DI registrations, service bus consumers/publishers, HTTP client calls, and cross-service relationships. It serves as the C#-specific extraction engine within a broader CodeGraph repository that maps codebases into graph representations.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information from T-SQL/MySQL constructs. It identifies and models database objects such as tables, views, stored procedures, functions, and indexes, producing an ExtractionResult that feeds into the broader CodeGraph repository's code analysis pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a plugin/extractor project that parses Terraform (HCL) infrastructure-as-code files and produces a graph representation of their contents. It extracts Terraform constructs—resources, data sources, modules, variables, outputs, locals, and tfvars—into graph nodes and edges, enabling the broader CodeGraph repository to analyze and navigate infrastructure dependencies alongside application code.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction project that uses the Tree-sitter parsing library to analyze source files across multiple programming languages (including C, C++, and others). It implements the ICodeExtractor interface to parse source files into syntax trees, then walks those trees to extract code definitions (classes, functions, etc.) and import/dependency relationships, producing structured graph nodes and edges for use in the broader CodeGraph repository analysis system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a .NET project responsible for extracting code graph data from TypeScript source files and projects. It manages a long-running Node.js server process that performs the actual TypeScript parsing and analysis, communicates with it to extract structural information and lint results, and transforms the responses into the domain model types (GraphNode, PendingEdge, ExtractionResult) used by the broader CodeGraph repository.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing cross-cutting concerns for host services in the CodeGraph repository. It implements internal service-to-service authentication using HMAC-signed tokens, exposes DI registration extensions for wiring up auth and host configuration, and defines activity sources for distributed tracing. The project serves as a shared foundation consumed by multiple host-level services within the repository.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP client for communicating with a CodeGraph Indexer service. It encapsulates all HTTP communication details (request building, error handling, JSON serialization) and exposes high-level async operations for triggering indexing workflows such as repository discovery, batch analysis, schema synchronization, community detection, and run status monitoring. The project is intended to be consumed by other services or applications within the repository via dependency injection.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web application that serves as the hosting layer for a code graph indexing system. It exposes HTTP API endpoints for triggering indexing operations (repository processing, schema sync, community detection, cross-repo linking, batch analysis) and hosts MassTransit message consumers that react to domain events such as repository processing, analysis completion, and removal. The project orchestrates the full indexing pipeline by coordinating between a graph store, project/analysis services, and a TypeScript sidecar process.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project responsible for defining, dispatching, and scheduling various asynchronous jobs within the CodeGraph system. It provides a command dispatcher pattern for routing named job types to their implementations, a scheduling service backed by cron expressions and time zones, and a hosted worker service that continuously polls for and runs due scheduled jobs. The project serves as the operational backbone for tasks like repository indexing, community detection, embedding ingestion, and AI-assistant data management.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the job scheduling and dispatching infrastructure in the CodeGraph system. It provides in-memory fakes, recording test doubles, and test fixtures to verify that job scheduling, lease acquisition, command dispatching, and background task execution behave correctly without requiring real external dependencies.

### CodeGraph.Mcp.Hub

CodeGraph.Mcp.Hub is a hub/gateway service for the Model Context Protocol (MCP) that aggregates and proxies multiple backend tool providers (MySQL, RabbitMQ, Shortcut, and shim-based MCP servers). It manages a catalog of tools and providers, enforces access control via token entitlements and provider policies, handles auditing of tool invocations, and protects sensitive data through column-level exposure policies. It serves as the central orchestration layer that AI clients interact with to discover and invoke tools across heterogeneous backends.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides typed access to a memory graph API service. It exposes operations for querying, searching, storing, and managing a graph-based memory store (entities, claims, relationships, subgraphs) over HTTP, scoped per user. The project also provides DI registration extensions so consuming applications can configure and inject the client via the standard .NET service collection.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API host that exposes a memory graph service. It provides REST endpoints for storing, querying, searching, and managing a knowledge graph of memory claims and entities, and also consumes messages from a MassTransit message bus to asynchronously ingest memory claims. The project handles persistence registration, authentication middleware, and health probing for the write pipeline.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository that handles in-memory data migration operations. Based on the project name, it likely provides utilities or infrastructure for migrating data structures, schemas, or state in memory rather than through persistent storage mechanisms.

### CodeGraph.Metrics

CodeGraph.Metrics is a microservice responsible for collecting and persisting metrics/telemetry events within the CodeGraph repository. It consumes messages from a message bus (MassTransit) such as LLM usage and MCP tool invocation events, records them via a metrics event recorder, and persists them to a database (with support for MariaDB migrations). It exposes a web application host with internal service authentication and standard retry policies for message consumption.

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines the DTOs, message types, request/response models, and exception types used across the CodeGraph repository. It serves as the common language between services such as the indexer, API, memory, and metrics hosts, providing strongly-typed message classes for message bus consumers, memory graph structures, and HTTP request/response payloads.

### CodeGraph.Services

CodeGraph.Services is the core service layer of the CodeGraph platform, providing business logic for indexing and analyzing code repositories, running LLM-powered code reviews and analysis, managing a knowledge graph of code structure, operating an AI assistant with MCP (Model Context Protocol) tool support, and maintaining a memory graph store. It serves as the central orchestration layer between the data layer (CodeGraph.Data), external LLM providers (OpenAI, Anthropic, LM Studio), and various API/host entry points. The project encompasses extractors, analyzers, query services, configuration resolvers, wiki management, embeddings, and metrics publishing.

### CodeGraph.Tests

CodeGraph.Tests is the test project for the CodeGraph repository, providing unit, integration, and contract tests for all major subsystems including graph extraction, data persistence, API controllers, services, memory operations, MCP (Model Context Protocol) tooling, LLM configuration, indexing pipelines, and security analysis. It contains a large library of in-memory fakes, recording stubs, and throwing stubs that implement production store and service interfaces, enabling isolated unit testing without real databases or external services. The project spans tests for MariaDB store implementations, Roslyn/SQL/Terraform/Ansible/ColdFusion/TreeSitter extractors, review services, assistant runs, wiki, metrics, and MCP hub functionality.

### reflect

The 'reflect' project appears to be a reflection or introspection utility library within the CodeGraph repository. Based on the project name and its role in the repository structure, it likely provides mechanisms for examining and manipulating code structure, types, or metadata at runtime or analysis time. The project is minimal or the graph data provided contains no node definitions to describe.

