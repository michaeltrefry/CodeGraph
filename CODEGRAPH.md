# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-29 12:08 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code analysis and knowledge graph platform that ingests software repositories, parses their source files across multiple languages, and stores the resulting structural and semantic metadata as a graph. The repository is organized as a multi-project .NET solution with a companion Angular frontend (CodeGraphWeb) and a TypeScript AST extraction tool.

Core responsibilities break down as follows:

**Extraction layer**: Language-specific extractor projects (CodeGraph.Extractors.CSharp, .TypeScript, .Sql, .Ansible, .ColdFusion, .Terraform, .TreeSitter) each implement a shared ICodeExtractor interface. They parse source files using Roslyn, Tree-sitter, or custom YAML/HCL parsers, and emit normalized GraphNode and PendingEdge objects that represent code symbols and their relationships.

**Indexing and orchestration**: CodeGraph.Indexer.Host is an ASP.NET Core service that exposes REST endpoints for triggering indexing operations (discovery, processing, schema sync, community detection, cross-repo linking, batch analysis) and listens on a MassTransit message bus for lifecycle events. CodeGraph.Indexer.Client provides a typed HTTP client for other services to invoke the indexer.

**Persistence**: CodeGraph.Data defines all store interfaces and entity models. Two concrete backends are provided: CodeGraph.Data.Neo4j (primary graph store using Cypher queries) and CodeGraph.Data.MariaDb (relational store using Entity Framework Core with MySqlConnector), covering code graph nodes/edges, analysis results, reviews, metrics, wiki content, job schedules, vector embeddings, and memory graphs.

**API host**: CodeGraph.Api is the primary ASP.NET Core Web API consumed by the frontend and external callers. It exposes endpoints for repository management, LLM-powered analysis and assistant chat, memory/knowledge graph operations, wiki, and administration, while also consuming message bus events to coordinate background pipelines.

**Background jobs**: CodeGraph.Jobs implements a cron-based scheduling engine and a dispatcher-based command pattern for named jobs (repository indexing, community detection, MCP doc regeneration). A hosted worker polls for due schedules and dispatches them.

**Memory service**: CodeGraph.Memory.Host is a standalone ASP.NET Core microservice exposing a graph-based memory store (entities and claims with per-user namespacing). CodeGraph.Memory.Client provides a typed HTTP client for consuming it.

**Metrics service**: CodeGraph.Metrics is a microservice that subscribes to LLM usage and MCP tool invocation messages via MassTransit and persists telemetry to MariaDB.

**Shared infrastructure**: CodeGraph.Models is the central shared-kernel library (DTOs, message types, request/response objects). CodeGraph.Host.Shared provides cross-cutting concerns (HMAC-based internal auth, OpenTelemetry). CodeGraph.Services contains the core business logic (extraction pipelines, LLM integration via OpenAI/Anthropic/LM Studio, semantic search via ONNX embeddings, security scanning, code review workflows).

**Testing**: CodeGraph.Tests and CodeGraph.Jobs.Tests provide unit and integration test coverage using in-memory fakes and recording stubs for all major subsystems.

**External dependencies**: The platform integrates with Neo4j (graph database), MariaDB/MySQL (relational store), a MassTransit-compatible message broker, OpenAI/Anthropic/LM Studio APIs for LLM features, and ONNX Runtime for local embeddings. It also spawns a child Node.js process for TypeScript AST analysis.

**Dependents**: An external service identified as 'CodeFlow' makes HTTP calls into CodeGraph, and CodeGraph itself makes HTTP calls to a service identified as 'TC.CodeGraphApi', suggesting CodeGraph is both a provider and consumer in a broader ecosystem of interconnected tools.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code analysis and knowledge graph platform. The backend (C#) indexes source repositories into a graph database, runs analysis pipelines, performs community detection, and exposes REST/streaming APIs. The frontend (Angular, in CodeGraphWeb) provides a rich UI for exploring code graphs, running AI-assisted reviews, managing wikis, viewing cluster visualizations, and administering the system. A TypeScript extraction tool (tools.ts-extractor) parses TypeScript projects and emits graph node/edge data consumed by the indexing pipeline.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API host for the CodeGraph platform — a system that indexes, analyzes, and queries code repositories as graph structures. It exposes REST endpoints for repository management, code analysis, LLM-powered assistant interactions, memory/knowledge graph operations, wiki, and administrative functions, while also consuming message bus events to orchestrate background processing pipelines (indexing, batch analysis, community detection, memory storage). It acts as the primary integration point between the frontend, background workers, and downstream services.

### CodeGraph.Data

CodeGraph.Data is the data access layer of the CodeGraph repository, defining all storage interfaces, entity models, and data transfer objects used across the system. It provides a comprehensive set of store interfaces (IGraphStore, IAnalysisStore, IReviewStore, IMetricsStore, IAssistantRunStore, IMemoryGraphStore, etc.) that abstract over multiple backend implementations including Neo4j and MariaDB/MySQL. The project serves as the contract boundary between business logic services and their persistence backends, covering code graph storage, LLM analysis results, assistant chat sessions, wiki content, job scheduling, metrics, security findings, and memory graph operations.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It implements all data store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, etc.) using Entity Framework Core with MySqlConnector, providing concrete MySQL-backed storage for code graph nodes/edges, LLM analysis results, code review runs, memory graphs, wiki content, job schedules, vector embeddings, and administrative data. The project also handles database schema migrations, AES encryption of sensitive configuration, and DI registration via extension methods.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph system. It provides concrete implementations of multiple data store interfaces, mapping between Neo4j graph nodes/relationships and domain entities for code graph data (nodes, edges, repositories, analysis batches, metrics, reviews, security findings, memory graphs, wiki content, job schedules, and vector embeddings). This project serves as the primary database adapter, translating Cypher query results into strongly-typed C# entities and writing domain objects back into the graph database.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a project responsible for parsing Ansible YAML files and extracting structured graph representations of their content. It identifies and models Ansible constructs such as playbooks, roles, tasks, handlers, variables, and cross-repository references, producing ExtractionResult objects that feed into a broader code graph system. It implements the ICodeExtractor interface, integrating with the repository's common extractor pipeline.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files and extracts structural code elements such as components, functions, SQL queries, HTTP calls, includes, and object invocations into a normalized ExtractionResult model. It implements the shared ICodeExtractor interface, making it a pluggable component in a broader code graph analysis pipeline that maps relationships and dependencies across a codebase.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic metadata from C# source files and solutions. It analyzes solution files, project dependencies, NuGet packages, and source code to produce extraction results containing information about classes, methods, routes, DI registrations, service bus events, and cross-service calls. It serves as the C#-specific extraction engine within a larger CodeGraph repository that likely builds code knowledge graphs.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a project responsible for parsing SQL files and extracting structural code graph information from them. It implements the ICodeExtractor interface to integrate with the broader CodeGraph extraction pipeline, parsing T-SQL syntax to identify and model database objects such as tables, views, stored procedures, functions, and indexes as nodes in a code graph.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a plugin/extractor project responsible for parsing Terraform (HCL) infrastructure-as-code files and transforming their contents into a graph model. It implements a common ICodeExtractor interface, extracting resources, modules, data sources, variables, outputs, locals, and cross-references from .tf and .tfvars files to populate a code graph with nodes and edges representing infrastructure relationships.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files in multiple languages (including C and C++). It implements the ICodeExtractor interface to walk syntax trees, extract symbol definitions (classes, functions, etc.) and import/dependency relationships, and produce structured graph nodes and edges consumed by the broader CodeGraph repository analysis pipeline.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data (nodes and edges) from TypeScript source files and projects. It manages a child Node.js server process that performs the actual TypeScript AST analysis, communicating with it to extract structural information and run linting, then translates the results into the repository's shared graph model types (GraphNode, PendingEdge, ExtractionResult).

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing cross-cutting concerns for host services in the CodeGraph repository. It implements internal service-to-service authentication using HMAC-signed tokens, along with DI registration extensions and OpenTelemetry activity source definitions for use across multiple host projects.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a typed HTTP client library that provides a programmatic interface for communicating with a CodeGraph indexer service. It abstracts away raw HTTP concerns (request building, authentication header injection, error parsing, JSON serialization) and exposes domain-specific async operations such as starting indexing runs, discovering repositories, syncing schemas, linking nodes, and detecting communities. It is intended to be consumed by other projects in the repository via dependency injection, registered through an IServiceCollection extension method.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is the ASP.NET Core web application host for the CodeGraph indexing service. It exposes REST API endpoints for triggering repository indexing operations (discovery, processing, re-indexing, schema sync, community detection, cross-repo linking, batch analysis) and wires together MassTransit message consumers that react to indexing lifecycle events published on a message bus. It acts as the orchestration entry point that coordinates graph storage, project analysis, and synthesis services.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It provides a dispatcher-based command pattern for running named jobs (such as repository indexing, community detection, and MCP doc regeneration), a scheduling service with cron-based triggers and timezone support, and a hosted worker that polls for due schedules. It acts as the runtime engine for all periodic and on-demand background processing in the system.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project that validates the job scheduling and dispatching infrastructure of the CodeGraph system. It contains unit tests for job scheduling (JobScheduleServiceTests), job command dispatching (JobCommandDispatcherTests), and specific job types (DiscoverRepositoriesJobTests, ProcessBatchAnalysisJobTests), along with in-memory and recording test doubles (fakes) for external service dependencies such as job stores, admin services, indexer clients, and batch analysis services.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides typed access to a remote memory graph API. It exposes operations for querying, storing, searching, and managing a graph-based memory store organized around entities and claims, with support for per-user namespacing. The project serves as the client-side integration layer for other components in the CodeGraph repository that need to interact with the memory service over HTTP.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API host that provides a graph-based memory storage and retrieval service. It exposes REST endpoints for storing, querying, searching, and managing memory claims and entities (including graph traversal, subgraph extraction, frontier expansion, and cleanup operations), and integrates with MassTransit for asynchronous message consumption of memory claim storage commands. It acts as the runtime host layer for a knowledge/memory graph system, sitting between external callers and a persistence-backed MemoryService.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository responsible for handling in-memory data migration operations. Based on its name and repository context, it likely provides utilities or abstractions for migrating graph or code analysis data structures between different memory representations or versions. The project's role is to support schema or data evolution within the CodeGraph system without requiring persistent storage migrations.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core microservice responsible for collecting and persisting telemetry/metrics events within the CodeGraph system. It subscribes to domain messages (LLM usage and MCP tool invocations) via MassTransit message broker, processes them through a metrics event recorder, and stores results in a configured database (supporting MariaDB). The project includes persistence registration with migration support, internal service authentication middleware, and standard consumer retry configuration.

### CodeGraph.Models

CodeGraph.Models is a shared data models library that defines the contracts, DTOs, message types, request/response objects, and exception types used across the CodeGraph repository. It serves as the central shared kernel for inter-service communication (message bus consumers), API requests/responses, memory graph structures, and analysis pipeline coordination.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a system that indexes, analyzes, and queries software repositories as graphs. It orchestrates code extraction and indexing pipelines, LLM-backed analysis (via OpenAI, Anthropic, and LM Studio), AI assistant interactions, repository health metrics, security scanning, code review workflows, cross-repository dependency linking, semantic search via ONNX embeddings, and a persistent memory store, all wired together through well-defined service interfaces consumed by API and job host projects in the same repository.

### CodeGraph.Tests

CodeGraph.Tests is the primary test project for the CodeGraph repository, containing unit and integration tests for all major subsystems including code extractors, data stores, API controllers, services, and host startup configurations. It provides a comprehensive suite of in-memory fakes, recording/stub implementations of interfaces, and test fixtures that stand in for real infrastructure (databases, HTTP clients, message buses) to enable isolated testing without external dependencies. The project covers everything from Roslyn-based C# code extraction and cross-repo graph linking to LLM configuration resolvers, memory graph operations, review services, security analyzers, and MariaDB store round-trip tests.

### reflect

The 'reflect' project appears to be a minimal or placeholder project within the CodeGraph repository, as no class/interface nodes were provided in the graph. Based on the project name 'reflect', it likely provides reflection or introspection utilities, but without graph nodes or source snippets, the exact purpose cannot be determined with confidence.

