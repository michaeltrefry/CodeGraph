# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-22 16:06 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code analysis and knowledge graph platform. At its core, it ingests source code repositories (C#, TypeScript, SQL, C/C++), parses them using language-specific extractors (Roslyn for C#, a Node.js/AST-based tool for TypeScript, Tree-sitter for C/C++, and a T-SQL parser for SQL), and builds structured graph representations of code entities (classes, methods, interfaces, endpoints, dependencies, NuGet packages, etc.) and their relationships. These graphs are persisted in a Neo4j graph database via CodeGraph.Data.Neo4j, which implements the store interfaces defined in CodeGraph.Data.

The system has several major subsystems working together: (1) Extraction and indexing — language-specific extractor projects produce ExtractionResult/ProjectMetadata fed into the graph store; (2) Background job processing — CodeGraph.Jobs schedules and dispatches named job types (repository discovery, re-indexing, batch analysis, community detection, MCP doc regeneration) via cron-based scheduling and a hosted worker; (3) Service layer — CodeGraph.Services provides business logic for AI-powered code analysis (via OpenAI, Anthropic, Gemini, and local LLM providers), semantic search with ONNX embeddings, project health/security analysis, cross-repository linking, a memory/knowledge graph subsystem, wiki management, AI-assisted code review, and an MCP (Model Context Protocol) server interface; (4) API layer — CodeGraph.Api is an ASP.NET Core Web API exposing REST endpoints for graph queries, cluster analysis, reviews, memory graphs, wiki content, and settings, and hosts MassTransit message consumers reacting to pipeline events; (5) Frontend — an Angular web application (CodeGraphWeb) visualizes graph data, clusters, memory/knowledge entities, wiki docs, repository reviews, and provides an AI chat interface.

Shared contracts are centralized in CodeGraph.Models (DTOs, request/response types, message contracts) and CodeGraph.Data (store interfaces and entity definitions), consumed by services, API, and test projects. Automated testing is handled by CodeGraph.Tests and CodeGraph.Jobs.Tests, which use in-memory fakes, stubs, and recording implementations to isolate behavior without real infrastructure. Two lower-confidence projects (CodeGraph.MemoryMigration and reflect) exist in the repository but lack sufficient code evidence to describe precisely — they are likely utility/migration modules. Overall, the repository is a self-contained platform for treating codebases as queryable, analyzable knowledge graphs, with LLM-assisted analysis as a first-class feature.

## Projects

### CodeGraph.Services

CodeGraph.Services is the core business logic and service layer of the CodeGraph repository — a tool that indexes, analyzes, and queries code repositories as graphs. It provides services for repository ingestion (parsing source files into graph nodes/edges), AI-powered code analysis via multiple LLM providers (OpenAI, Anthropic, Gemini, local), project health and security analysis, cross-repository linking, semantic search with ONNX embeddings, a memory/knowledge graph subsystem, wiki management, AI-assisted code review, and an MCP (Model Context Protocol) server interface for LLM tooling integration.

### CodeGraph.Data

CodeGraph.Data is the data access and persistence abstraction layer for the CodeGraph repository. It defines the store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IWikiStore, IMemoryGraphStore, IVectorStore, IJobScheduleStore, etc.) and the entity/DTO classes used throughout the system, while leaving concrete implementations (Neo4j, in-memory) in sibling projects. It acts as the contract boundary between domain services and persistence backends.

### CodeGraph.Models

CodeGraph.Models is a shared data model library that defines the DTOs, message contracts, request/response types, and domain objects used across the CodeGraph repository. It serves as the central schema layer consumed by API consumers, services, and other components — covering areas such as memory graph structures, analysis pipeline messaging, and HTTP request/response shapes.

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API that serves as the HTTP and messaging entry point for the CodeGraph system — a platform for indexing, analyzing, and querying source code repositories as a graph. It exposes REST endpoints for querying code nodes, projects, clusters, reviews, memory/knowledge graphs, wiki content, and administrative settings, while also hosting MassTransit message consumers that react to background pipeline events such as repository indexing, batch analysis, and synthesis completion.

### CodeGraph.Tests

CodeGraph.Tests is the automated test project for the CodeGraph repository. It contains unit and integration tests covering extractors, services, API controllers, data stores, memory graph operations, and analysis pipelines. The project relies heavily on in-memory fake/stub/recording implementations of core interfaces to isolate and verify behavior without requiring real databases or external services.

### CodeGraph

CodeGraph is a full-stack code analysis and knowledge graph platform. It consists of a .NET backend that indexes source repositories (C#, TypeScript, NuGet), builds graph representations of code structure, runs batch analysis jobs, and exposes REST/streaming APIs; and an Angular frontend (CodeGraphWeb) that visualizes graph data, cluster analysis, memory/knowledge entities, wiki documentation, repository reviews, and provides an AI-assisted chat interface. A TypeScript extractor tool (tools.ts-extractor) handles AST-level extraction of TypeScript projects into graph node/edge DTOs.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit/integration test project for the CodeGraph.Jobs library. It contains test fixtures, in-memory fakes, and recording stubs for service dependencies, along with test classes that verify job scheduling, command dispatching, and job execution behavior. The project exercises components such as JobCommandDispatcher, JobScheduleService, and individual job types (DiscoverRepositoriesJob, ProcessBatchAnalysisJob) in isolation without real infrastructure.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (repository discovery, re-indexing, batch analysis, community detection, MCP doc regeneration, etc.), a dispatcher that routes job execution requests by type, a scheduling service backed by cron expressions, and a hosted worker that polls for due schedules. The project functions as the asynchronous processing backbone of the larger CodeGraph system.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# static analysis project that extracts structural and semantic metadata from .NET solutions and source files. It uses Roslyn (the .NET compiler platform) to parse C# syntax trees and semantic models, identifying classes, methods, interfaces, HTTP endpoints, dependency injection registrations, service bus interactions, NuGet package references, and cross-service calls. It implements extractor service interfaces (ISolutionAnalyzer, ICodeExtractor, INuGetReferenceExtractor) to feed structured ExtractionResult and ProjectMetadata into a larger CodeGraph system.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a source code analysis project that uses the Tree-sitter parsing library to extract structural information (definitions and imports) from source files written in C, C++, and potentially other languages. It implements the ICodeExtractor interface to produce graph nodes and edges representing code entities (classes, functions, etc.) and their relationships, feeding into a broader code graph construction pipeline.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph system, providing concrete implementations of all data store interfaces defined in the CodeGraph.Data abstraction layer. It handles graph node and edge storage, code analysis results, memory graph operations, vector embeddings, wiki content, job scheduling, and repository metadata — all persisted in a Neo4j graph database via Cypher queries.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis component that parses T-SQL source files and extracts structural graph information from them. It identifies and models SQL database objects such as tables, views, stored procedures, functions, and indexes, along with their relationships (e.g., table references within procedures/views), producing an ExtractionResult that feeds into the broader CodeGraph repository analysis pipeline.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data (nodes and edges) from TypeScript source files and projects. It manages a background Node.js server process to perform TypeScript-specific analysis (including AST extraction and linting), and implements the repository's standard extractor and analyzer interfaces to integrate with the broader CodeGraph pipeline.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository that handles in-memory data migration or transformation tasks. Based on the project name, it likely provides functionality for migrating data structures or graph representations in memory, potentially serving as a utility or support module for the broader CodeGraph system. Without source code or graph nodes provided, the exact technical responsibilities cannot be fully determined.

### reflect

The 'reflect' project appears to be a reflection or introspection utility within the CodeGraph repository, likely providing mechanisms to analyze, traverse, or describe code structures programmatically. Based on the project name and its position in the repository, it likely enables runtime or compile-time inspection of types, classes, or graph nodes.

