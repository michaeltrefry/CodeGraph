# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-09 00:35 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that indexes source code repositories into a Neo4j graph database, performs AI-assisted analysis, and exposes APIs and visualizations for code search, impact analysis, security scanning, and knowledge management.

The repository is organized into a set of cooperating projects with clear layering:

**Data & Models layer**: `CodeGraph.Models` defines shared DTOs, message contracts, and entity types used across all projects. `CodeGraph.Data` declares persistence interfaces (IGraphStore, IAnalysisStore, IVectorStore, IWikiStore, IJobScheduleStore, etc.). `CodeGraph.Data.Neo4j` implements those interfaces with Cypher queries against a Neo4j graph database, handling code nodes/edges, AI batch results, vector embeddings, wiki pages, job schedules, and health/security metrics.

**Extraction layer**: Multiple language-specific extractors implement a shared ICodeExtractor interface. `CodeGraph.Extractors.CSharp` uses Roslyn to analyze C# solutions, extracting classes, methods, DI registrations, HTTP routes, service bus usage, and NuGet references. `CodeGraph.Extractors.TypeScript` spawns an external Node.js server process for TypeScript AST extraction and linting. `CodeGraph.Extractors.TreeSitter` provides language-agnostic parsing via the Tree-sitter library. `CodeGraph.Extractors.Sql` parses T-SQL files to extract tables, views, stored procedures, and their relationships.

**Services layer**: `CodeGraph.Services` is the core business logic orchestrator. It manages the full pipeline from repository discovery (GitHub, GitLab, local folders) and file extraction through graph construction, cross-repository linking, community/cluster detection, security scanning, health metrics, semantic/vector search, batch LLM analysis (supporting Anthropic, OpenAI, Gemini, and local models), wiki management, and an AI chat/MCP tool interface.

**API layer**: `CodeGraph.Api` is an ASP.NET Core Web API exposing REST endpoints for graph queries, repository indexing, wiki management, memory store operations, and an AI assistant. It also hosts MassTransit message consumers that react to asynchronous pipeline events (indexing completion, batch analysis submission, synthesis completion). The frontend is an Angular application for graph visualization.

**Jobs layer**: `CodeGraph.Jobs` is a separate ASP.NET Core application providing HTTP endpoints and cron-scheduled background jobs for repository discovery, batch analysis processing, re-indexing, community detection, MCP doc regeneration, and graph relationship linking. It uses a dispatcher pattern to route job types to implementations.

**Test projects**: `CodeGraph.Tests` and `CodeGraph.Jobs.Tests` provide unit and integration tests using xUnit, with rich in-memory fakes and recording doubles for databases, HTTP clients, message buses, and LLM providers.

**Key external dependencies** include Neo4j (graph database), MassTransit (message bus), Roslyn (C# compilation/analysis), Tree-sitter (multi-language parsing), Node.js (TypeScript extraction subprocess), and multiple LLM provider SDKs (Anthropic, OpenAI, Gemini). The system has no observed external consumers — it appears to be a self-contained platform.

## Projects

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit/integration test project for the CodeGraph.Jobs component. It provides test doubles (in-memory stores, recording fakes), test-specific job subclasses, and xUnit test classes that verify the behavior of job scheduling, command dispatching, and the various background jobs (DiscoverRepositories, ProcessRepositories, ProcessBatchResults, ProcessBatchAnalysis). The project exercises job lifecycle concerns such as argument normalization, lease acquisition, and downstream service invocation.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository. It provides unit and integration tests covering extractors (Roslyn, SQL, TreeSitter, NuGet), graph store operations, indexing pipelines, analysis services (batch analysis, security, prompt building), repository providers (GitHub, GitLab, folder-based), wiki services, memory services, and startup/DI registration. The project supplies a rich set of in-memory fakes, stubs, recording doubles, and test helpers that stand in for external dependencies such as databases, HTTP clients, message buses, and analysis model providers.

### CodeGraph

CodeGraph is a full-stack repository analysis and visualization platform. It indexes source code repositories (C#/.NET and TypeScript), builds a graph of nodes and relationships, and exposes APIs for code search, impact analysis, cluster detection, security/health analysis, wiki management, and an AI chat assistant. The frontend is an Angular application that visualizes the code graph, while the backend includes indexing pipelines, job scheduling, and TypeScript/NuGet extraction tools.

### CodeGraph.Data

CodeGraph.Data defines the data access layer contracts and entity models for the CodeGraph repository analysis platform. It provides a comprehensive set of interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IVectorStore, IWikiStore, etc.) that abstract persistence operations for code graph nodes/edges, AI analysis results, metrics, wiki content, job scheduling, and memory graphs. This project serves as the abstraction boundary between the domain/service layer and concrete storage implementations (primarily Neo4j, as seen from the external Neo4j implementations).

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API that serves as the backend for a code analysis and knowledge graph platform. It exposes REST endpoints for querying and managing a graph of code nodes (projects, files, methods, etc.), running repository indexing and analysis pipelines, managing a wiki and memory store, and interacting with an AI assistant. It also hosts a set of MassTransit message consumers that react to asynchronous pipeline events such as repository indexing completion, batch analysis submission, and analysis synthesis completion.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic information from C# source files and solutions. It builds a code graph by analyzing classes, methods, interfaces, namespaces, HTTP routes, dependency injection registrations, service bus interactions, NuGet references, and cross-service calls. It serves as the C#-specific extraction backend within the broader CodeGraph repository, implementing extraction service interfaces defined in a shared services layer.

### CodeGraph.Jobs

CodeGraph.Jobs is an ASP.NET Core web application that provides HTTP endpoints and background scheduling for repository analysis jobs. It orchestrates long-running tasks such as discovering repositories, processing batch analysis results, re-indexing, detecting communities, regenerating MCP docs, and linking graph relationships, using a cron-based schedule service and a dispatcher pattern to route job types to their implementations.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a source code analysis project that uses the Tree-sitter parsing library to extract structural information (definitions, imports, relationships) from source files in multiple programming languages. It implements the ICodeExtractor interface to produce graph nodes and edges representing code entities, serving as a language-agnostic extraction backend within the CodeGraph repository's pipeline.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j persistence layer for the CodeGraph repository. It implements multiple data store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IVectorStore, IMemoryGraphStore, IWikiStore, IJobScheduleStore, etc.) by translating application-level operations into Cypher queries executed against a Neo4j graph database. The project handles everything from storing and querying code graph nodes/edges and cross-repository relationships, to managing AI analysis batches, vector embeddings, memory entities, wiki pages, job schedules, and health/security metrics.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis component that parses T-SQL source files and extracts structural graph information from them. It identifies and records database objects such as tables, views, stored procedures, functions, and indexes, along with their relationships (e.g., table references within procedures), producing an ExtractionResult for use in a broader code graph system. It implements the ICodeExtractor interface, fitting into a plugin-style extractor architecture shared across multiple languages or file types in the repository.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It provides TypeScript-specific implementations for parsing individual files, analyzing full TypeScript projects via tsconfig, and managing a long-running Node.js server process that performs the actual AST extraction and linting work. This project bridges the C# CodeGraph pipeline with TypeScript tooling by spawning and communicating with an external TypeScript server process.

### CodeGraph.Models

CodeGraph.Models is a shared data model library that defines the contracts, DTOs, and message types used across the CodeGraph system. It contains message classes for inter-service communication (consumed by API consumers), request/response models for API endpoints, memory graph entities for knowledge graph operations, and exception types for error handling. This project serves as the central contracts package that other projects in the repository depend on for type-safe communication.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph system — a multi-repository code intelligence platform that indexes source code into a graph database, analyzes it using LLMs, and exposes query, assistant, and MCP tool interfaces. It orchestrates the full lifecycle from repository discovery and file extraction through graph construction, cross-repo linking, community detection, security scanning, health metrics, semantic search, batch AI analysis, and a wiki knowledge base, while abstracting over multiple LLM providers (Anthropic, OpenAI, Gemini, local) and repository sources (GitHub, GitLab, local folders).

