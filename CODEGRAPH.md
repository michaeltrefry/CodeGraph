# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-03 01:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that indexes source code repositories into a graph database and provides analysis, querying, and AI-assisted tooling over the resulting graph. The system is composed of roughly twenty projects organized into distinct layers: (1) Extraction — language-specific extractors (C#/Roslyn, TypeScript/Tree-sitter/Node.js, SQL/T-SQL, Terraform/HCL, Ansible/YAML, ColdFusion, and a generic Tree-sitter backend) parse source files and emit structured graph nodes and edges; (2) Indexing — CodeGraph.Indexer.Host is an ASP.NET Core service that orchestrates the indexing pipeline, receiving extraction results, running linking and community-detection passes, and persisting the graph; (3) Data access — CodeGraph.Data defines store interfaces and DTOs; CodeGraph.Data.Neo4j and CodeGraph.Data.MariaDb provide concrete implementations backed by Neo4j (graph queries) and MariaDB/MySQL (relational/metrics/vector data) respectively; (4) Core services — CodeGraph.Services implements business logic for graph analysis, LLM-backed code review, a conversational graph assistant (MCP server), semantic search via ONNX embeddings, wiki management, memory/knowledge-graph operations, and admin reporting; (5) API surface — CodeGraph.Api is an ASP.NET Core Web API that exposes REST endpoints for repository management, AI-assisted querying, code review, memory, wiki, and administration, and hosts MassTransit message consumers that react to pipeline events; (6) Supporting microservices — CodeGraph.Memory.Host manages a graph-based knowledge/claim store; CodeGraph.Metrics collects LLM-usage and tool-invocation events; CodeGraph.Jobs schedules and dispatches background tasks (re-indexing, discovery, community detection, batch analysis) via cron; (7) Shared libraries — CodeGraph.Models holds message and DTO contracts used for inter-service communication; CodeGraph.Host.Shared provides HMAC-based inter-service auth and OpenTelemetry definitions; CodeGraph.Indexer.Client and CodeGraph.Memory.Client are typed HTTP clients for consuming the indexer and memory services; (8) Frontend — an Angular application (CodeGraphWeb) provides a rich UI for browsing the code graph, managing repositories, running AI reviews, exploring cluster/memory graphs, configuring LLM providers, and editing a wiki. Inter-service communication uses both MassTransit message bus events and direct HTTP calls through typed clients. The platform depends externally on Neo4j, MariaDB/MySQL, OpenAI/Anthropic/LM Studio LLM APIs, ONNX runtime for embeddings, a Node.js process for TypeScript AST analysis, and MassTransit-compatible message infrastructure. Externally, a 'CodeFlow' system (and a 'codeflow-team' client) makes HTTP calls into the CodeGraph API, indicating CodeGraph serves as a backend intelligence service for at least one downstream consumer.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code intelligence platform consisting of a backend (.NET) and a frontend (Angular). The backend indexes source code repositories into a graph database, performs analysis (community detection, security, health), supports LLM-based assistants and reviews, and provides migration tooling. The Angular frontend (CodeGraphWeb) exposes a rich UI for browsing the code graph, managing repositories, running AI-assisted reviews, exploring memory/cluster graphs, administering LLM providers and prompts, and editing a wiki. A TypeScript extraction tool (tools.ts-extractor) provides AST-level analysis of TypeScript projects to feed the indexing pipeline.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core web API layer of the CodeGraph system — a platform that indexes, analyzes, and queries code repositories and database schemas using graph-based representations and LLM-powered tooling. It exposes REST endpoints for repository management, code analysis, AI-assisted querying, code review, memory (knowledge graph), wiki, and administrative configuration, and also hosts MassTransit message consumers that react to indexing pipeline events. It bridges the HTTP surface with internal services, a message bus, and optionally a remote indexer service.

### CodeGraph.Data

CodeGraph.Data is the data access and persistence abstraction layer for the CodeGraph repository. It defines the full set of store interfaces (IGraphStore, IAnalysisStore, IReviewStore, IMetricsStore, IMemoryGraphStore, IAssistantRunStore, etc.) and their corresponding entity/DTO classes that represent every persisted domain object in the system. Concrete implementations of these interfaces exist in sibling projects (CodeGraph.Data.MariaDb, CodeGraph.Data.Neo4j), making this project the canonical contract between application services and any backing database technology.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph storage, analysis, metrics, reviews, memory graph, assistant runs, wiki, admin, vector store, etc.) using Entity Framework Core (via CodeGraphDbContext) and raw MySqlConnector connections for bulk/performance-sensitive operations. It also handles database migrations, AES encryption of sensitive configuration values, and dependency injection wiring for all MariaDB-backed services.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph, vector, memory, wiki, job scheduling, metrics, analysis, review, etc.) by translating between Neo4j graph primitives (nodes, relationships, records) and the application's domain entities and models. The project serves as the sole Neo4j adapter in the repository, encapsulating all Cypher query execution, result mapping, and schema migration logic.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a static analysis component that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and extracts a graph representation of their structure and relationships. It identifies Ansible-specific constructs such as modules, tasks, roles, hosts, and cross-repository references, producing ExtractionResult objects containing GraphNodes and edges for use in a broader code graph analysis system. It implements the ICodeExtractor interface, integrating with the repository's generic extraction pipeline.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a code analysis project that parses ColdFusion (.cfm/.cfc) source files and extracts structural metadata such as components, functions, include directives, SQL queries, HTTP calls, object creation, and component invocations. It implements the ICodeExtractor interface and produces ExtractionResult objects consumed by the broader CodeGraph repository for building a code knowledge graph. The project uses regex-based parsing to identify ColdFusion-specific constructs in both tag-based and CFScript syntax.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic information from C# source files and solutions. It walks syntax trees to identify classes, methods, interfaces, DI registrations, HTTP routes, service bus events, NuGet dependencies, and cross-service calls, producing structured ExtractionResult models for downstream graph-building or analysis tools. It serves as the C#-specific extraction engine within a larger CodeGraph repository that likely supports code intelligence, dependency mapping, or architecture visualization.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information (tables, views, procedures, functions, indexes, and their relationships) from T-SQL syntax. It implements the ICodeExtractor interface and produces ExtractionResult objects representing the code graph of SQL database objects, supporting both CREATE and ALTER statements. It handles MySQL-to-T-SQL syntax normalization as a preprocessing step before parsing with the Microsoft TSqlParser.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis component that parses Terraform/HCL (HashiCorp Configuration Language) files and extracts their structural elements — resources, modules, data sources, variables, outputs, locals, and inter-resource references — into a graph model. It implements the ICodeExtractor interface, integrating with the broader CodeGraph repository's extraction pipeline to represent Terraform infrastructure-as-code as a queryable graph of nodes and edges.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction library that uses the Tree-sitter parsing framework to analyze source files of various programming languages. It walks syntax trees to extract definitions (classes, functions, etc.) and import relationships, producing structured graph nodes and edges consumed by the broader CodeGraph system. The project acts as a language-agnostic extraction backend, with language-specific behavior driven by pluggable LanguageSpec configurations registered per file extension.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a long-running Node.js analysis server process, communicates with it to extract structural information (nodes, edges) and lint results from TypeScript codebases, and converts the results into the shared CodeGraph model types used by the broader repository.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing common hosting utilities and internal service authentication mechanisms for the CodeGraph repository. It implements a lightweight HMAC-based token system for authenticating inter-service communication, along with DI registration extensions and OpenTelemetry activity source definitions used across multiple host services.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is an HTTP client library that provides a typed .NET client for communicating with a CodeGraph indexer service. It exposes operations for triggering and monitoring indexer runs (batch analysis, linking, community detection, schema sync, discovery, re-indexing, and repository processing) over HTTP, and integrates with ASP.NET Core's dependency injection system via service collection extensions.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web application that serves as the host process for a code graph indexing pipeline. It exposes HTTP endpoints for triggering indexing operations (repository processing, schema synchronization, linking, community detection, batch analysis) and wires up a set of MassTransit message consumers that react to pipeline events such as repository indexing completion, analysis batch submission, and repository removal. The project orchestrates the full lifecycle of building and maintaining a graph representation of code repositories.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (repository discovery, re-indexing, community detection, MCP doc regeneration, batch analysis processing, etc.), a dispatcher that routes job execution requests by type, a scheduling service that manages cron-based schedules persisted to a database, and a hosted worker that periodically triggers due schedules. The project acts as the task orchestration layer for the larger CodeGraph system.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the CodeGraph.Jobs subsystem. It tests job scheduling, command dispatching, and job execution logic by providing in-memory fakes and recording test doubles for external dependencies (store, admin service, indexer client, batch analysis service, etc.), then asserting behavior of the real job scheduler and dispatcher classes.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides typed access to a remote memory graph service. It defines a contract (IMemoryClient) and a concrete implementation (HttpMemoryClient) for querying, writing, searching, and managing a graph-based memory store organized around entities, claims, and relationships, all scoped per user. The project also includes DI registration helpers so consuming applications can easily wire up the client via IServiceCollection.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API host that exposes a graph-based memory system for storing, querying, and managing knowledge claims. It provides REST endpoints for CRUD operations on memory entities and claims (including graph traversal, search, subgraph extraction, frontier expansion, and cleanup), consumes messages from a MassTransit message bus to asynchronously store memory claims, and handles persistence initialization and service registration at startup.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository responsible for handling in-memory data migration operations. Based on the project name and its position in the repository, it likely provides utilities or services for migrating data structures, schemas, or state in memory rather than through persistent storage mechanisms.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core microservice responsible for collecting and persisting metrics events within the CodeGraph repository. It subscribes to domain events (such as LLM usage and MCP tool invocations) via MassTransit message consumers, records those events through a metrics persistence layer, and exposes internal service authentication middleware. It handles its own database setup including migrations and supports MariaDB as a storage provider.

### CodeGraph.Models

CodeGraph.Models is a shared data contracts library that defines the message types, request/response DTOs, domain entities, and exception types used across the CodeGraph system. It serves as the common models layer consumed by multiple other projects (Indexer, API, Memory, Metrics hosts) for inter-service messaging via a message bus, memory graph operations, and HTTP API interactions. The library has no business logic of its own — it provides the type contracts that allow different services to communicate with a shared schema.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph system — a platform for indexing, analyzing, and querying software repositories as knowledge graphs. It provides services for code extraction and graph indexing pipelines, LLM-backed analysis (via OpenAI, Anthropic, and LM Studio providers), AI-assisted code review, a conversational graph assistant (MCP server), memory/knowledge-graph operations, semantic search via ONNX embeddings, wiki management, administrative reporting, and cross-repository dependency linking.

### CodeGraph.Tests

CodeGraph.Tests is the automated test project for the CodeGraph repository, containing unit and integration tests that verify the behavior of services, controllers, data stores, extractors, memory subsystems, and infrastructure components. It heavily uses hand-written test doubles (fakes, recording implementations, stubs, and no-op implementations) of core interfaces rather than a mocking framework, and provides a comprehensive InMemoryGraphStore that implements the full IGraphStore contract in memory to support fast, hermetic service-level tests. The project spans controller tests, service logic tests, MariaDB integration tests, extractor tests (Roslyn, SQL, Terraform, Ansible, ColdFusion, etc.), and startup/DI registration validation tests.

### reflect

The 'reflect' project appears to be a reflection or introspection utility within the CodeGraph repository. Based on the project name and its role in the repository structure, it likely provides mechanisms for examining and describing code structures, types, and relationships at runtime or analysis time. The project is minimal with no nodes listed in the graph, suggesting it may be a foundational or utility module.

