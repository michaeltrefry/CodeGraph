# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-18 15:33 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code analysis and knowledge graph platform composed of roughly 25 projects spanning backend services, data access layers, language-specific extractors, background jobs, and an Angular frontend. At its core, the system ingests source repositories (C#, TypeScript, Rust, SQL, Terraform, Ansible, ColdFusion, C/C++, and more), parses them via pluggable ICodeExtractor implementations (Roslyn, Tree-sitter, SCIP/rust-analyzer, a Node.js TypeScript server, and custom parsers), and stores the resulting code graph — nodes, edges, symbols, and relationships — in either Neo4j (graph database) or MariaDB/MySQL (relational), with both backends abstracted behind store interfaces defined in CodeGraph.Data. The CodeGraph.Services project provides the central business logic tier: repository indexing and extraction orchestration, LLM provider routing (OpenAI, Anthropic, LM Studio), semantic search via ONNX embeddings, AI-assisted code review, wiki management, memory graph operations, and MCP (Model Context Protocol) tool exposure. CodeGraph.Api is the ASP.NET Core Web API surface that wires these services to REST endpoints, authentication/authorization middleware, and MassTransit message bus consumers. CodeGraph.Indexer.Host is a separate ASP.NET Core host that runs the indexing pipeline, reacting to events (batch analysis, community detection, synthesis, discovery) via MassTransit, and manages a TypeScript sidecar process. CodeGraph.Jobs provides cron-based background job scheduling (repository indexing, community detection, embedding ingestion) with a command-dispatcher pattern. CodeGraph.Memory.Host is a dedicated REST service for graph-based memory storage (entities, claims, relationships) consumed by other services via CodeGraph.Memory.Client. CodeGraph.Mcp.Hub is a standalone MCP gateway that proxies and exposes multiple backend data sources (MySQL, RabbitMQ, Shortcut) as MCP tools with access control and audit logging. CodeGraph.Metrics is a background consumer that records LLM usage and MCP invocation telemetry. Cross-cutting concerns (HMAC-based internal auth, OpenTelemetry) are shared via CodeGraph.Host.Shared, and CodeGraph.Models centralizes all DTOs, message types, and contracts. Externally, the repository makes HTTP calls to its own API layer (TC.CodeGraphApi) and is called by at least two external systems identified as 'CodeFlow' and 'codeflow-team', indicating it serves as a backend code intelligence service consumed by downstream workflow or project management tooling.

## Languages

- **C#**: 81 % of non-blank LOC (81,172 LOC, 609 files)
- **TypeScript**: 17 % of non-blank LOC (17,562 LOC, 84 files)

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code analysis and knowledge graph platform. The backend (C#) indexes source repositories (C#, TypeScript, Rust), stores graph data in Neo4j/MariaDB, runs scheduled jobs for community detection, embedding ingestion, and batch analysis, and exposes APIs for graph queries, LLM-powered reviews, wiki management, MCP (Model Context Protocol) integration, and an AI assistant. The frontend (Angular) provides a rich web UI for exploring the code graph, managing repositories, viewing impact analysis, interacting with the AI assistant, administering LLM providers/prompts, and browsing wiki pages. Supporting tools include a TypeScript AST extractor and an SQL parser used for validation.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API layer of the CodeGraph system, responsible for exposing REST endpoints that serve a code analysis, graph exploration, AI assistant, wiki, MCP (Model Context Protocol) hub, and repository indexing platform. It wires together authentication/authorization, message bus consumers (via MassTransit), middleware for MCP telemetry and entitlement enforcement, and delegates to domain services for graph operations, memory, indexing, LLM configuration, and reviews. The project acts as the primary integration surface between external clients and all internal CodeGraph services.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all persistence interfaces and entity types used across the system. It provides a comprehensive set of store interfaces (IGraphStore, IAnalysisStore, IReviewStore, IMetricsStore, IAssistantRunStore, IMemoryGraphStore, etc.) that abstract over multiple storage backends (Neo4j, MariaDB/MySQL) and entity classes that represent the database rows or graph nodes for code analysis, LLM integration, assistant chat, MCP hub tooling, wiki, and job scheduling features.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph platform. It implements all data store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IMemoryGraphStore, and many others) using Entity Framework Core (CodeGraphDbContext) and raw MySqlConnector queries. The project provides concrete MySQL-backed implementations for storing and querying code graph nodes/edges, LLM analysis results, AI assistant runs, wiki pages, job schedules, MCP hub configurations, memory graphs, vector embeddings, and repository health/security metrics.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces covering code graph nodes and edges, analysis batches, memory graph (entities and claims), wiki pages, job scheduling, vector embeddings, and migration export — all backed by Cypher queries executed against a Neo4j database. It serves as the primary database adapter project, translating between domain model types and Neo4j INode/IRecord representations.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a code analysis plugin that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and extracts a graph representation of their structure and relationships. It implements the ICodeExtractor interface to integrate with the broader CodeGraph repository analysis pipeline, producing GraphNode objects and edges that capture how Ansible artifacts reference and depend on each other.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files and extracts structural and relational metadata — including components, functions, SQL queries, HTTP calls, include directives, and object invocations — into a normalized ExtractionResult model. It implements the shared ICodeExtractor interface, positioning it as one of several language extractors in a broader code-graph analysis platform that maps relationships between code artifacts.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural metadata from C# solutions and source files. It analyzes solution files, projects, and individual source code to produce extraction results containing code graph information including classes, methods, routes, DI registrations, service bus events, NuGet dependencies, and cross-service calls. It acts as a concrete implementation of extractor service interfaces defined elsewhere in the repository.

### CodeGraph.Extractors.Rust

CodeGraph.Extractors.Rust is a project responsible for extracting code graph data from Rust projects. It orchestrates the generation and parsing of SCIP (Stack graphs Index Protocol) JSON files produced by rust-analyzer or similar tooling, then transforms that data into CodeGraph's internal extraction model (nodes, edges, symbols) for use in code analysis and navigation.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a project responsible for parsing SQL source files and extracting structural graph information from T-SQL code. It processes SQL file content, normalizes syntax (including MySQL compatibility), and builds a code graph by visiting SQL AST nodes to identify tables, views, stored procedures, functions, indexes, and their relationships. It implements the repository's common ICodeExtractor interface, making it a pluggable SQL-specific analysis component within the broader CodeGraph system.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a plugin/extractor project that parses HashiCorp Configuration Language (HCL) files used by Terraform, extracting infrastructure-as-code definitions (resources, modules, variables, outputs, data sources, locals, and tfvars) into a graph model. It implements the ICodeExtractor interface, making it a pluggable component in the larger CodeGraph repository that builds code/infrastructure dependency graphs. The extracted nodes and edges represent Terraform constructs and their relationships (e.g., module references, resource dependencies, cross-repo edges).

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files across multiple programming languages (including C and C++). It implements the ICodeExtractor interface to parse source files into syntax trees, then walks those trees to extract code definitions (classes, functions, etc.) and import/dependency relationships, producing structured graph nodes and edges for the CodeGraph repository analysis system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based TypeScript analysis server process, communicates with it to extract structural information (nodes and edges) from TypeScript codebases, and also provides linting capabilities. It acts as a bridge between the broader CodeGraph system's extraction pipeline and a dedicated TypeScript language server.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library that provides cross-cutting concerns for host services in the CodeGraph repository. Its primary focus is internal service-to-service authentication using HMAC-signed tokens, along with DI registration helpers and OpenTelemetry activity source definitions for shared hosting infrastructure.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP client for interacting with the CodeGraph indexer service. It exposes operations for triggering and monitoring indexing pipeline stages (batch analysis, linking, community detection, schema sync, discovery, re-indexing) and querying run status, with DI registration support via service collection extensions.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is the ASP.NET Core web host and entry point for the CodeGraph indexing service. It exposes REST API endpoints for triggering repository indexing operations, wires up MassTransit message consumers that react to indexing pipeline events (batch analysis, synthesis, repository processing, community detection, etc.), and manages background services such as a TypeScript sidecar process. It acts as the orchestration layer that bridges HTTP-triggered operations and the internal event-driven indexing pipeline.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution subsystem. It defines a command-dispatcher pattern for executing named job types (repository indexing, community detection, embedding ingestion, etc.), a cron-based schedule management service, and a hosted worker that periodically checks for due schedules and triggers them. It also provides DI registration extensions and startup configuration for persistence.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the job scheduling and dispatching subsystem of the CodeGraph repository. It provides in-memory fakes and recording test doubles for external dependencies (store, admin service, indexer client, batch analysis service, etc.) and contains test classes that verify the behavior of job scheduling, command dispatching, and specific job implementations like DiscoverRepositoriesJob and ProcessBatchAnalysisJob.

### CodeGraph.Mcp.Hub

CodeGraph.Mcp.Hub is a Model Context Protocol (MCP) hub server that acts as a centralized gateway exposing multiple backend data sources and services as MCP tools. It integrates with MySQL databases, RabbitMQ message brokers, and Shortcut (project management) APIs, providing tool discovery, access control via token entitlements, audit logging, credential management, and sensitive data column policies. The project also supports a 'shim' pattern to proxy and forward calls to external downstream MCP servers.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library for interacting with a memory graph service. It provides a typed .NET client (HttpMemoryClient) that wraps REST API calls to read, query, write, and manage a knowledge graph of entities, claims, and relationships, scoped per-user. The project also includes DI registration extensions, configuration options, and a custom exception type, making it easy to consume the memory service from other .NET applications in the repository.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API service that provides a graph-based memory storage and retrieval system. It exposes REST endpoints for storing, querying, searching, and managing memory claims and entities (nodes in a knowledge graph), and integrates MassTransit for asynchronous message consumption of claim storage requests. The project handles persistence configuration, authentication middleware, and health probing for the write pipeline.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository focused on in-memory data migration functionality. Based on the project name, it likely provides utilities for migrating or transforming data structures in memory, potentially serving as a migration layer for the CodeGraph system's internal data representations. The project's role is infrastructure-level, supporting data transformation or schema evolution without persistent storage operations.

### CodeGraph.Metrics

CodeGraph.Metrics is a background service responsible for collecting and persisting metrics events within the CodeGraph repository. It consumes messages from a message bus (MassTransit) — specifically LLM usage events and MCP tool invocation events — and records them via a metrics event recorder, likely to a database backed by MariaDB or a compatible SQL provider.

### CodeGraph.Models

CodeGraph.Models is a shared data models library that defines the contracts, DTOs, message types, and exceptions used across the CodeGraph repository. It serves as the central schema layer, providing message classes consumed by indexer, API, memory, and metrics services, along with request/response objects, memory graph structures, and utility types for graph node identification.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform, a code intelligence system that indexes source repositories into a graph database, analyzes them with LLMs, and exposes the results via query services, an AI assistant, code review pipelines, and MCP (Model Context Protocol) tools. It provides service implementations for repository indexing and extraction, LLM provider configuration and routing (OpenAI, Anthropic, LM Studio), semantic search via ONNX embeddings, memory graph operations, wiki management, vitals/security analysis, and cross-repository dependency linking. This project acts as the central service tier consumed by API controllers, background job runners, and messaging consumers throughout the repository.

### CodeGraph.Tests

CodeGraph.Tests is the primary test project for the CodeGraph repository, providing unit and integration tests across all major subsystems including extractors, data stores, services, API controllers, memory systems, and host startup configurations. It contains a large collection of in-memory fakes, recording stubs, and no-op implementations of nearly every service interface in the system, enabling isolated unit testing without real database or external service dependencies. The project covers extractors for multiple languages (C#, Rust, Python, SQL, Terraform, Ansible, ColdFusion), data store round-trip tests against MariaDB and Neo4j, controller behavior tests, LLM configuration resolver tests, MCP hub and memory service tests, and indexing pipeline tests.

### reflect

The 'reflect' project appears to be a minimal or empty project within the CodeGraph repository, as no class/interface nodes were provided in the graph. Based on the project name 'reflect', it likely serves as a reflection or introspection utility module, potentially providing runtime type analysis or metadata inspection capabilities for the broader repository.

