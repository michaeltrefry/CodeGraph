# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-28 13:32 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack, multi-service code intelligence and knowledge graph platform. It ingests source repositories across multiple languages (C#, TypeScript, SQL, Terraform, Ansible, ColdFusion, C/C++) and infrastructure-as-code formats, parses them using language-specific extractor plugins, and stores the resulting graph of nodes and edges in either a Neo4j or MariaDB/MySQL backend. The extracted graph captures code structure (classes, methods, interfaces, dependencies, routes, events, NuGet packages, SQL objects, Terraform resources, Ansible roles, etc.) and cross-repository relationships.

The system is composed of several cooperating services and libraries: CodeGraph.Indexer.Host orchestrates the indexing pipeline (repository discovery, per-language extraction, schema sync, community detection, cross-repo linking, batch analysis) and manages a TypeScript sidecar process; CodeGraph.Api exposes the primary REST and streaming HTTP surface (including an MCP protocol endpoint) for graph queries, LLM-powered code review and assistant interactions, wiki, memory graph, and admin operations; CodeGraph.Memory.Host provides a dedicated graph-based memory/knowledge store for entities and claims; CodeGraph.Metrics collects and persists usage telemetry (LLM calls, MCP tool invocations); and CodeGraph.Jobs runs cron-scheduled background jobs that drive periodic indexing, analysis, and maintenance tasks. All services communicate internally via MassTransit message consumers and typed HTTP clients (CodeGraph.Indexer.Client, CodeGraph.Memory.Client). Internal service-to-service authentication uses HMAC-signed tokens defined in CodeGraph.Host.Shared.

Shared abstractions are layered cleanly: CodeGraph.Models defines all message and DTO types; CodeGraph.Data defines store interfaces; CodeGraph.Data.MariaDb and CodeGraph.Data.Neo4j provide interchangeable persistence implementations; CodeGraph.Services contains the core domain/business logic (LLM provider integration via OpenAI/Anthropic/LM Studio, ONNX semantic embeddings, security scanning, community detection, wiki, assistant with tool use). The Angular frontend provides UI for browsing the graph, running AI reviews, managing wiki and memory graphs, and administering the system. A standalone TypeScript extractor tool performs AST-level analysis of TS/JS projects and emits graph DTOs consumed by the indexing pipeline.

Externally, CodeGraph is called by at least three other repositories: KanbanBoard (6 observed HTTP call edges), CodeFlow (3 edges), and TC.CodeGraphApi (2 edges). CodeGraph itself calls out to TC.CodeGraphApi (2 edges), indicating a bidirectional or federated relationship with that service. The overall architecture is a distributed, polyglot, graph-centric code analysis platform with pluggable storage backends, pluggable language extractors, LLM-augmented analysis, and a multi-service deployment model.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **KanbanBoard**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=DELETE, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/memory/writes/{param}, route_template=api/memory/writes/{receiptId}, confidence_band=medium
- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code analysis and knowledge graph platform. The backend (.NET) indexes source repositories (C#, TypeScript, NuGet) into a graph database, runs LLM-powered analysis/review jobs, supports migration between graph stores, and exposes REST/streaming APIs. The frontend (Angular) provides a rich UI for browsing the code graph, running AI-assisted reviews, managing wiki pages, inspecting clusters and memory graphs, and administering the system. A TypeScript extractor tool parses TS/JS projects and emits graph node/edge DTOs consumed by the indexing pipeline.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API layer of the CodeGraph system, exposing HTTP endpoints for repository indexing, code graph analysis, LLM-powered code review and assistant features, memory/knowledge graph management, wiki, and administrative operations. It bridges external HTTP clients (including an MCP protocol surface) with internal services by hosting controllers, message consumers (via MassTransit), authentication handlers, and startup/configuration wiring. The project also contains remote service adapters that forward indexer and memory operations to other service boundaries within the repository.

### CodeGraph.Data

CodeGraph.Data is the data access layer of the CodeGraph repository, defining the contracts (interfaces) and entity models for all persistence operations across the system. It provides a comprehensive set of store interfaces covering graph data (nodes, edges, cross-repo relationships), AI/LLM analysis, assistant runs, wiki content, memory graphs, metrics, reviews, job scheduling, and administrative functions, with implementations expected in separate projects (MariaDb, Neo4j). The project acts as the shared abstraction boundary between the application's business logic and its underlying storage backends.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It implements all data store interfaces defined in the CodeGraph.Data abstraction layer, providing concrete EF Core and raw SQL implementations backed by a MariaDB/MySQL database. The project handles graph storage (nodes, edges, cross-repo relationships), LLM analysis batches, code metrics, security findings, reviews, wiki content, assistant runs, memory graph operations, vector embeddings, job scheduling, and database migrations.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph nodes/edges, analysis batches, memory graph, vector embeddings, wiki, job scheduling, and more) using the Neo4j graph database via Cypher queries. This project acts as the sole infrastructure adapter between the rest of the application and a Neo4j instance, handling node/edge CRUD, full-text and vector search, migrations, and data export/migration utilities.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a specialized code analysis component that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and extracts a graph representation of their structure and relationships. It identifies nodes such as plays, tasks, roles, handlers, and variables, and detects cross-repository edges and module references, feeding the results into the broader CodeGraph analysis pipeline via the ICodeExtractor interface.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files and extracts structural code graph metadata such as components, functions, SQL queries, HTTP calls, include directives, and object invocations. It implements the shared ICodeExtractor interface and produces ExtractionResult objects consumed by the broader CodeGraph pipeline for static analysis or dependency mapping.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to parse and extract structural metadata from C# source files and Visual Studio solutions. It identifies classes, methods, interfaces, namespaces, routing attributes, dependency injection registrations, HTTP client calls, service bus events, NuGet dependencies, and other code-level constructs, producing structured ExtractionResult models consumed by the broader CodeGraph system. It acts as the C#-specific extraction engine within a multi-language code graph / architecture analysis platform.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL script files and extracts structural graph information from them. It identifies and models SQL database objects (tables, views, stored procedures, functions, indexes) and their relationships, producing an ExtractionResult that feeds into the broader CodeGraph repository's code analysis pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis component that parses Terraform/HCL configuration files and extracts structured graph data from them. It implements the ICodeExtractor interface, processing .tf and .tfvars files to identify resources, modules, data sources, variables, outputs, locals, and cross-references between them, producing a graph model of infrastructure-as-code relationships. This project plays the role of a language-specific extractor plugin within the larger CodeGraph repository, which appears to build code/infrastructure knowledge graphs.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files for multiple languages (at minimum C and C++). It implements the ICodeExtractor interface, walking parsed syntax trees to extract symbol definitions (classes, functions, etc.) and import/dependency relationships, then producing structured graph nodes and edges for the CodeGraph knowledge graph system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based server process that performs the actual TypeScript AST analysis, communicates with it to extract structural information (nodes and edges) and lint results, and translates those results into the CodeGraph domain model (GraphNode, PendingEdge, ExtractionResult). It acts as a bridge between the broader CodeGraph extraction pipeline and a TypeScript-specific analysis server.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library for the CodeGraph repository that provides cross-cutting concerns for host services, specifically focused on internal service-to-service authentication using HMAC-signed tokens. It defines the contracts, implementations, and DI registration extensions needed for services to issue and validate internal JWT-like tokens, along with shared hosting utilities such as activity sources for distributed tracing.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP interface for communicating with a CodeGraph Indexer service. It exposes operations for managing indexing runs (listing, querying status), triggering indexing operations (batch analysis, link detection, community detection, schema synchronization, repository discovery, and re-indexing), and handles HTTP communication details such as authentication header injection, query string building, error parsing, and JSON serialization. It is intended to be consumed by other projects in the repository via dependency injection registration.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is the ASP.NET Core web host for the CodeGraph indexing service. It exposes HTTP endpoints for triggering repository indexing operations (discovery, processing, schema sync, linking, community detection, batch analysis) and wires up MassTransit message consumers that react to events in the indexing pipeline (repository processing, analysis batches, synthesis completion, removal). It also manages a TypeScript sidecar process used for language-specific analysis.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (repository discovery, indexing, community detection, MCP doc generation, batch analysis, etc.), a command dispatcher that routes job execution by type name, a scheduling service that manages cron-based schedules with timezone support, and a hosted worker that polls for due schedules. The project wires all of these components together via a DI extension and exposes an entry point, acting as a self-contained job-runner service.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project that verifies the behavior of the job scheduling and dispatching infrastructure in the CodeGraph system. It tests components responsible for acquiring and managing job schedules, dispatching job commands to appropriate services (indexer, admin, batch analysis, assistant cleanup), and ensuring correct argument normalization and lease handling. The project uses in-memory fakes and recording test doubles to isolate and assert on interactions with external service interfaces.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is a .NET HTTP client library that provides typed access to a remote memory graph service. It exposes operations for querying, traversing, writing, and managing a knowledge graph of entities and claims on behalf of named users, communicating over HTTP with JSON serialization. The project defines the client interface, its HTTP implementation, configuration options, and a DI registration extension for ASP.NET Core applications.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API service that provides a graph-based memory store for entities and claims. It exposes REST endpoints for storing, querying, searching, and managing memory claims and entity graphs, and also consumes messages from a message bus (MassTransit) to process incoming memory claims asynchronously. It handles persistence setup, authentication middleware, and health probing for the write pipeline.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository responsible for handling in-memory data migrations or transformations of code graph structures. Based on the project name alone (no source nodes were provided in the graph), it likely manages version transitions or schema evolution for code graph data held in memory.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core microservice responsible for collecting and persisting metrics events within the CodeGraph repository. It subscribes to domain messages (such as LLM usage and MCP tool invocations) via MassTransit message consumers, records those events through a metrics recorder abstraction, and handles persistence setup including database migrations. The service also includes internal service authentication middleware, suggesting it operates in a multi-service architecture where services authenticate with each other.

### CodeGraph.Models

CodeGraph.Models is a shared data contracts library that defines the message types, request/response DTOs, memory graph structures, and exception types used across the CodeGraph repository. It serves as the canonical type definitions for inter-service communication (message bus consumers in Indexer, API, Memory, and Metrics hosts), memory/knowledge graph operations, and HTTP API request/response shapes.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform, a code intelligence system that indexes source repositories into a graph database, analyzes them with LLM providers (OpenAI, Anthropic, LM Studio), performs code reviews, tracks health metrics, manages a structured memory store, and exposes graph query capabilities. It provides services for repository indexing, cross-repo linking, community detection, security scanning, semantic search via ONNX embeddings, an AI assistant with tool use, wiki management, and admin reporting. This project acts as the service/domain layer consumed by API hosts, background job runners, and MCP servers within the broader repository.

### CodeGraph.Tests

CodeGraph.Tests is the comprehensive unit and integration test suite for the CodeGraph repository. It contains test classes covering controllers, services, data stores, extractors, memory subsystems, and host startup across all major subsystems of the application. The project makes extensive use of in-memory fakes, recording stubs, and no-op implementations of repository interfaces to isolate system-under-test components from real infrastructure dependencies.

### reflect

The 'reflect' project appears to be a reflection or introspection utility within the CodeGraph repository, likely providing runtime type inspection, metadata access, or code analysis capabilities. Based on the project name and its position in a repository called CodeGraph, it likely supports graph-based code analysis by enabling reflection over code structures or type information.

