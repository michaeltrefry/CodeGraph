# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-29 00:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that ingests source code repositories, builds a graph representation of their structure and relationships, analyzes them using LLMs, and exposes the results through a web UI and REST APIs. The repository is organized as a multi-project .NET solution with an Angular frontend.

Architecturally, the system is composed of several cooperating layers and services:

1. **Extraction layer**: Language-specific extractor plugins (C#/Roslyn, TypeScript/Node.js, SQL, ColdFusion, Ansible, Terraform, Tree-sitter for C/C++ and others) each implement a common ICodeExtractor interface and parse source files into typed graph nodes and edges.

2. **Indexer service** (CodeGraph.Indexer.Host): An ASP.NET Core host that orchestrates repository discovery, code extraction, graph construction, linking, community/cluster detection, and batch LLM analysis. It is driven by a MassTransit message bus and exposes a REST API for triggering indexing operations. A typed HTTP client (CodeGraph.Indexer.Client) allows other projects to invoke it programmatically.

3. **Data/persistence layer**: CodeGraph.Data defines store interfaces and domain entities. Two backend implementations are provided — CodeGraph.Data.MariaDb (Entity Framework Core + raw MySqlConnector against MariaDB/MySQL, handling relational data including LLM config, assistant runs, wiki, metrics, and migrations) and CodeGraph.Data.Neo4j (Cypher queries against Neo4j, handling the code graph, semantic memory, embeddings, wiki, and job scheduling).

4. **API gateway** (CodeGraph.Api): An ASP.NET Core Web API that serves as the primary HTTP interface for clients. It exposes endpoints for code node queries, project/cluster/schema browsing, search, AI assistant chat, memory graphs, wiki, security findings, and administration. It also consumes message bus events to stay in sync with indexing and analysis lifecycle changes.

5. **Memory subsystem** (CodeGraph.Memory.Host + CodeGraph.Memory.Client): A dedicated ASP.NET Core service for graph-based semantic memory storage and retrieval (entities, claims, relationships, subgraph traversal). Other services interact with it via the typed HttpMemoryClient.

6. **Services library** (CodeGraph.Services): The core business logic layer, coordinating LLM-backed analysis (OpenAI, Anthropic, LM Studio), code review, a conversational graph assistant, repository discovery, cross-repo linking, and a semantic memory subsystem.

7. **Jobs subsystem** (CodeGraph.Jobs): A background scheduling service with cron-based triggers for recurring tasks such as repository discovery, re-indexing, community detection, batch LLM analysis, MCP documentation regeneration, and assistant retention cleanup.

8. **Metrics service** (CodeGraph.Metrics): An ASP.NET Core service that consumes MassTransit messages for LLM usage and MCP tool invocation events and persists them to a relational store.

9. **Shared libraries**: CodeGraph.Models defines all shared DTOs, message bus contracts, and domain types. CodeGraph.Host.Shared provides cross-cutting concerns including HMAC-based internal service authentication and distributed tracing ActivitySources.

10. **Testing**: CodeGraph.Tests and CodeGraph.Jobs.Tests provide unit and integration test coverage with in-memory fakes and recording stubs for all major interfaces.

**External dependencies**: Neo4j (graph database), MariaDB/MySQL (relational database), MassTransit (message bus abstraction), Roslyn (.NET compiler platform for C# extraction), Tree-sitter (multi-language parsing), Node.js (TypeScript analysis server), OpenAI/Anthropic/LM Studio APIs (LLM providers), Entity Framework Core.

**Dependents**: An external system called 'CodeFlow' makes HTTP calls into CodeGraph (likely consuming the API gateway). CodeGraph itself calls an external 'TC.CodeGraphApi' endpoint, suggesting integration with a hosted or tenant-specific instance of its own API.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code intelligence platform consisting of a .NET backend and an Angular frontend. It indexes source code repositories into a graph database, analyzes them using LLMs, and exposes the results through a web UI with features including code graph visualization, impact analysis, cluster detection, wiki management, memory/knowledge graphs, security analysis, and an AI assistant. The repository also contains tooling for TypeScript AST extraction and linting used during the indexing pipeline.

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API project that serves as the primary HTTP gateway for the CodeGraph platform — a system that indexes, analyzes, and graphs code repositories. It exposes REST endpoints for querying code nodes, projects, clusters, schemas, search, AI assistant interactions, memory graphs, wiki content, and administrative operations, while also acting as a message bus consumer that reacts to indexing, analysis, and repository lifecycle events published by other services.

### CodeGraph.Data

CodeGraph.Data is the data access and persistence layer for the CodeGraph repository. It defines a comprehensive set of store interfaces (IGraphStore, IAnalysisStore, IReviewStore, IMetricsStore, IAssistantRunStore, IMemoryGraphStore, etc.) and entity/DTO classes that abstract over multiple backend implementations (Neo4j graph database, MariaDB/MySQL relational database). The project provides contracts for storing and querying code graph nodes and edges, LLM analysis results, assistant chat runs, wiki content, metrics, security findings, job schedules, and memory graph data.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IMemoryGraphStore, and many others) using Entity Framework Core and raw MySqlConnector queries against a MariaDB/MySQL database. The project handles schema migrations, connection management, encryption of sensitive values, and all CRUD/query operations for graph nodes, edges, analysis results, LLM configuration, assistant runs, wiki pages, job schedules, and a semantic memory graph.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph store, memory graph store, vector store, wiki store, job schedule store, and migration runner) by issuing Cypher queries against a Neo4j database and mapping results to typed domain entities. The project serves as the sole Neo4j driver integration point in the repository, encapsulating all graph database I/O for code graph analysis, semantic memory, embeddings, wiki pages, and job scheduling.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a plugin/module within the CodeGraph repository responsible for parsing Ansible YAML files and extracting structured graph data from them. It identifies Ansible-specific constructs such as playbooks, tasks, roles, handlers, variables, and cross-repository references, converting them into graph nodes and edges conforming to the CodeGraph model. It implements the shared ICodeExtractor interface, making it a swappable extractor component in a larger code analysis pipeline.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files to identify and extract structural code elements such as components, functions, SQL queries, HTTP calls, file includes, and object invocations. It implements the common ICodeExtractor interface, integrating into the broader CodeGraph repository's pipeline for building code graphs from multi-language codebases. The extracted data is returned as an ExtractionResult model consumed by downstream graph-building services.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic metadata from C# source files and solutions. It analyzes codebases to produce a graph of classes, methods, dependencies, routes, DI registrations, service bus interactions, and other architectural elements. It serves as the C#-specific extraction engine within a broader multi-language code graph system.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a project that parses SQL files (T-SQL/MySQL) and extracts structural code graph information from them, including tables, views, stored procedures, functions, and indexes. It implements the ICodeExtractor interface to integrate into the broader CodeGraph repository analysis pipeline, producing ExtractionResult objects that represent the SQL schema and object relationships as graph nodes.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis plugin that parses Terraform/HCL infrastructure-as-code files and extracts their structural elements (resources, modules, variables, data sources, outputs, locals) into a graph representation. It implements the ICodeExtractor interface to integrate with the broader CodeGraph repository analysis pipeline, producing nodes and edges that represent Terraform entities and their cross-references.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction component that uses the Tree-sitter parsing library to analyze source files across multiple programming languages (including C, C++, and others registered via extensions). It implements the ICodeExtractor interface to extract structural information—definitions, imports, and relationships—from source code files, producing graph nodes and edges that feed into the broader CodeGraph repository analysis system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based TypeScript analysis server process, coordinates project-level analysis via tsconfig files, and implements linting capabilities — producing structured graph nodes and edges that feed into the broader CodeGraph system.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library for the CodeGraph repository that provides cross-cutting host-level concerns, specifically internal service-to-service authentication using HMAC-signed tokens and host bootstrapping utilities. It defines the contracts, implementations, and DI registration extensions for issuing and validating internal JWT/token credentials between services, along with shared hosting primitives like ActivitySources for distributed tracing.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP client for communicating with the CodeGraph Indexer service. It abstracts the HTTP communication layer, exposing operations for managing indexer runs, triggering analysis pipelines (batch analysis, linking, community detection, schema synchronization, repository discovery, and re-indexing), and querying run status. The project is intended to be consumed by other projects in the repository via dependency injection, as evidenced by the service collection extension method.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is the ASP.NET Core web host for the CodeGraph indexing service. It exposes a REST API for triggering repository indexing operations (discovery, processing, schema sync, linking, community detection, batch analysis) and wires up MassTransit message consumers that react to indexing lifecycle events. It acts as the runtime entry point and orchestration layer, coordinating domain services (graph store, batch analysis, project processing, community detection, security/vitals analysis) via message-driven workflows.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a dispatcher pattern for typed job commands, a scheduling service backed by cron expressions and a database, and a hosted worker that periodically checks for and runs due scheduled jobs. The project orchestrates several concrete jobs such as repository discovery, re-indexing, community detection, MCP documentation regeneration, batch analysis processing, and assistant retention cleanup.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the job scheduling and dispatching infrastructure of the CodeGraph system. It contains test doubles (recording/in-memory implementations of service interfaces) and test classes that verify the behavior of job scheduling, command dispatching, and job-specific execution logic such as repository discovery and batch analysis processing.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides typed, async access to a remote memory graph service. It defines the IMemoryClient interface and its HttpMemoryClient implementation, which wrap HTTP calls for querying, navigating, writing, searching, and managing a graph-based memory store (entities, claims, relationships, subgraphs). The project also includes DI registration helpers and exception/options types for consuming the client in .NET applications.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API service that provides a graph-based memory storage and retrieval system. It exposes REST endpoints for storing, querying, searching, and managing memory claims and entities (including graph traversal, subgraph extraction, frontier expansion, and cleanup operations), and consumes messages from a MassTransit message bus to asynchronously process memory claim storage. It acts as the hosted runtime layer for the memory subsystem within the CodeGraph repository.

### CodeGraph.MemoryMigration

No graph nodes or source code were provided for the CodeGraph.MemoryMigration project, making it impossible to determine its specific responsibilities from the evidence given. Based on the project name alone, it likely handles in-memory data migration logic within the CodeGraph repository, but this is speculative without structural or source evidence.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core service responsible for collecting and persisting metrics events within the CodeGraph system. It consumes messages from a message bus (MassTransit) — specifically LLM usage and MCP tool invocation events — and records them via a metrics event recorder abstraction, likely backed by a relational database (with MariaDB support indicated by storage options).

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines the message, request, response, and domain model types used across the CodeGraph repository. It serves as the canonical source of DTOs, message bus contracts, memory graph structures, and exception types, enabling multiple projects (Indexer, API, Memory, Metrics) to communicate without circular dependencies.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a system that indexes source code repositories into a graph database, analyzes them with LLM providers, and exposes the results through query, review, assistant, and memory services. It coordinates repository discovery, code extraction, graph construction, cross-repo linking, LLM-backed analysis (via OpenAI, Anthropic, and LM Studio), AI-assisted code review, a conversational graph assistant, a semantic memory subsystem, and administrative reporting, all wired together through a dependency-injected service model.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository, covering unit and integration tests across the entire application surface: API controllers, data stores, service logic, code extractors, memory graph operations, LLM configuration, indexing pipelines, and metrics infrastructure. It provides a comprehensive set of in-memory fakes, recording stubs, and no-op implementations of all major interfaces (IGraphStore, IMemoryGraphStore, IAdminStore, IAnalysisModelProvider, etc.) to enable isolated, deterministic testing without external dependencies. The project also includes integration-style tests against MariaDB stores and external providers using real database connections when configured.

### reflect

The 'reflect' project appears to be a minimal or placeholder module within the CodeGraph repository, as no class/interface nodes were provided in the graph. Based on the name 'reflect', it likely provides reflection or introspection utilities, possibly for analyzing code structure or metadata at runtime.

