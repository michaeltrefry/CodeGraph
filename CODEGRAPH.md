# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-11 12:05 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-project .NET/Angular repository that ingests source code repositories, builds graph-based representations of their structure and dependencies, and exposes analysis results through a web API and UI frontend.

The system is organized into distinct layers that collaborate through well-defined interfaces:

**Data contracts and models**: `CodeGraph.Models` defines shared DTOs, message types, and domain entities consumed across all layers. `CodeGraph.Data` declares the store interfaces (IGraphStore, IAnalysisStore, IMemoryGraphStore, IWikiStore, IJobScheduleStore, etc.) without binding to any database technology.

**Persistence**: `CodeGraph.Data.Neo4j` implements those store interfaces by translating domain operations into Cypher queries against a Neo4j graph database, serving as the sole concrete storage adapter.

**Code extraction pipeline**: Multiple extractor projects implement a shared ICodeExtractor interface, each targeting a different language or toolchain — `CodeGraph.Extractors.CSharp` uses Roslyn to analyze C# solutions (classes, methods, DI registrations, HTTP clients, NuGet deps, etc.), `CodeGraph.Extractors.TypeScript` drives a background TypeScript AST server process, `CodeGraph.Extractors.TreeSitter` uses the Tree-sitter library for multi-language parsing, and `CodeGraph.Extractors.Sql` parses SQL files to extract tables, views, procedures, and relationships. All extractors emit ExtractionResult objects (GraphNode, PendingEdge) consumed by the service layer.

**Business logic**: `CodeGraph.Services` is the core orchestration layer. It coordinates file discovery, extractor invocation, cross-repository dependency linking, community/cluster detection, security scanning, LLM-assisted code review and Q&A, memory graph management, wiki documentation generation, and MCP (Model Context Protocol) server exposure. It sits between the persistence layer and the API/job layers.

**HTTP gateway**: `CodeGraph.Api` is an ASP.NET Core Web API that exposes REST endpoints for project/repository management, graph queries, memory/knowledge-graph operations, wiki management, cluster detection, and AI-assisted Q&A. It also hosts MassTransit message consumers that react to asynchronous events from background workers.

**Background jobs**: `CodeGraph.Jobs` is a separate ASP.NET Core service providing cron-scheduled and on-demand background job execution (repository discovery, batch processing, batch result processing). It uses a dispatcher pattern backed by a persistent job schedule store and communicates results via a message bus.

**Testing**: `CodeGraph.Tests` and `CodeGraph.Jobs.Tests` provide unit and integration test coverage using in-memory fakes and recording test doubles for all major interfaces, following xUnit conventions.

Key external dependencies observed across the codebase include Neo4j (graph database), MassTransit (message bus abstraction), Roslyn (C# code analysis), Tree-sitter (multi-language parsing), LLM provider integrations (for AI-assisted features), and Angular (frontend UI). No external repositories are listed as consumers of this codebase — it appears to be a self-contained application platform.

## Projects

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project that verifies the behavior of job scheduling, dispatching, and execution logic within the CodeGraph.Jobs subsystem. It provides in-memory fakes and recording test doubles for infrastructure dependencies (schedule store, message bus, admin service, batch analysis service), and contains xUnit-style async test classes for job types including DiscoverRepositoriesJob, ProcessRepositoriesJob, ProcessBatchResultsJob, and the JobCommandDispatcher/JobScheduleService orchestration layer.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository. It contains unit and integration tests covering extractors, services, data stores, API controllers, memory systems, and analysis pipelines. The project relies heavily on in-memory fake/stub implementations of core interfaces (IGraphStore, IMemoryGraphStore, IExclusionService, IAnalysisModelProvider, etc.) to isolate the system under test without requiring real databases or external services.

### CodeGraph

CodeGraph is a repository analysis and visualization platform that indexes code repositories, builds graph-based representations of codebases (nodes, edges, clusters, dependencies), and exposes them through an Angular web frontend and a set of backend services. The project encompasses TypeScript/Angular UI components, REST API services, background job infrastructure, code extraction/analysis pipelines, wiki management, AI-assisted code review, memory graph features, and a TypeScript AST extractor tool.

### CodeGraph.Data

CodeGraph.Data is the data access and persistence abstraction layer for the CodeGraph repository. It defines the store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IMemoryGraphStore, IWikiStore, IVectorStore, IJobScheduleStore, etc.) and the corresponding entity/DTO classes that represent persisted data. It sits between the domain/service layer and the concrete database implementations (e.g., Neo4j), providing a technology-agnostic contract for all storage operations across code graph analysis, memory graphs, wiki content, job scheduling, and code review.

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API that serves as the central HTTP and messaging gateway for the CodeGraph system — a platform for indexing, analyzing, and querying code repositories as graphs. It exposes REST endpoints for project/repository management, code graph queries, memory/knowledge-graph operations, wiki management, cluster detection, AI-assisted Q&A, and administrative settings, while also hosting MassTransit message consumers that react to asynchronous events produced by background analysis workers.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (Microsoft's .NET compiler platform) to parse and extract structural metadata from C# solutions and source files. It walks syntax trees to identify classes, methods, interfaces, routes, DI registrations, service bus events, HTTP client calls, NuGet dependencies, and other code relationships, producing structured ExtractionResult objects consumed by the broader CodeGraph system.

### CodeGraph.Jobs

CodeGraph.Jobs is an ASP.NET Core web service that manages and executes background jobs related to code repository analysis and graph processing. It provides HTTP endpoints for triggering jobs (repository discovery, batch processing, etc.), a cron-based job scheduling system backed by persistent schedule entities, and a dispatcher pattern for routing job execution by type. The project serves as the job orchestration layer within the CodeGraph repository ecosystem.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files and extract structural information (definitions and imports) for multiple programming languages. It implements the ICodeExtractor interface to integrate with the broader CodeGraph pipeline, parsing syntax trees to identify named entities (classes, functions, etc.) and their relationships, then emitting graph nodes and edges for the CodeGraph knowledge graph.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph, vector, memory, wiki, job scheduling, etc.) by translating domain operations into Cypher queries executed against a Neo4j database. This project serves as the primary infrastructure adapter, mapping between the application's domain models (graph nodes, edges, analysis batches, security findings, review runs, wiki pages, memory entities, etc.) and their Neo4j node/relationship representations.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a project responsible for parsing SQL files and extracting structural information (tables, views, procedures, functions, indexes, and relationships) into a graph-compatible model. It serves as a SQL-specific extraction plugin within the larger CodeGraph repository, implementing the shared ICodeExtractor interface to produce ExtractionResult objects that represent the code graph nodes and edges derived from SQL source files.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a background TypeScript analysis server process, delegates extraction and linting work to it, and transforms the results into the shared graph model (GraphNode, PendingEdge, ExtractionResult) used by the broader CodeGraph repository.

### CodeGraph.Models

CodeGraph.Models is a shared data contract library containing DTOs, message types, request/response models, and domain entities used across the CodeGraph system. It defines the structures for memory graph operations, message bus events, API requests/responses, and exception types that are consumed by other projects such as API consumers and services within the repository.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph repository — a system that indexes source code repositories into a graph database, analyzes them using multiple LLM providers, and exposes the results via query services, an AI assistant, and MCP (Model Context Protocol) servers. It orchestrates everything from file discovery and code extraction through cross-repo dependency linking, community detection, security scanning, code review generation, memory graph management, and wiki documentation, acting as the service layer between the data/persistence layer and the API/job layers.

