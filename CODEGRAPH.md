# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-18 12:13 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack, multi-language code intelligence platform that indexes source repositories, builds graph-based representations of their structure, and exposes that graph through a web UI, REST API, and AI-powered tooling. The repository is organized as a .NET solution with an Angular frontend and a TypeScript extractor tool, structured around several distinct layers that work together as a distributed system.

At the core, CodeGraph.Services contains the primary business logic: orchestrating multi-language code extraction, cross-repository dependency linking, LLM-backed analysis and review workflows, a knowledge/memory graph, wiki management, and MCP (Model Context Protocol) server tooling. This layer is consumed by two ASP.NET Core host projects — CodeGraph.Api (the user-facing and administrative HTTP surface) and CodeGraph.Indexer.Host (the indexing pipeline host) — and by CodeGraph.Jobs, which provides cron-based background job scheduling and dispatching.

Persistence is split across two backing stores: CodeGraph.Data.Neo4j implements Cypher-based read/write operations against a Neo4j graph database for structural graph data, analysis batches, memory graphs, vector embeddings, and wiki content; CodeGraph.Data.MariaDb provides EF Core and raw SQL implementations against MariaDB/MySQL for relational concerns including metrics, reviews, MCP hub configuration, job schedules, and admin data. Both implement store interfaces defined in CodeGraph.Data, the canonical contract layer.

Language-specific extraction is handled by a family of ICodeExtractor plugins: CodeGraph.Extractors.CSharp (Roslyn-based), CodeGraph.Extractors.TypeScript (delegates to a Node.js analysis server via IPC), CodeGraph.Extractors.Rust (invokes scip-rust/rust-analyzer to produce SCIP JSON), CodeGraph.Extractors.Sql, CodeGraph.Extractors.Ansible, CodeGraph.Extractors.ColdFusion, CodeGraph.Extractors.Terraform, and CodeGraph.Extractors.TreeSitter (C/C++ via Tree-sitter). Each plugin produces ExtractionResult objects (nodes and edges) consumed by the indexing pipeline.

Async inter-service communication is handled via MassTransit message consumers registered in both the API and indexer hosts, coordinating pipeline stages such as repository processing, batch analysis submission, and synthesis completion. CodeGraph.Indexer.Client provides a typed HTTP client for triggering indexing operations from other components.

Supporting services include: CodeGraph.Memory.Host (a standalone ASP.NET Core service exposing a memory graph API for structured entity/claim knowledge, backed by Neo4j); CodeGraph.Metrics (a microservice consuming LLM and MCP usage events from the message bus and persisting metrics); CodeGraph.Mcp.Hub (a multi-provider MCP hub exposing AI-callable tools for MySQL querying, RabbitMQ inspection, and Shortcut project management, with per-token entitlements and audit logging); and CodeGraph.Host.Shared (shared HMAC-based inter-service authentication utilities).

Shared contracts are centralized in CodeGraph.Models (DTOs, message types, request/response structures) and CodeGraph.Data (store interfaces and entity definitions). Test coverage is provided by CodeGraph.Tests (comprehensive unit/integration tests with in-memory fakes for all external dependencies) and CodeGraph.Jobs.Tests (unit tests for job scheduling and dispatching).

The Angular frontend (CodeGraphWeb, referenced in the top-level analysis) provides visualization, AI assistant chat, wiki, admin, and code review UI, consuming the CodeGraph.Api HTTP surface.

Externally, the repository makes outbound HTTP calls to a service identified as TC.CodeGraphApi (suggesting a separate deployment or tenant instance of the API). It is itself depended upon by CodeFlow and codeflow-team repositories, which make inbound HTTP calls to CodeGraph's API — indicating CodeGraph serves as a code intelligence backend for at least one external workflow or tooling platform. External infrastructure dependencies include Neo4j, MariaDB/MySQL, RabbitMQ (via MassTransit), and LLM provider APIs.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code analysis platform that indexes source repositories (C#, TypeScript, Rust), builds a graph of their structure, and exposes that graph through a web UI and API. The repository contains a .NET backend (jobs, pipeline, migration services, MCP hub integration) and an Angular frontend (CodeGraphWeb) that provides visualization, AI assistant chat, wiki, admin, and code review features. A TypeScript extractor tool (tools/ts-extractor) handles AST-level analysis of TypeScript workspaces.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API host for the CodeGraph platform — a code intelligence and analysis system that indexes source repositories, builds graph-based representations of code structure, and exposes LLM-powered assistant, code review, memory, and MCP (Model Context Protocol) hub capabilities. It wires together authentication, message bus consumers (via MassTransit), REST controllers, middleware, and service registrations that delegate to downstream services for graph storage, indexing, analysis, and AI interactions. The project serves as the HTTP surface and integration layer, coordinating async workflows through message consumers and exposing administrative, user-facing, and internal endpoints.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all persistence interfaces (store contracts) and entity/DTO classes used across the system. It abstracts over multiple backing stores (Neo4j graph database, MariaDB/MySQL relational database) and covers domains including code graph storage, LLM analysis, AI assistant runs, wiki content, MCP hub tooling, memory graphs, metrics, reviews, job scheduling, and vector embeddings. It acts as the canonical contract layer that upstream services depend on and downstream storage implementations fulfill.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph platform. It provides concrete EF Core and raw SQL implementations of all data store interfaces (graph, analysis, metrics, review, memory, MCP, wiki, admin, etc.) backed by a MariaDB database, including schema migration support via MariaDbMigrationRunner. The project also handles connection management, AES encryption of sensitive credentials, and dependency injection registration for all stores.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph nodes/edges, analysis batches, memory graph, vector embeddings, wiki, job scheduling, and metrics) by issuing Cypher queries against a Neo4j database via the official .NET driver. Its primary role is to translate between the domain model objects defined elsewhere in the repository and Neo4j graph nodes/relationships, providing all read/write operations needed by higher-level services.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a code graph extraction plugin that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and produces a structured graph representation of their contents and relationships. It identifies Ansible-specific constructs such as tasks, modules, roles, handlers, and variables, and emits graph nodes and edges that can be consumed by the broader CodeGraph analysis pipeline. Its role in the repository is to extend the generic ICodeExtractor interface with Ansible-specific YAML parsing logic.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion source files (.cfm/.cfc) and produces a structured ExtractionResult representing the code graph for those files. It implements the shared ICodeExtractor interface from the repository's extraction service layer, using regex-based parsing to identify ColdFusion constructs such as components, functions, includes, SQL queries, HTTP calls, object creations, and component invocations.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a Roslyn-based static analysis project that extracts structural and semantic metadata from C# solutions and source files. It analyzes class hierarchies, method signatures, dependency injection registrations, HTTP routes, service bus events, NuGet references, and cross-service call patterns, producing an ExtractionResult graph consumed by the broader CodeGraph system. It implements service interfaces (ISolutionAnalyzer, ICodeExtractor, INuGetReferenceExtractor) and uses MSBuild and Roslyn APIs to load and walk syntax trees.

### CodeGraph.Extractors.Rust

CodeGraph.Extractors.Rust is a project responsible for extracting code graph data from Rust projects. It works by invoking external tooling (likely rust-analyzer or scip-rust) to generate SCIP (Semantic Code Intelligence Protocol) JSON output, then parsing that JSON to produce structured ExtractionResult objects containing nodes, edges, and symbol information suitable for ingestion into a broader code graph system.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information from them. It identifies SQL objects such as tables, views, stored procedures, functions, and indexes, then builds a graph representation of their definitions and relationships. This project implements the ICodeExtractor interface, making it a pluggable SQL-specific extractor within the larger CodeGraph repository's code analysis pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a parser/extractor plugin that processes Terraform HCL (HashiCorp Configuration Language) files and extracts structured graph data from them. It identifies Terraform constructs such as resources, modules, data sources, variables, outputs, and locals, then produces graph nodes and edges representing their relationships. This project implements the ICodeExtractor interface, integrating into the broader CodeGraph repository's extensible code analysis pipeline.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files and extract structural information (definitions, imports, relationships) from multiple programming languages including C and C++. It implements the ICodeExtractor interface to produce graph nodes and edges representing code structure, feeding into the broader CodeGraph repository analysis pipeline.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based TypeScript analysis server process, delegates extraction and linting work to it via IPC, and converts the results into the repository's internal graph model (GraphNode, PendingEdge, ExtractionResult). It acts as the TypeScript-specific extractor plugin within a broader multi-language code analysis pipeline.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library that provides internal service-to-service authentication and host bootstrapping utilities for the CodeGraph repository. It implements a lightweight HMAC-based token scheme (create, sign, validate) for securing inter-service communication, and exposes DI extension methods to register these services consistently across all host projects.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP client for communicating with a CodeGraph Indexer service. It abstracts the HTTP communication details and exposes operations for triggering indexing pipeline stages (batch analysis, linking, community detection, schema sync, repository discovery, re-indexing) and querying run status. The project is intended to be consumed by other components in the repository via dependency injection registration.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web application that serves as the hosting layer for a code graph indexing pipeline. It exposes REST API endpoints for triggering indexing operations (repository processing, schema sync, cross-repo linking, community detection, batch analysis) and wires up MassTransit message consumers that react to pipeline events such as repository processing, analysis batch submission, and synthesis completion. It orchestrates a graph-based code analysis system, persisting results to a graph store and coordinating work across services via a message bus.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (indexing repositories, detecting communities, ingesting embeddings, etc.), a dispatcher that routes job execution by type name, a scheduling service that manages cron-based schedules stored in a database, and a hosted worker that periodically polls for due schedules and runs them. The project acts as the asynchronous task engine for the broader CodeGraph system.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the job scheduling and dispatching infrastructure of the CodeGraph system. It contains test doubles (recording/fake implementations of service interfaces), an in-memory job schedule store, and test classes that verify the behavior of job command dispatching, schedule management, and specific job types such as batch analysis processing and repository discovery.

### CodeGraph.Mcp.Hub

CodeGraph.Mcp.Hub is a multi-provider MCP (Model Context Protocol) hub service that exposes a unified set of AI-callable tools for querying MySQL databases, inspecting RabbitMQ queues, and interacting with the Shortcut project management API. It manages tool catalogs, per-token entitlements, provider configuration/credentials, audit logging, sensitive column access policies, and can proxy calls to downstream MCP shim servers. The project serves as the core runtime engine for the hub, providing in-memory store implementations, hosted service lifecycle management, and DI registration extensions.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides a typed .NET interface for communicating with a remote memory graph service. It abstracts REST API calls for querying, storing, and managing a graph-based memory system that tracks entities, claims, relationships, and subgraphs on a per-user basis. The project exposes a dependency-injection-friendly registration extension and a concrete HttpClient-backed implementation of the IMemoryClient contract.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web service that exposes a memory graph API, allowing clients to store, query, search, and manage structured knowledge (entities, claims, and their relationships) in a graph database. It supports both HTTP REST endpoints and asynchronous message consumption via MassTransit for write operations, and includes persistence configuration, authentication middleware, and health monitoring for write path validation.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository that handles in-memory data migration operations. Based on the project name, it likely provides infrastructure for migrating data structures or graph representations in memory, potentially serving as a migration layer for the CodeGraph system's internal data models.

### CodeGraph.Metrics

CodeGraph.Metrics is a microservice responsible for collecting and persisting metrics events within the CodeGraph repository. It consumes domain events (such as LLM usage and MCP tool invocations) from a message bus (MassTransit), records them via a metrics recorder abstraction, and manages its own persistence layer with database migrations. It runs as an ASP.NET Core web application with internal service authentication.

### CodeGraph.Models

CodeGraph.Models is a shared models/contracts library that defines the data transfer objects, message types, request/response structures, and exception types used across the CodeGraph repository. It serves as the central contract layer consumed by indexers, API hosts, memory services, and metrics consumers, enabling decoupled communication between distributed components via message bus patterns and shared graph data structures.

### CodeGraph.Services

CodeGraph.Services is the primary business logic layer of the CodeGraph system, a code intelligence platform that indexes source repositories into a graph database and provides AI-powered analysis, review, search, and assistant capabilities. It orchestrates code extraction from multiple languages (C#, TypeScript, Rust, etc.), cross-repository dependency linking, LLM-backed analysis and review workflows, a memory/knowledge graph system, wiki management, and MCP (Model Context Protocol) server tooling. The project acts as the central service layer consumed by API and job host projects, defining the interfaces and concrete implementations for all major application concerns.

### CodeGraph.Tests

CodeGraph.Tests is the primary test project for the CodeGraph repository, containing unit and integration tests that verify the behavior of services, controllers, data stores, extractors, and infrastructure components across the entire system. It provides a comprehensive suite of test doubles (in-memory stores, recording fakes, stub providers, and no-op implementations) that substitute for real external dependencies such as databases, LLM providers, message buses, and external APIs. The project exercises everything from code graph extraction pipelines and batch LLM analysis to memory operations, MCP hub services, repository review workflows, and MariaDB data layer round-trips.

### reflect

The 'reflect' project appears to be a reflection or introspection utility within the CodeGraph repository, likely providing mechanisms to analyze, traverse, or describe code structures. Based on the project name and its position in the repository, it likely supports runtime or static analysis of code graphs, types, or class hierarchies.

