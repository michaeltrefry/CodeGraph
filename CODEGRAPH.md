# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-08 11:50 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-project .NET/Angular platform for automated code analysis and knowledge graph construction. It ingests source repositories (C#/.NET, TypeScript, SQL, C/C++), extracts structural and semantic information from them, persists the results in a Neo4j graph database, and exposes the data through a REST API and Angular web frontend.

The repository is organized into well-defined layers:

**Extraction layer** — Language-specific extractors (CodeGraph.Extractors.CSharp using Roslyn, CodeGraph.Extractors.TypeScript using an IPC-managed analysis server, CodeGraph.Extractors.Sql parsing T-SQL, CodeGraph.Extractors.TreeSitter using the Tree-sitter library for C/C++) each implement a shared ICodeExtractor interface and produce ExtractionResult models (GraphNode, PendingEdge, etc.).

**Services layer** — CodeGraph.Services is the central business logic hub. It orchestrates repository discovery and cloning (GitHub, GitLab, local), drives the extraction pipeline, links cross-repository dependencies, performs community detection, runs security scanning, generates wiki documentation, and provides semantic search via ONNX embeddings. It consumes the extractor projects and the data abstractions.

**Data layer** — CodeGraph.Data defines all storage interfaces (IGraphStore, IAnalysisStore, IWikiStore, IVectorStore, IMemoryGraphStore, IJobScheduleStore, etc.) and entity/DTO types, acting as the contract boundary. CodeGraph.Data.Neo4j implements these interfaces by translating operations into Cypher queries against a Neo4j backend.

**API layer** — CodeGraph.Api is an ASP.NET Core Web API that exposes REST endpoints (nodes, projects, clusters, search, memory, wiki, settings) and hosts MassTransit consumers for domain events (repository indexing, batch analysis completion, memory storage). It delegates to CodeGraph.Services and bridges HTTP clients with the internal message bus.

**Jobs layer** — CodeGraph.Jobs is a separate ASP.NET Core application providing cron-scheduled and on-demand job execution (DiscoverRepositories, ProcessRepositories, ProcessBatchResults, ProcessBatchAnalysis). It uses a dispatcher pattern to invoke named job commands and coordinates analysis workloads asynchronously.

**Models layer** — CodeGraph.Models is a shared library defining inter-service message types, API request/response DTOs, memory graph entity models, and shared exception types used across all other projects.

**Test projects** — CodeGraph.Tests and CodeGraph.Jobs.Tests provide unit and integration test coverage using in-memory fakes and recording test doubles for all major interfaces, isolating components without requiring live database or network connections.

The system depends on Neo4j (graph persistence), MassTransit (message bus), Roslyn (.NET compiler APIs), Tree-sitter (multi-language parsing), ONNX Runtime (embeddings/semantic search), and GitHub/GitLab APIs (repository discovery). No external repositories are known to depend on this one.

## Projects

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project that verifies the behavior of the job scheduling and execution infrastructure in the CodeGraph system. It contains unit tests for job classes (DiscoverRepositories, ProcessRepositories, ProcessBatchResults, ProcessBatchAnalysis), the JobCommandDispatcher, and the JobScheduleService, using in-memory fakes and recording test doubles in place of real services and storage backends.

### CodeGraph.Tests

CodeGraph.Tests is the test project for the CodeGraph repository, containing unit and integration tests for graph extraction, analysis services, data stores, and repository providers. It provides in-memory fake/stub implementations of core interfaces (IGraphStore, IWikiStore, IExclusionService, IAnalysisModelProvider, etc.) to isolate components under test, and exercises business logic spanning code graph extraction (Roslyn, SQL, TreeSitter), batch analysis workflows, security scanning, repository provider discovery, and wiki seeding.

### CodeGraph

CodeGraph is a full-stack code analysis and visualization platform that parses source repositories (C#/.NET and TypeScript), extracts structural information (nodes, edges, dependencies, security findings, clusters), stores it in a graph database, and presents it through an Angular web frontend. The system supports background job processing for indexing, community detection, batch analysis, and wiki documentation generation, while exposing a REST API consumed by both the web UI and external tooling.

### CodeGraph.Data

CodeGraph.Data is the data access and persistence abstraction layer for the CodeGraph repository. It defines all storage interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IWikiStore, IVectorStore, IMemoryGraphStore, IJobScheduleStore, etc.) and the corresponding entity/DTO classes used throughout the system. The project acts as a contract boundary between domain services and concrete database implementations (e.g., Neo4j), enabling dependency injection and testability via in-memory implementations.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API layer of the CodeGraph system, responsible for exposing HTTP endpoints and coordinating message-driven background processing. It provides REST controllers for querying and managing a code graph (nodes, projects, clusters, search, memory, wiki, settings), and hosts MassTransit consumers that react to domain events such as repository indexing, analysis batch completion, and memory storage. It acts as the central orchestration hub, delegating to domain services while bridging HTTP clients and the internal message bus.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to parse and extract structural and semantic information from C# source files and solutions. It identifies classes, methods, interfaces, namespaces, dependency injection registrations, HTTP routes, service bus event patterns, NuGet references, and cross-service call relationships, producing structured ExtractionResult models for use by other parts of the CodeGraph repository. It serves as the C#-specific extraction engine within a broader multi-language code graph construction system.

### CodeGraph.Jobs

CodeGraph.Jobs is an ASP.NET Core web application that serves as a job execution and scheduling service for the CodeGraph repository analysis platform. It exposes HTTP endpoints to trigger jobs (discovering repositories, processing repositories, processing batch results), manages cron-based job schedules via a scheduling service and background worker, and dispatches named job commands through a type-safe dispatcher pattern. The project acts as the runtime orchestration layer that coordinates various analysis workloads (indexing, community detection, MCP doc generation, etc.) against code repositories.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files of multiple languages (including C and C++). It implements the ICodeExtractor interface to parse source files into ASTs, then walks those trees to extract symbol definitions (classes, functions, etc.) and import/dependency relationships, producing GraphNode and edge data structures that feed into the broader CodeGraph repository analysis pipeline.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph, vector, memory, wiki, metrics, analysis, job scheduling, etc.) by translating domain operations into Cypher queries executed against a Neo4j database. This project serves as the sole infrastructure adapter between the application's data abstractions and the Neo4j graph database backend.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis component that parses T-SQL source files and extracts structural graph information from them. It identifies and records SQL objects such as tables, views, stored procedures, functions, and indexes, along with their relationships (e.g., table references within procedures), producing an ExtractionResult that feeds into the broader CodeGraph repository analysis pipeline.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a background TypeScript analysis server process, delegates extraction and linting work to it via IPC, and maps the results into the shared CodeGraph model types (GraphNode, PendingEdge, ExtractionResult). It implements the ICodeExtractor and ITypeScriptAnalyzer service interfaces defined elsewhere in the repository.

### CodeGraph.Models

CodeGraph.Models is a shared data models library that defines the contracts used across the CodeGraph repository system. It contains message types for inter-service communication (consumed by API consumers), request/response DTOs for API endpoints, memory graph entity models for a knowledge graph subsystem, and shared exception types for retry logic control.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a system that indexes source code repositories into a knowledge graph, analyzes them with LLMs, and exposes the results via query APIs and AI assistant interfaces. It orchestrates repository discovery and cloning (GitHub, GitLab, local folders), file extraction and graph indexing, cross-repository dependency linking, code health/vitals analysis, semantic search via ONNX embeddings, community detection, security scanning, and a wiki. It serves as the hub connecting the data layer (CodeGraph.Data), code extractors (CodeGraph.Extractors.*), and consumer-facing APIs.

