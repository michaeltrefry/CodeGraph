# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-28 14:08 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that indexes source repositories into a graph database and exposes that graph for analysis, visualization, and AI-assisted reasoning. The repository is organized as a multi-project .NET solution with an Angular frontend.

Core responsibilities break down as follows:

**Extraction pipeline**: Language-specific extractor projects (CSharp/Roslyn, TypeScript/Node.js sidecar, SQL/T-SQL, Terraform/HCL, Ansible/YAML, ColdFusion, C/C++ via Tree-sitter) each implement a common ICodeExtractor interface and emit normalized graph nodes and edges (ExtractionResult). These feed into CodeGraph.Indexer.Host, an ASP.NET Core service that orchestrates the indexing pipeline — triggering repository processing, schema sync, community detection, and cross-repo linking — and wires up MassTransit message consumers for asynchronous pipeline events.

**Persistence layer**: Two interchangeable storage backends are provided. CodeGraph.Data.Neo4j implements all store interfaces using Cypher queries against a Neo4j graph database. CodeGraph.Data.MariaDb implements the same interfaces using Entity Framework Core and raw MySqlConnector queries against MariaDB/MySQL. CodeGraph.Data defines the shared interface contracts (IGraphStore, IAnalysisStore, IReviewStore, IMetricsStore, IMemoryGraphStore, IWikiStore, etc.) and all entity/DTO types, acting as the abstraction boundary.

**API layer**: CodeGraph.Api is the ASP.NET Core Web API that fronts all platform capabilities over REST and Server-Sent Events. It handles authentication (JWT, local dev, MCP personal access tokens), delegates to domain services in CodeGraph.Services, and integrates with the message bus for asynchronous processing.

**Business logic**: CodeGraph.Services is the core service layer integrating LLM providers (OpenAI, Anthropic, LM Studio) for code analysis, review, and a conversational AI assistant. It also manages a semantic memory store, exposes MCP (Model Context Protocol) tools for AI agents, and connects repository providers (GitHub, GitLab, local folders).

**Supporting microservices**: CodeGraph.Memory.Host is a dedicated REST service for the memory graph (entities and claims with semantic search). CodeGraph.Metrics is a microservice that consumes MassTransit events (LLM usage, MCP tool invocations) and records metrics. CodeGraph.Jobs provides cron-based and manual job scheduling with a dispatcher routing to typed handlers for recurring tasks (re-indexing, community detection, batch analysis, MCP doc regeneration).

**Client libraries**: CodeGraph.Indexer.Client and CodeGraph.Memory.Client are typed HTTP client libraries with DI registration helpers, enabling other services to call the indexer and memory APIs without coupling to HTTP details.

**Shared infrastructure**: CodeGraph.Host.Shared provides HMAC-based internal service-to-service authentication, DI extensions, and observability primitives (activity sources) consumed across host projects. CodeGraph.Models is the central DTO/message-contract library shared across all services.

**Frontend**: CodeGraphWeb is an Angular application providing UI for graph exploration, cluster visualization, project health, security findings, wiki management, AI chat assistant, memory graphs, admin configuration, and schema browsing.

**Testing**: CodeGraph.Tests and CodeGraph.Jobs.Tests provide unit and integration test coverage using in-memory fakes, recording stubs, and throwing test doubles for all major subsystems.

**External dependencies**: The platform depends on Neo4j or MariaDB for persistence, a MassTransit-compatible message bus for async event processing, OpenAI/Anthropic/LM Studio for LLM capabilities, GitHub/GitLab APIs for repository access, and a Node.js process as a sidecar for TypeScript analysis.

**Dependents**: External repositories that call into CodeGraph's API include KanbanBoard (six observed HTTP call edges), CodeFlow (three edges), and TC.CodeGraphApi (two edges). CodeGraph itself also makes outbound HTTP calls to TC.CodeGraphApi (two edges), suggesting a bidirectional integration with that system.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **KanbanBoard**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=DELETE, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/memory/writes/{param}, route_template=api/memory/writes/{receiptId}, confidence_band=medium
- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack repository analysis platform consisting of a .NET backend and an Angular frontend. The backend indexes codebases (C#, TypeScript, NuGet references) into a graph database, supports migration between Neo4j and MariaDB, runs background jobs for community detection, LLM-powered reviews, and re-indexing, and exposes REST/streaming APIs. The Angular frontend (CodeGraphWeb) provides a rich UI for exploring code graphs, clusters, project health, security findings, wiki management, AI chat assistant, memory graphs, admin configuration, and schema browsing.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API layer of the CodeGraph system, responsible for exposing REST endpoints that front all major platform capabilities: code repository indexing and analysis, AI-assisted code review, an LLM-backed conversational assistant, a knowledge graph memory system, database schema discovery, wiki management, and admin/reporting functions. It integrates with a message bus (MassTransit-style consumers) to handle asynchronous processing events, implements multiple authentication schemes (JWT, local dev, MCP personal access tokens), and acts as an orchestration hub that delegates to domain services while streaming results to clients via Server-Sent Events.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all storage interfaces and entity/data-transfer types used across the system. It declares a comprehensive set of store interfaces covering graph storage, analysis, review, metrics, memory graph, wiki, LLM configuration, assistant runs, job scheduling, vector search, MCP tokens, and admin operations. It serves as the contract layer between domain services and concrete database implementations (Neo4j and MariaDB), while also housing all entity classes that map to persisted records.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, and many others) using Entity Framework Core (CodeGraphDbContext) and raw MySqlConnector queries. The project handles schema migrations, node/edge graph storage, LLM configuration, memory graph operations, assistant run tracking, wiki, job scheduling, vector embeddings, and administrative data — serving as the single source of truth for all persistent state in the system.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements all data-store interfaces defined in the broader CodeGraph.Data abstraction layer, providing concrete Cypher-based implementations for storing and querying code graphs (nodes, edges, projects), AI analysis batches, memory graphs, vector embeddings, wiki pages, job schedules, and metrics — all backed by a Neo4j graph database.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a code analysis component that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and extracts a graph representation of their structure and relationships. It identifies Ansible-specific constructs such as plays, tasks, roles, variables, and cross-repository references, emitting graph nodes and edges that feed into the broader CodeGraph repository analysis system. It implements the ICodeExtractor interface, integrating with the platform's general extraction pipeline.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a code analysis project responsible for parsing ColdFusion (.cfm/.cfc) source files and extracting structural metadata from them. It implements the repository's common ICodeExtractor interface to produce ExtractionResult objects containing information about components, functions, include directives, SQL queries, HTTP calls, object creation, and component invocations found within ColdFusion files. This project serves as the ColdFusion-specific plugin within a broader multi-language code graph extraction system.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a Roslyn-based static analysis project that parses C# source code and .NET solutions to extract structural metadata — classes, methods, interfaces, routes, DI registrations, service bus events, HTTP client calls, NuGet dependencies, and more — producing a normalized ExtractionResult graph. It serves as the C# language extraction backend within the broader CodeGraph repository, implementing extractor service interfaces consumed by higher-level orchestration layers.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL script files and extracts structural graph information from them. It processes T-SQL syntax to identify and catalog database objects such as tables, views, stored procedures, functions, and indexes, producing an ExtractionResult that contributes to the broader CodeGraph repository's code analysis pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis plugin that parses HashiCorp Configuration Language (HCL) files used by Terraform infrastructure-as-code. It extracts structural elements such as resources, modules, data sources, variables, outputs, and locals from .tf and .tfvars files, then produces a graph of nodes and edges representing relationships (references, dependencies) between infrastructure components. This project implements the ICodeExtractor interface, making it a pluggable extractor within the larger CodeGraph repository's extraction pipeline.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files across multiple programming languages (at minimum C and C++). It implements the ICodeExtractor interface to parse source files into syntax trees, then walks those trees to extract symbol definitions (classes, functions, etc.) and import/dependency relationships, producing structured graph nodes and edges for the CodeGraph knowledge graph system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a .NET project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based TypeScript analysis server process, communicates with it to extract structural information (nodes, edges) and lint results from TypeScript codebases, and transforms those results into the repository's shared CodeGraph model types. It acts as a bridge between the .NET CodeGraph pipeline and TypeScript-specific tooling.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing cross-cutting concerns for host services within the CodeGraph repository. It implements internal service-to-service authentication using HMAC-signed tokens, along with DI registration extensions and observability primitives (activity sources) intended to be consumed by multiple host projects.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a typed HTTP client library that provides a .NET abstraction over a remote CodeGraph Indexer service API. It exposes operations for triggering indexing workflows (batch analysis, discovery, linking, community detection, schema synchronization, repository processing) and querying run status, while handling HTTP mechanics such as request construction, error parsing, and identity header attachment. It also includes DI registration extensions for easy integration into ASP.NET Core or generic host applications.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is an ASP.NET Core web application that serves as the hosting layer for a code graph indexing pipeline. It exposes HTTP API endpoints for triggering indexing operations (repository processing, schema sync, community detection, cross-repo linking, batch analysis) and wires up MassTransit message consumers that react to pipeline events published across the message bus. It coordinates services for graph storage, project analysis, community detection, security analysis, and a TypeScript sidecar process.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (repository discovery, indexing, community detection, MCP doc regeneration, batch analysis, etc.), a dispatcher that routes job execution requests to typed command handlers, and a scheduling service that manages cron-based and manual job schedules persisted to a database. A hosted worker service polls for due schedules and triggers execution, making this project the operational backbone for automated, recurring analysis tasks.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit/integration test project for the CodeGraph.Jobs scheduling and dispatch infrastructure. It provides in-memory fakes and recording test doubles for external service dependencies (job store, admin service, indexer client, batch analysis service, etc.) and contains test classes that verify job scheduling logic, command dispatching, and argument normalization without requiring real infrastructure.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is a .NET HTTP client library that provides typed access to a remote memory graph API. It defines an abstraction (IMemoryClient) and a concrete HTTP implementation (HttpMemoryClient) for querying, writing, and managing a knowledge graph of entities and claims, including operations like graph traversal, semantic search, subgraph extraction, and diagnostic inspection. The project also provides dependency injection registration helpers for integrating the client into ASP.NET Core or similar host applications.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API service that acts as the hosting layer for a memory graph system. It exposes REST endpoints for storing, querying, searching, and managing a knowledge graph of memory claims and entities, while also consuming messages via MassTransit for asynchronous claim ingestion. It handles persistence registration, service configuration, and health monitoring for the memory write pipeline.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository focused on handling in-memory data migration operations. Based on the project name, it likely provides utilities or abstractions for migrating code graph data structures between different formats or versions in memory, serving as a supporting module for the broader CodeGraph system.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core microservice responsible for collecting and persisting metrics events within the CodeGraph repository. It consumes messages from a message bus (MassTransit) — specifically LLM usage events and MCP tool invocation events — and records them via a metrics storage layer, supporting both MariaDB and other persistence providers with database migration support.

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines the DTOs, message types, request/response models, and domain entities used across the CodeGraph repository. It serves as the central schema layer consumed by multiple services including indexers, API hosts, memory services, and metrics consumers, enabling decoupled communication via message bus contracts and typed API boundaries.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform, a code intelligence and developer tooling system. It provides services for indexing and analyzing source code repositories into a graph database, running LLM-based code analysis and reviews via multiple AI providers (OpenAI, Anthropic, LM Studio), managing a conversational AI assistant, maintaining a semantic memory store, and exposing MCP (Model Context Protocol) tools for AI agents. The project serves as the integration hub between the data layer (IGraphStore, IMemoryGraphStore), external LLM APIs, repository providers (GitHub, GitLab, local folders), and higher-level API/host projects.

### CodeGraph.Tests

CodeGraph.Tests is the test project for the CodeGraph repository, containing unit and integration tests for all major subsystems including code extractors, API controllers, data stores, services, memory operations, and host startup validation. It provides a comprehensive suite of in-memory fakes, recording stubs, and throwing test doubles that implement the production interfaces, enabling isolated unit tests without real database or HTTP dependencies. The project covers extractors (Roslyn, SQL, Terraform, Ansible, ColdFusion), MariaDB store round-trips, LLM configuration resolution, review workflows, indexer operations, MCP telemetry, and memory graph services.

### reflect

The 'reflect' project appears to be a reflection or introspection utility module within the CodeGraph repository. Based on the name and typical patterns for such modules, it likely provides mechanisms to inspect, traverse, or analyze code structure at runtime or compile time. However, due to the absence of actual graph node data, class definitions, and source snippets in the provided input, a precise technical description cannot be fully established.

