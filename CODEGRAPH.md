# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-10 13:29 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-project .NET/Angular monorepo that implements a code analysis and knowledge graph platform. Its core purpose is to index software repositories across multiple languages (C#, TypeScript, SQL, C/C++), extract structural information (classes, methods, interfaces, dependencies, call graphs, DI registrations, message bus consumers/publishers, HTTP routes), store that information in a Neo4j graph database, and expose it through a REST API and Angular web UI with AI-assisted review, wiki, and Q&A capabilities.

The repository is organized into distinct layers that collaborate in a pipeline:

1. **Extraction layer**: Language-specific extractors (CodeGraph.Extractors.CSharp via Roslyn, CodeGraph.Extractors.TypeScript via a long-running IPC server, CodeGraph.Extractors.Sql via T-SQL parsing, CodeGraph.Extractors.TreeSitter via Tree-sitter AST walking) all implement the ICodeExtractor interface and produce GraphNode/PendingEdge/ExtractionResult structures consumed by the service layer.

2. **Data contract layer**: CodeGraph.Data defines the full set of storage interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IWikiStore, IVectorStore, IMemoryGraphStore, IJobScheduleStore, etc.) and plain entity/DTO types, with no implementation. CodeGraph.Data.Neo4j provides all concrete Neo4j-backed implementations via Cypher queries.

3. **Business logic layer**: CodeGraph.Services orchestrates the full pipeline — repository discovery, multi-language extraction, cross-repo linking, community/cluster detection, impact analysis, batch LLM analysis with multiple AI provider integrations, security scanning, wiki generation, and memory graph management. It sits between the storage interfaces and the API/job layers.

4. **API layer**: CodeGraph.Api is an ASP.NET Core Web API that exposes REST endpoints for graph queries, code node browsing, cluster/community data, wiki content, AI-assisted Q&A, memory entities, and review workflows. It also hosts MassTransit-style message bus consumers that react to async events (repository indexing completion, batch analysis results, memory storage).

5. **Job execution layer**: CodeGraph.Jobs is a separate ASP.NET Core service providing HTTP-triggered and cron-scheduled background jobs (repository discovery, processing, batch result handling, MCP doc regeneration, community detection). It uses a dispatcher pattern to route named job types to implementations and persists schedules.

6. **Shared contracts**: CodeGraph.Models defines message bus event types, HTTP request/response models, memory graph entities, and domain exceptions used across all other projects.

7. **Test infrastructure**: CodeGraph.Tests and CodeGraph.Jobs.Tests provide xUnit-based unit and integration tests backed by comprehensive in-memory fakes/stubs for all storage interfaces, AI providers, and external services, enabling testing without real databases or external dependencies.

The system has no identified external repository dependencies within this analysis scope. It depends on Neo4j as its primary data store, Roslyn for C# AST analysis, Tree-sitter for C/C++ parsing, one or more LLM providers for AI features, and MassTransit (or a compatible abstraction) for async messaging. The Angular frontend and a TypeScript analysis server process are also part of the repository. Overall confidence is high given the comprehensive, consistent evidence across all sub-project analyses.

## Projects

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit/integration test project for the CodeGraph.Jobs library. It provides test doubles (in-memory stores, recording service fakes), test-specific job subclasses, and xUnit-style test classes that verify the behavior of job scheduling, dispatch, and execution logic including DiscoverRepositoriesJob, ProcessRepositoriesJob, ProcessBatchResultsJob, and the JobCommandDispatcher and JobScheduleService components.

### CodeGraph.Tests

CodeGraph.Tests is the test project for the CodeGraph repository, containing unit and integration tests that verify the behavior of extractors, services, data stores, API controllers, and analysis pipelines. It provides a comprehensive set of in-memory test doubles (fake/stub/recording implementations of data stores, service interfaces, and external providers) that allow tests to run without real databases, AI providers, or external HTTP services. The project covers functionality including C# Roslyn extraction, SQL extraction, cross-repo linking, security analysis, batch AI analysis workflows, repository review services, wiki management, and API controller behavior.

### CodeGraph

CodeGraph is a code analysis and visualization platform that indexes software repositories, extracts structural information (classes, dependencies, call graphs), and exposes this data through a web UI and API. The repository contains a .NET backend for indexing and analysis pipelines, an Angular frontend for browsing code graphs, clusters, wiki, and AI-assisted review features, and TypeScript tooling for extracting project structure from TypeScript/JavaScript codebases.

### CodeGraph.Data

CodeGraph.Data defines the data access layer contracts and entity models for the CodeGraph repository. It provides a comprehensive set of interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IReviewStore, IWikiStore, IVectorStore, IMemoryGraphStore, IJobScheduleStore, IExclusionStore, IDbHealthStore, IMigrationRunner) that abstract storage operations for code graph nodes, edges, analysis results, metrics, reviews, wiki content, vector embeddings, and job scheduling. The project contains only interface definitions and plain entity/DTO classes — concrete implementations (Neo4j-backed) and test in-memory implementations live in separate projects within the repository.

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API that serves as the primary backend for a code analysis and knowledge graph platform. It exposes REST endpoints for querying repository/project graphs, code nodes, clusters, wiki content, memory entities, AI-assisted Q&A, and review workflows, while also hosting message bus consumers that react to async events such as repository indexing, batch analysis, and memory storage. The project orchestrates multiple domain services (graph analysis, community detection, security scanning, impact analysis, reviews) and integrates with a message broker via MassTransit-style consumers.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic information from C# source files and solutions. It walks syntax trees to identify classes, methods, interfaces, routes, DI registrations, message bus consumers/publishers, and cross-service HTTP calls, producing structured ExtractionResult models. It acts as the C#-specific extraction backend within a larger CodeGraph repository that maps codebases into graph representations.

### CodeGraph.Jobs

CodeGraph.Jobs is an ASP.NET Core web service that manages and executes background jobs for the CodeGraph repository analysis platform. It provides HTTP endpoints for triggering jobs (repository discovery, processing, and batch result handling), a cron-based job scheduling system with persistence, and a dispatcher pattern for routing named job types to their implementations. The project acts as the job execution backbone of the broader CodeGraph system, coordinating repository indexing, community detection, MCP doc regeneration, and batch analysis workflows.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files in multiple languages (at minimum C and C++). It implements the ICodeExtractor interface to parse source code into an AST, then walks that AST to extract definitions (classes, functions, etc.) and import/dependency relationships, producing GraphNode and PendingEdge structures that feed into the broader CodeGraph knowledge graph pipeline.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph nodes/edges, analysis batches, vector embeddings, wiki pages, job schedules, memory graphs, etc.) by translating between the application's domain models and Neo4j's Cypher query language and driver API. This project serves as the single integration point between the rest of the application and a Neo4j graph database, handling everything from code graph CRUD operations to AI analysis tracking, security findings, health metrics, and review run management.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information from T-SQL constructs such as tables, views, stored procedures, functions, and indexes. It implements the ICodeExtractor interface to integrate with the broader CodeGraph repository's extraction pipeline, producing an ExtractionResult that represents SQL database object definitions and their relationships.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a long-running TypeScript analysis server process, delegates extraction and linting work to it via IPC, and maps the results into the shared CodeGraph domain model (GraphNode, PendingEdge, ExtractionResult). It serves as the TypeScript-specific backend for the broader CodeGraph repository analysis pipeline.

### CodeGraph.Models

CodeGraph.Models is a shared models/contracts library that defines the data structures, message types, request/response objects, and domain entities used across the CodeGraph system. It serves as the canonical contract layer consumed by API consumers, services, and other components within the repository, encompassing message bus event types, memory graph entities, HTTP request/response models, and exception types.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph system — a platform that indexes source code repositories into a knowledge graph, analyzes them using multiple LLM providers, and exposes graph query, review, security scanning, and AI assistant capabilities. It implements the full pipeline from file discovery and extraction through cross-repo linking, community detection, impact analysis, batch LLM analysis, and wiki/memory management, serving as the central orchestration hub between data storage (CodeGraph.Data), language extractors (CodeGraph.Extractors), and API/job layers.

