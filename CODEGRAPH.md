# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-29 18:09 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that ingests source repositories, parses them across multiple languages, stores the resulting structural data as a graph, and exposes querying, analysis, and AI-assisted review capabilities through REST APIs and a web UI.

The repository is organized into distinct layers that work together in a pipeline:

1. **Extraction layer** – Language-specific extractor plugins (C#/Roslyn, TypeScript/Node.js server, SQL/T-SQL, ColdFusion, Ansible YAML, Terraform HCL, and a generic Tree-sitter backend) each implement a shared ICodeExtractor interface and produce GraphNode/edge models representing code structure and cross-file relationships.

2. **Indexing pipeline** – CodeGraph.Indexer.Host is an ASP.NET Core service that orchestrates repository discovery, extraction, graph linking, community detection, and batch analysis. It exposes REST endpoints and listens to a MassTransit message bus for lifecycle events. CodeGraph.Indexer.Client provides a typed HTTP client for other services to trigger and monitor pipeline stages. CodeGraph.Jobs adds cron-based scheduling on top, dispatching typed job commands (index, detect communities, regenerate docs, etc.) via a polling worker.

3. **Storage layer** – CodeGraph.Data defines the full set of async repository interfaces (graph, analysis, review, metrics, wiki, memory, vector search, admin, LLM config, job schedules). Two concrete backends implement these interfaces: CodeGraph.Data.Neo4j stores graph nodes, edges, AI analysis batches, memory entities, wiki content, and vector embeddings in a Neo4j database via Cypher; CodeGraph.Data.MariaDb stores relational data (metrics, configuration, assistant runs, migrations) using Entity Framework Core and MySqlConnector, including AES encryption for sensitive values.

4. **Service/business-logic layer** – CodeGraph.Services contains the core logic: repository indexing orchestration, cross-repo dependency linking, LLM-powered code review and analysis, semantic search via ONNX embeddings, a structured memory/knowledge graph for AI context, wiki management, MCP (Model Context Protocol) tool exposure, and vitals/health computation.

5. **API host** – CodeGraph.Api is the primary ASP.NET Core Web API, exposing controllers for graph queries, repository indexing, AI-assisted review, memory/knowledge-graph operations, wiki, search, admin management, and MCP token access. It also hosts MassTransit consumers reacting to indexing events.

6. **Memory subsystem** – CodeGraph.Memory.Host is a separate ASP.NET Core microservice exposing a knowledge-graph memory store (entities, claims, relationships, subgraph retrieval, supersession chains) via REST and message bus. CodeGraph.Memory.Client provides a typed HTTP client for consuming it.

7. **Metrics service** – CodeGraph.Metrics is a small ASP.NET Core microservice that consumes MassTransit messages for LLM usage and MCP tool invocations and persists them to MariaDB.

8. **Shared infrastructure** – CodeGraph.Models is the central contracts/DTOs library shared across all services. CodeGraph.Host.Shared provides HMAC/JWT service-to-service authentication middleware and OpenTelemetry activity source constants.

9. **Frontend** – CodeGraphWeb is an Angular application providing a rich UI for exploring code graphs, clusters, memory entities, wiki pages, admin operations, and AI assistant interactions.

**External dependencies observed:** Neo4j (graph database), MariaDB/MySQL (relational storage), MassTransit (message bus abstraction, likely over RabbitMQ or similar), Roslyn (.NET compiler platform for C# parsing), Tree-sitter (multi-language parsing), Node.js subprocess (TypeScript analysis server), ONNX Runtime (semantic embeddings), and one or more LLM providers (for code review and assistant features).

**Cross-repository relationships:** The CodeGraph API is called by an external 'CodeFlow' system (multiple HTTP call edges observed). CodeGraph itself makes HTTP calls to its own API (TC.CodeGraphApi). This suggests CodeGraph is a backend service consumed by at least one other application (CodeFlow) for code intelligence features.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack application for analyzing, visualizing, and querying code repositories as graph structures. The repository contains a .NET backend (services, jobs, data layer) that indexes source code into a graph database, runs LLM-powered analysis and reviews, and exposes APIs; alongside an Angular frontend (CodeGraphWeb) that provides a rich UI for exploring code graphs, clusters, memory entities, wiki pages, admin operations, and AI assistant interactions. There is also a TypeScript extraction tool that parses TypeScript projects into graph node/edge representations.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API host for the CodeGraph platform — a code intelligence system that indexes source repositories, analyzes them with LLMs, and exposes the results through a rich REST API. It wires together controllers covering graph queries, repository indexing, AI-assisted code review, a memory/knowledge-graph subsystem, wiki, search, admin management, and MCP (Model Context Protocol) token access, while also hosting MassTransit message consumers that react to indexing lifecycle events published by other services in the same repository.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all storage interfaces (repository contracts) and entity/model classes used across the system. It provides a comprehensive set of async interfaces for graph storage, analysis, review, metrics, wiki, assistant runs, LLM configuration, memory, vector search, job scheduling, and administrative operations, with implementations in Neo4j and MariaDb backends living in sibling projects.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph platform. It provides concrete implementations of all data store interfaces (graph, analysis, metrics, review, memory, vector, wiki, admin, assistant, etc.) using Entity Framework Core and raw Dapper-style SQL via MySqlConnector. It also manages database schema migrations, AES encryption for sensitive configuration values, and dependency injection wiring for all MariaDB-backed stores.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces covering code graph nodes and edges, AI analysis batches, memory graph (entity/claim knowledge graph), wiki content, job scheduling, vector embeddings, and repository metadata — all persisted to a Neo4j graph database via Cypher queries. It serves as the concrete infrastructure implementation for all Neo4j-related storage in the repository.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a code analysis plugin that parses Ansible YAML files (playbooks, roles, tasks, handlers, variables, requirements, and role metadata) and extracts a graph representation of their structure and relationships. It implements the ICodeExtractor interface to integrate with the broader CodeGraph system, producing GraphNode objects and cross-reference edges that represent Ansible constructs and their dependencies within and across repositories.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a specialized code analysis plugin that parses ColdFusion (.cfm/.cfc) source files to extract structural metadata such as components, functions, SQL queries, HTTP calls, file includes, and object invocations. It implements the ICodeExtractor interface, integrating into the broader CodeGraph repository's extraction pipeline to produce ExtractionResult models representing the code graph of ColdFusion codebases.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic information from C# source files and solutions. It parses syntax trees to identify classes, methods, interfaces, routes, DI registrations, service bus events, HTTP client calls, NuGet dependencies, and other code relationships, producing structured ExtractionResult models consumed by the broader CodeGraph system. It implements extractor service interfaces defined elsewhere in the repository, acting as the C#-specific backend for a code graph generation pipeline.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis component that parses T-SQL source files and extracts structural graph metadata from them. It identifies SQL objects such as tables, views, stored procedures, functions, and indexes, along with their relationships (e.g., table references and execute calls), and produces an ExtractionResult for use in a broader code graph system. It implements the shared ICodeExtractor interface, fitting into a multi-language extraction pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code graph extraction plugin that parses Terraform HCL (HashiCorp Configuration Language) files and converts their structure into a graph representation. It identifies Terraform constructs such as resources, data sources, modules, variables, outputs, locals, and tfvars assignments, extracts their properties and inter-dependencies, and produces graph nodes and edges that integrate into the broader CodeGraph repository analysis system.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction project that uses the Tree-sitter parsing library to analyze source files across multiple programming languages. It parses source files into syntax trees and extracts structural information such as symbol definitions (classes, functions, etc.) and import/dependency relationships, producing graph nodes and edges for consumption by the broader CodeGraph system. It implements the ICodeExtractor interface, making it a pluggable extractor backend that supports language-specific configurations via LanguageSpec registrations.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based TypeScript analysis server process, communicates with it to extract structural information and lint results, and transforms the responses into graph nodes and edges used by the broader CodeGraph system. It implements service interfaces defined elsewhere in the repository, acting as the TypeScript-specific backend for code extraction and analysis.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library providing common hosting utilities and internal service authentication mechanisms for services within the CodeGraph repository. It implements a token-based authentication system (likely HMAC-signed JWT or custom tokens) for service-to-service communication, along with DI registration extensions and OpenTelemetry activity source constants for use across multiple host projects.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP client for communicating with a CodeGraph Indexer service. It exposes operations for triggering and monitoring indexing pipeline stages (discovery, processing, linking, community detection, schema synchronization) and querying run history, and includes DI registration extensions for integration into ASP.NET Core or similar host applications.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is the ASP.NET Core web host and message-processing entry point for the CodeGraph indexing pipeline. It exposes REST API endpoints for triggering indexing operations (repository processing, schema sync, community detection, batch analysis), registers MassTransit consumers that react to domain events propagated over a message bus, and orchestrates downstream services such as graph storage, cross-repo linking, community detection, and batch analysis. It acts as the integration layer between external HTTP clients, the message bus, and the internal indexing/analysis service layer.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution engine within the CodeGraph repository. It defines a set of typed job commands (e.g., repository indexing, community detection, documentation regeneration), a dispatcher that routes job-type strings to their concrete implementations, a scheduling service that manages cron-based job schedules persisted to a database, and a hosted worker that polls for due schedules and executes them. The project serves as the asynchronous task infrastructure layer for the broader CodeGraph system.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a unit test project for the CodeGraph.Jobs scheduling and job-dispatch system. It contains in-memory fakes and recording test doubles for external service interfaces (IJobScheduleStore, IAdminService, IBatchAnalysisService, IIndexerClient, etc.) along with test classes that verify the behaviour of job scheduling, command dispatching, and specific job implementations such as discover-repositories and process-batch-analysis jobs.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is a .NET client library that provides typed HTTP access to a memory graph service. It exposes an interface and concrete implementation for querying, mutating, and managing a graph-based memory store (entities, claims, relationships, subgraphs, diagnostics, and write operations) over HTTP, and includes DI registration helpers for integration into ASP.NET Core applications.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web service that exposes a graph-based memory system via REST API and message bus consumers. It provides endpoints for storing, querying, searching, and managing memory claims and entities organized as a knowledge graph, with support for operations like frontier expansion, subgraph retrieval, supersession chains, and cleanup. The project acts as the hosting layer for a memory service, wiring together persistence, MassTransit message consumers, and HTTP controllers.

### CodeGraph.MemoryMigration

No graph nodes or source code were provided for the CodeGraph.MemoryMigration project, making it impossible to determine its specific responsibilities with confidence. Based on the project name alone, it likely handles in-memory data migration or schema/model migration tasks within the CodeGraph repository, but this is speculative without supporting evidence.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core microservice responsible for collecting and persisting metrics events within the CodeGraph system. It consumes messages from a message bus (MassTransit) — specifically LLM usage and MCP tool invocation events — and records them via a metrics event recorder, likely backed by a relational database (with MariaDB support indicated). The service includes infrastructure setup for persistence, authentication middleware for internal service communication, and database migration support.

### CodeGraph.Models

CodeGraph.Models is a shared models/contracts library that defines the data transfer objects, message contracts, request/response types, and domain entities used across the CodeGraph repository. It serves as the central type-sharing project, providing message classes consumed by indexer, API, memory, and metrics services, as well as memory graph structures, analysis-related entities, and exception types.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph repository — a code intelligence platform that indexes source repositories into a graph database, analyzes code quality and security, integrates with multiple LLM providers for AI-powered analysis and an interactive assistant, and exposes MCP (Model Context Protocol) tools for external AI agent consumption. It provides service implementations for repository indexing pipelines, cross-repo dependency linking, vitals/health metrics computation, project and repository code reviews, semantic search via ONNX embeddings, a structured memory graph for AI context, wiki management, and administrative reporting.

### CodeGraph.Tests

CodeGraph.Tests is the unified test project for the CodeGraph repository, providing unit and integration tests across all subsystems including API controllers, data stores, services, extractors, memory operations, metrics, indexer, and host startup. It supplies a rich library of in-memory fakes, recording stubs, and no-op implementations of every major interface so that service-level logic can be exercised without real databases, HTTP endpoints, or LLM providers. The project also contains integration-style tests that spin up real MariaDB or Git repositories to verify persistence contracts and pipeline behavior end-to-end.

### reflect

The 'reflect' project appears to be a minimal or empty project within the CodeGraph repository, as no class/interface nodes were provided in the graph. Based on the project name 'reflect', it likely provides reflection or introspection utilities, but without any nodes or source code to analyze, this cannot be confirmed.

