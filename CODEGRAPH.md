# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-08 12:19 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-project .NET/Angular platform for automated code intelligence: it discovers, parses, and indexes software repositories into a Neo4j graph database, enriches the graph with AI-powered analysis, and exposes the results through a REST API and Angular SPA.

Architectural layers and how they fit together:

1. **Shared contracts** — `CodeGraph.Models` defines cross-cutting DTOs (messages, requests/responses, memory graph entities). `CodeGraph.Data` declares all storage and service interfaces (IGraphStore, IWikiStore, IExclusionService, etc.) without any implementation, acting as the dependency-inversion boundary between business logic and persistence.

2. **Persistence** — `CodeGraph.Data.Neo4j` is the sole concrete implementation of those interfaces. It translates Cypher queries against a Neo4j database into typed C# entities, covering node/edge storage, vector embeddings, wiki content, job schedules, metrics, and AI batch tracking.

3. **Extraction pipeline** — Four language-specific extractor projects parse source code and emit `ExtractionResult` graphs (nodes + edges) into the shared model:
   - `CodeGraph.Extractors.CSharp` uses Roslyn to analyse .NET solutions, identifying classes, methods, DI registrations, routes, service-bus consumers, and cross-service HTTP calls.
   - `CodeGraph.Extractors.TypeScript` manages a long-running TypeScript analysis server subprocess and transforms its AST output into graph nodes/edges.
   - `CodeGraph.Extractors.TreeSitter` uses the Tree-sitter library for C/C++ (and potentially other languages).
   - `CodeGraph.Extractors.Sql` parses T-SQL to catalogue tables, views, stored procedures, functions, and indexes.

4. **Business logic / orchestration** — `CodeGraph.Services` is the core domain layer. It orchestrates repository discovery (GitHub, GitLab, local folders), drives the extraction pipeline, indexes results into Neo4j, runs LLM-powered batch analysis and health scoring, performs semantic search via ONNX embeddings, links cross-repo dependencies, runs community detection, and manages a wiki system.

5. **API host** — `CodeGraph.Api` is an ASP.NET Core service that exposes REST endpoints (projects, nodes, clusters, memory, search, wiki, settings) and hosts MassTransit-style message consumers that react to asynchronous pipeline events (repository indexing, batch analysis, memory storage).

6. **Job scheduling** — `CodeGraph.Jobs` is a separate ASP.NET Core service providing cron-based scheduling and HTTP-triggered background jobs (discover repositories, process repositories, process batch results). A dispatcher layer routes named job types to their implementations.

7. **Testing** — `CodeGraph.Tests` and `CodeGraph.Jobs.Tests` provide unit and integration tests using in-memory stub implementations of all major interfaces as test doubles, covering extractors, services, job scheduling, and dispatching.

Key external dependencies observed: Neo4j (graph database), Roslyn (C# analysis), Tree-sitter (multi-language parsing), ONNX Runtime (semantic embeddings), MassTransit (messaging), Angular (frontend SPA), and LLM APIs (batch analysis/health scoring). Nothing external depends on this repository as a library — it is a self-contained application platform.

## Projects

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project for the CodeGraph.Jobs library, providing unit tests and test infrastructure for job scheduling, dispatching, and execution components. It contains test doubles (in-memory stores, recording services, testable job subclasses) and xUnit-style test classes that verify the behavior of job types such as DiscoverRepositoriesJob, ProcessRepositoriesJob, ProcessBatchResultsJob, and the JobCommandDispatcher and JobScheduleService.

### CodeGraph.Tests

CodeGraph.Tests is the automated test project for the CodeGraph repository, providing unit and integration tests for code graph extraction, analysis, indexing, and service logic. It contains in-memory stub implementations of all major data store and service interfaces (IGraphStore, IWikiStore, IExclusionService, etc.) used as test doubles, alongside test classes covering extractors (Roslyn, SQL, TreeSitter, NuGet, cross-repo linking), batch analysis workflows, security scanning, repo provider integrations (GitHub, GitLab, folder-based), and various service-layer behaviors.

### CodeGraph

CodeGraph is a code analysis and visualization platform that indexes software repositories, builds graph-based representations of code structure (nodes, edges, clusters, dependencies), and provides an Angular web frontend plus backend services for exploring, querying, and documenting codebases. It includes a TypeScript extractor tool, an indexing pipeline for .NET solutions and NuGet references, background jobs for batch analysis and community detection, and a wiki/documentation system, all tied together through a REST API consumed by the Angular SPA.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining the storage abstractions (interfaces) and entity/DTO classes used throughout the system. It declares contracts for persisting and querying graph nodes/edges, code analysis results, repository metadata, file metrics, security findings, wiki content, job schedules, vector embeddings, and memory graph data — all implemented externally (primarily by a Neo4j backend). This project acts as the shared data contract layer between the domain services and the database implementations.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core web API and message-consumer host for the CodeGraph system. It exposes REST endpoints for querying and managing a code-knowledge graph (projects, nodes, clusters, memory, search, wiki, settings) and integrates with a message bus (MassTransit-style consumers) to react to asynchronous pipeline events such as repository indexing, batch analysis, and memory storage.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic metadata from C# source files and solutions. It walks syntax trees to identify classes, methods, interfaces, routes, dependency injection registrations, messaging patterns (service bus, consumers), and cross-service HTTP calls, producing a structured ExtractionResult graph. It serves as the C#-specific extraction backend within the larger CodeGraph repository, implementing shared extractor service interfaces.

### CodeGraph.Jobs

CodeGraph.Jobs is an ASP.NET Core web service that manages and executes background jobs for analyzing code repositories. It provides HTTP endpoints for triggering jobs (discover repositories, process repositories, process batch results), a cron-based scheduling system for automated job execution, and a dispatcher layer that routes named job types to their corresponding implementations. The project serves as the job orchestration backbone of the CodeGraph repository analysis platform.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction project that uses the Tree-sitter parsing library to analyze source files across multiple programming languages (at minimum C and C++). It parses source code into syntax trees, then walks those trees to extract code definitions (classes, functions, etc.) and import/dependency relationships, producing structured graph nodes and edges for use in the CodeGraph knowledge graph system.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph, vector, memory, wiki, job scheduling, metrics, analysis, exclusions) by translating Cypher queries against a Neo4j database into typed C# entities. This project serves as the sole data access layer between the rest of the CodeGraph system and its Neo4j graph database, handling everything from code node/edge storage and traversal to AI analysis batch tracking, vector embeddings, and wiki content management.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis project that parses SQL files and extracts structural graph information from them. It processes T-SQL syntax to identify and catalog database objects such as tables, views, stored procedures, functions, and indexes, producing an ExtractionResult that feeds into the broader CodeGraph repository's code analysis pipeline. It implements the ICodeExtractor interface, making it a pluggable SQL-specific extractor within the system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a .NET project responsible for extracting code graph data from TypeScript source files and projects. It manages a long-running TypeScript analysis server process, communicates with it to extract AST/structural information and run linting, and transforms the results into the repository's shared graph model (GraphNode, PendingEdge, ExtractionResult). It serves as the TypeScript-specific extraction layer within a broader code graph construction pipeline.

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines the message types, request/response objects, memory graph entities, and exception types used across the CodeGraph system. It serves as the central contract project consumed by API consumers, workers, and other components, enabling loose coupling through well-defined DTOs for messaging, HTTP requests, and graph memory operations.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a multi-repository code intelligence system that indexes source code into a knowledge graph, analyzes it with LLMs, and exposes querying, assistant, and MCP server capabilities. It orchestrates repository discovery (GitHub, GitLab, local folders), code extraction and graph indexing pipelines, AI-powered batch analysis and health scoring, semantic search via ONNX embeddings, cross-repo dependency linking, community detection, and a wiki system, all wired together through well-defined service interfaces.

