# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-09 13:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a multi-language source code analysis and visualization platform that parses repositories, builds a graph of code relationships, and exposes query, analysis, and assistant capabilities through a web API and Angular UI.

The system is organized into distinct layers:

**Extraction layer**: Language-specific extractors parse source code into structured graph data. CodeGraph.Extractors.CSharp uses Roslyn to extract classes, methods, interfaces, DI registrations, routes, HTTP clients, and messaging relationships from C# solutions. CodeGraph.Extractors.TypeScript bridges a Node.js analysis server to extract TypeScript project structure. CodeGraph.Extractors.TreeSitter uses the Tree-sitter library for additional language support (C, C++, and others). CodeGraph.Extractors.Sql parses T-SQL to extract database objects (tables, views, stored procedures, functions, indexes). All extractors implement a shared ICodeExtractor interface.

**Data/persistence layer**: CodeGraph.Data defines technology-agnostic storage interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IVectorStore, IWikiStore, IMemoryGraphStore, IJobScheduleStore, etc.) and the entity/DTO types used across the system. CodeGraph.Data.Neo4j implements all of these interfaces using Cypher queries against a Neo4j graph database, acting as the sole concrete storage backend.

**Models layer**: CodeGraph.Models is a shared contract library defining message types, request/response objects, memory graph structures, and exceptions for loosely coupled inter-component communication.

**Services layer**: CodeGraph.Services contains the core business logic — repository indexing, cross-repo dependency linking, LLM-based batch analysis (OpenAI, Anthropic, Gemini, local models), security scanning, code health/vitals analysis, semantic vector search, wiki management, a graph-aware AI assistant, and Model Context Protocol (MCP) server endpoint generation.

**API layer**: CodeGraph.Api is an ASP.NET Core Web API exposing REST endpoints for querying repositories, nodes, clusters, memory graphs, wiki, and settings. It also hosts message bus consumers that react asynchronously to indexing, batch analysis, and memory storage events, coordinating community detection, impact analysis, and documentation pipelines.

**Jobs layer**: CodeGraph.Jobs is a separate ASP.NET Core application that manages background and scheduled (cron-based) job execution — repository discovery, batch processing, community detection, MCP documentation regeneration — via a dispatcher that maps job names to implementations.

**Test projects**: CodeGraph.Tests and CodeGraph.Jobs.Tests provide unit and integration tests using in-memory stores, recording fakes, and stub implementations as test doubles, covering services, extractors, data stores, analysis pipelines, and job scheduling logic.

The platform depends on Neo4j as its graph database, Roslyn and Tree-sitter for code parsing, a message bus for async event dispatch, multiple LLM providers for AI analysis, and a Node.js TypeScript analysis server. The Angular frontend (embedded in the CodeGraph project) consumes the API. No external systems are known to depend on this repository.

## Projects

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project for the CodeGraph.Jobs library, providing unit tests for job scheduling, dispatching, and execution logic. It includes test doubles (in-memory stores, recording fakes) and concrete test subclasses of production jobs to verify behavior in isolation without real infrastructure dependencies.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository. It provides unit and integration tests for services, extractors, data stores, and analysis pipelines. The project contains test doubles (in-memory stores, recording/stub implementations, fake providers) that stand in for real dependencies, enabling isolated testing of graph indexing, code analysis, security scanning, batch AI analysis, wiki management, and repository provider logic.

### CodeGraph

CodeGraph is a repository analysis and visualization platform that indexes source code repositories (C#/.NET and TypeScript), builds a graph of code relationships (nodes, edges, dependencies, clusters), and exposes this graph through a web UI and API. It includes backend services for extracting and analyzing code, background jobs for processing, and an Angular frontend for browsing projects, nodes, clusters, impact analysis, security findings, wiki documentation, and an AI chat assistant.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository. It defines the storage abstraction interfaces (IGraphStore, IAnalysisStore, IMetricsStore, IVectorStore, IWikiStore, IMemoryGraphStore, IJobScheduleStore, etc.) and the entity/DTO classes used to persist and retrieve code graph data, analysis results, metrics, wiki content, and job scheduling state. This project sits between the domain/service layer and concrete storage backends (e.g., Neo4j), providing a technology-agnostic contract for all persistence operations.

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API that serves as the central orchestration and query layer for a code graph analysis platform. It exposes REST endpoints for querying code repositories, nodes, clusters, memory, wiki, and settings, while also hosting message consumers that react to asynchronous events (repository indexing, batch analysis, memory storage) dispatched via a message bus. The project coordinates multiple downstream services (graph store, analysis pipelines, community detection, impact analysis, wiki, MCP documentation) and acts as the primary backend for a UI or other clients.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses Roslyn (the .NET compiler platform) to extract structural and semantic metadata from C# source files and Visual Studio solutions. It walks syntax trees to identify classes, methods, interfaces, routes, DI registrations, messaging consumers/publishers, HTTP client calls, and other architectural relationships, producing structured ExtractionResult models for use by the broader CodeGraph system.

### CodeGraph.Jobs

CodeGraph.Jobs is an ASP.NET Core web application that manages and executes background jobs related to code graph analysis. It provides HTTP API endpoints for triggering jobs (repository discovery, batch processing, etc.), a cron-based job scheduling system, and a dispatcher mechanism that maps named job types to their implementations. The project serves as the job execution backbone of the CodeGraph repository, coordinating operations like repository indexing, community detection, and MCP documentation regeneration.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction project that uses the Tree-sitter parsing library to analyze source files across multiple languages (including C, C++, and others). It parses source code into syntax trees, then walks those trees to extract structural information — definitions (classes, functions, etc.) and imports — which are emitted as graph nodes and edges for consumption by the broader CodeGraph system. It implements the ICodeExtractor interface, acting as a pluggable language-agnostic extraction backend driven by language-specific specifications.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j persistence layer for the CodeGraph repository. It provides concrete implementations of all data store interfaces (graph nodes/edges, analysis batches, vector embeddings, wiki pages, job schedules, memory graphs, etc.) by translating between the application's domain models and Cypher queries executed against a Neo4j graph database. This project acts as the sole infrastructure adapter between the application's data abstractions and the underlying graph database engine.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL-specific code analysis component that parses SQL files and extracts structural graph information from them. It processes T-SQL syntax to identify and model database objects such as tables, views, stored procedures, functions, and indexes, then returns an ExtractionResult for integration into the broader CodeGraph repository analysis pipeline. It implements the shared ICodeExtractor interface, making it a pluggable extractor alongside other language-specific extractors in the system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a project responsible for extracting code graph data from TypeScript source files and projects. It bridges a TypeScript analysis server (likely a Node.js process) with the broader CodeGraph system, handling file-level extraction, full project analysis via tsconfig, and linting. It implements shared extractor/analyzer interfaces defined in other projects within the repository.

### CodeGraph.Models

CodeGraph.Models is a shared data contract library that defines the message types, request/response objects, memory graph structures, and exception types used across the CodeGraph system. It serves as the canonical model layer consumed by API consumers, producers, and other projects in the repository, enabling loosely coupled communication via message bus patterns and standardized data structures for repository analysis and memory graph operations.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph system — a multi-repository code intelligence platform that indexes source code into a knowledge graph, analyzes it with LLMs, and exposes query/assistant capabilities. It provides services for repository indexing, cross-repo dependency linking, LLM-based batch analysis (via OpenAI, Anthropic, Gemini, or local models), code health/security/vitals analysis, semantic search, a wiki, a graph-aware AI assistant, and Model Context Protocol (MCP) server endpoints.

