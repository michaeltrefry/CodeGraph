# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-05-13 14:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a full-stack code intelligence platform that indexes source code repositories into graph databases and provides AI-assisted analysis, review, and querying capabilities. The repository is organized as a multi-project .NET solution with an Angular frontend (CodeGraphWeb) and a TypeScript extraction tool (ts-extractor).

The system works as follows: Language-specific extractor projects (CSharp, TypeScript, SQL, Terraform, Ansible, ColdFusion, TreeSitter) parse source files and produce graph nodes and edges representing code structure and dependencies. These extraction results are fed into an indexing pipeline (CodeGraph.Indexer.Host) that orchestrates multi-stage processing — including batch analysis, community detection, schema sync, and linking — communicating via MassTransit message consumers. Extracted graph data is persisted to both a Neo4j property graph database (CodeGraph.Data.Neo4j) and a MariaDB/MySQL relational database (CodeGraph.Data.MariaDb), with storage abstractions defined in CodeGraph.Data. The ASP.NET Core Web API (CodeGraph.Api) exposes REST endpoints for querying the code graph, triggering re-indexing, managing wiki documentation, running LLM-powered code reviews and chat (via OpenAI, Anthropic, and LM Studio), and administering the system. A dedicated memory service (CodeGraph.Memory.Host) maintains a graph-based knowledge store of entities and claims, accessible via a typed HTTP client (CodeGraph.Memory.Client). Background jobs (CodeGraph.Jobs) handle scheduled tasks such as repository discovery, indexing, community detection, and MCP doc regeneration via a cron-based worker service. Cross-cutting concerns — shared data models (CodeGraph.Models), authentication/HMAC token infrastructure (CodeGraph.Host.Shared), metrics collection (CodeGraph.Metrics), and business logic services (CodeGraph.Services) — are factored into dedicated projects consumed by the API and indexer hosts. The test project (CodeGraph.Tests) provides broad unit and integration coverage using in-memory fakes and recording stubs.

External dependencies include Neo4j (graph storage), MariaDB/MySQL (relational storage), MassTransit (messaging), OpenTelemetry (observability), Roslyn (C# AST analysis), Tree-sitter (multi-language parsing), Microsoft.SqlServer.TransactSql.ScriptDom (T-SQL parsing), and multiple LLM provider APIs. The repository itself is called by external systems: CodeFlow and codeflow-team make HTTP calls into CodeGraph, and CodeGraph in turn makes HTTP calls to an external TC.CodeGraphApi service, suggesting it operates within a larger ecosystem of related tooling.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/health, route_template=health, confidence_band=medium

## Dependents (these services call us)

- **CodeFlow**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium
- **codeflow-team**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/llm-providers/models, route_template=api/llm-providers/models, confidence_band=medium
  - HTTP_CALLS: http_method=PUT, url_pattern=/api/admin/llm-providers/{param}, route_template=api/admin/llm-providers/{provider}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/admin/llm-providers, route_template=api/admin/llm-providers, confidence_band=medium

## Projects

### CodeGraph

CodeGraph is a full-stack code intelligence platform consisting of a .NET backend and an Angular frontend (CodeGraphWeb). The system indexes source code repositories into graph databases, analyzes code structure and dependencies, provides AI-assisted code review and chat capabilities, manages wiki documentation, and exposes administrative tooling. The repository also includes a TypeScript extraction tool (ts-extractor) for parsing TypeScript projects into graph node/edge representations.

### CodeGraph.Api

CodeGraph.Api is the ASP.NET Core Web API layer of the CodeGraph system, exposing REST endpoints for code graph analysis, repository indexing, LLM-powered code review and assistant features, memory graph management, wiki, and administrative operations. It orchestrates messaging via MassTransit consumers that react to indexing and analysis lifecycle events, and delegates heavy operations to internal services while providing SSE-based streaming for long-running tasks. The project also handles authentication (JWT, local dev, and MCP personal access tokens), authorization policies, and application startup/configuration wiring.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining all storage abstractions (interfaces) and entity/DTO classes used throughout the system. It provides a comprehensive set of store interfaces covering graph nodes and edges, code analysis results, LLM configuration, assistant runs, wiki content, memory graphs, metrics, reviews, job scheduling, and more — with implementations delegated to MariaDb and Neo4j backends in sibling projects.

### CodeGraph.Data.MariaDb

CodeGraph.Data.MariaDb is the MariaDB/MySQL persistence layer for the CodeGraph platform. It provides concrete implementations of all data store interfaces using Entity Framework Core (via CodeGraphDbContext) and raw MySqlConnector queries, covering graph nodes and edges, code analysis results, LLM configuration, AI assistant runs, memory graphs, metrics, reviews, wiki, job scheduling, and migration execution. The project also handles database schema migrations, AES encryption of sensitive connection strings and tokens, and dependency injection registration.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces covering graph nodes and edges, analysis batches, AI-generated summaries, metrics, security findings, code reviews, wiki pages, job scheduling, memory graph operations, and vector embeddings — all stored in and retrieved from a Neo4j graph database. This project serves as the primary infrastructure adapter that translates between domain entities and Neo4j's property graph model via Cypher queries.

### CodeGraph.Extractors.Ansible

CodeGraph.Extractors.Ansible is a project responsible for parsing Ansible YAML files and extracting structured graph data (nodes and edges) from them. It classifies Ansible file types (playbooks, roles, variables, handlers, requirements, etc.), parses their contents into graph nodes and relationships, and produces an ExtractionResult consumed by the broader CodeGraph system for code/infrastructure graph analysis.

### CodeGraph.Extractors.ColdFusion

CodeGraph.Extractors.ColdFusion is a language-specific extraction plugin that parses ColdFusion (.cfm/.cfc) source files and produces a structured graph model of their contents. It identifies components, functions, SQL queries, HTTP calls, include directives, and object invocations within ColdFusion markup and CFScript syntax, and integrates into the broader CodeGraph extraction pipeline via the ICodeExtractor interface.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a C# code analysis project that uses the Roslyn compiler platform to extract structural and semantic metadata from C# solutions and source files. It walks syntax trees to identify classes, methods, interfaces, routes, DI registrations, HTTP client calls, service bus publishers/consumers, and other architectural relationships, producing a structured ExtractionResult graph. It serves as the C#-specific extraction engine within the broader CodeGraph repository, implementing shared extractor service interfaces.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis component that parses T-SQL source files and extracts structural graph information from them. It identifies and records SQL objects such as tables, views, stored procedures, functions, indexes, and cross-object references by traversing the parsed SQL AST. This project serves as a SQL-specific extractor plugin within the larger CodeGraph repository, implementing a shared ICodeExtractor interface to integrate with the broader code graph extraction pipeline.

### CodeGraph.Extractors.Terraform

CodeGraph.Extractors.Terraform is a code analysis plugin that parses HashiCorp Configuration Language (HCL) files used by Terraform infrastructure-as-code projects. It extracts structural elements such as resources, modules, data sources, variables, locals, and outputs into a graph representation, including cross-references and dependency edges between nodes. The project implements the ICodeExtractor interface, fitting into a larger CodeGraph repository that builds knowledge graphs from source code across multiple languages and formats.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction module that uses the Tree-sitter parsing library to analyze source files across multiple programming languages. It parses source code into syntax trees, then walks those trees to extract definitions (classes, functions, etc.) and import/dependency relationships, producing graph nodes and edges for the broader CodeGraph system. It implements the ICodeExtractor interface, making it a pluggable extractor component within the repository's extraction pipeline.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a .NET project responsible for extracting code graph data from TypeScript source files and projects. It manages a background Node.js server process to perform TypeScript-native analysis (including AST extraction and linting), communicates with it via request/response protocols, and translates the results into the shared CodeGraph model types (GraphNode, PendingEdge, ExtractionResult) consumed by the rest of the repository.

### CodeGraph.Host.Shared

CodeGraph.Host.Shared is a shared infrastructure library for the CodeGraph repository that provides cross-cutting hosting concerns, primarily focused on internal service-to-service authentication using HMAC-signed tokens. It supplies DI registration extensions, token creation/validation primitives, and OpenTelemetry activity source definitions that other host projects in the repository depend on.

### CodeGraph.Indexer.Client

CodeGraph.Indexer.Client is a .NET client library that provides a typed HTTP interface for communicating with a CodeGraph indexer service. It encapsulates all HTTP mechanics (request building, serialization, error handling, identity propagation) behind a clean interface, and exposes operations for managing indexer runs, triggering analysis pipelines (batch analysis, linking, community detection, schema sync, discovery, re-indexing, and repository processing). The project also provides a DI extension method for registering the client in ASP.NET Core applications via IServiceCollection.

### CodeGraph.Indexer.Host

CodeGraph.Indexer.Host is the ASP.NET Core web host and message-processing entry point for the CodeGraph indexing pipeline. It exposes HTTP API endpoints for triggering indexing operations (repository processing, schema sync, community detection, batch analysis), registers MassTransit message consumers that orchestrate the multi-stage indexing workflow, and wires together persistence, services, and infrastructure via a centralized Startup class.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job scheduling and execution project within the CodeGraph repository. It defines a set of named job types (repository discovery, indexing, community detection, MCP doc regeneration, etc.), a command-dispatcher pattern to route and execute those jobs by name, and a cron-based scheduling service that persists schedules, tracks next-run times, and fires jobs via a hosted worker service.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project for the job scheduling and dispatch infrastructure of the CodeGraph system. It contains unit tests for job scheduling services, command dispatchers, and related components, along with test doubles (recording/fake implementations) of external service interfaces used to verify behavior without real dependencies.

### CodeGraph.Memory.Client

CodeGraph.Memory.Client is an HTTP client library that provides typed access to a remote memory graph service. It exposes operations for querying, reading, writing, and managing a knowledge graph of entities and claims on behalf of named users. The project defines the client interface, its HTTP implementation, configuration options, and ASP.NET Core DI registration helpers.

### CodeGraph.Memory.Host

CodeGraph.Memory.Host is an ASP.NET Core web API host that exposes a knowledge graph memory service. It provides REST endpoints for storing, querying, searching, and managing memory claims and entities (a graph-based knowledge store), and integrates with a message bus (MassTransit) to consume asynchronous store-memory-claims messages. It handles persistence registration, service wiring, and includes health-check infrastructure for validating the write pipeline end-to-end.

### CodeGraph.MemoryMigration

CodeGraph.MemoryMigration appears to be a project within the CodeGraph repository focused on in-memory migration functionality. Based on the name, it likely handles migration of data structures or graph representations in memory, potentially serving as a migration layer for CodeGraph's core data model.

### CodeGraph.Metrics

CodeGraph.Metrics is an ASP.NET Core microservice responsible for collecting and persisting metrics events from other services in the CodeGraph system. It consumes messages from a message bus (MassTransit) — specifically LLM usage and MCP tool invocation events — and records them via a persistence layer, supporting both MariaDB and other storage backends with database migration support.

### CodeGraph.Models

CodeGraph.Models is a shared data models library that defines the DTOs, message contracts, request/response objects, and exception types used across the CodeGraph repository. It serves as the central contract layer consumed by multiple services including indexers, API consumers, memory hosts, and metrics collectors, ensuring consistent data structures for inter-service communication via messaging and HTTP.

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph platform — a system for indexing, analyzing, and querying code repositories as graphs. It provides services for extracting code structure into a graph database, running LLM-powered analysis and code reviews via multiple AI providers (OpenAI, Anthropic, LM Studio), managing an AI assistant with tool-use capabilities, maintaining a memory graph for persistent knowledge, and exposing administrative, query, wiki, and metrics functionality consumed by API and indexer host projects.

### CodeGraph.Tests

CodeGraph.Tests is the automated test project for the CodeGraph repository, containing unit and integration tests that verify the correctness of extractors, services, API controllers, data stores, and host startup logic across the entire codebase. It heavily relies on in-memory fake/recording/stub implementations of nearly every external interface (graph stores, LLM config repositories, indexer services, memory stores, wiki stores, etc.) to isolate units under test. The project spans test classes covering code extraction (Roslyn, SQL, Terraform, Ansible, ColdFusion), data layer round-trips (MariaDB stores), service behavior (review, analysis, security, memory, assistant), controller routing, and startup/DI registration validation.

### reflect

The 'reflect' project appears to be a minimal or empty project within the CodeGraph repository, as no class/interface nodes were provided in the graph. Based on the project name 'reflect', it likely provides reflection or introspection capabilities, but without any nodes or source code to analyze, the specific technical responsibilities cannot be determined.

