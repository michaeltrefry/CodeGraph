# CodeGraph

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-18 11:54 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

CodeGraph is a code intelligence platform that ingests software repositories, extracts structural and semantic graphs from their source code, persists those graphs in Neo4j, and exposes analysis, search, and AI-assisted review capabilities through a REST API and Angular frontend.

The repository is organized into distinct layers that collaborate through shared models and an asynchronous message bus:

1. **Extraction layer** — Four language-specific extractors implement a common `ICodeExtractor` interface: `CodeGraph.Extractors.CSharp` uses Roslyn to analyze .NET solutions (classes, methods, routes, DI registrations, NuGet refs, service-bus events); `CodeGraph.Extractors.TypeScript` shells out to a Node.js analysis server for TypeScript projects; `CodeGraph.Extractors.TreeSitter` handles C/C++ via the Tree-sitter parsing library; and `CodeGraph.Extractors.Sql` parses SQL scripts to extract tables, views, stored procedures, and indexes. All extractors produce `ExtractionResult` models consumed by the indexing pipeline.

2. **Data contracts layer** (`CodeGraph.Data`) — Defines store interfaces (`IGraphStore`, `IMemoryGraphStore`, `IVectorStore`, `IWikiStore`, `IJobStore`, etc.) and DTOs without committing to any backend. `CodeGraph.Data.Neo4j` is the sole concrete implementation, translating all domain operations into Cypher queries against a Neo4j database.

3. **Shared models** (`CodeGraph.Models`) — DTOs, message contracts, request/response objects, and domain entities used across all projects as the common type system.

4. **Services layer** (`CodeGraph.Services`) — Core business logic: indexing/extraction pipelines, multi-provider LLM analysis (OpenAI, Anthropic, Gemini, local), ONNX-based semantic embeddings/vector search, repository health vitals, security scanning, cross-repo dependency linking, AI-assisted code review, a memory/knowledge graph subsystem, wiki management, and an MCP (Model Context Protocol) server for LLM tool integration.

5. **Jobs layer** (`CodeGraph.Jobs`) — A background scheduling and dispatch system that runs named job types (repository discovery, batch analysis, community/cluster detection, cross-repo link detection, MCP doc regeneration, full re-indexing) on cron schedules via a hosted worker.

6. **API layer** (`CodeGraph.Api`) — An ASP.NET Core Web API that orchestrates everything: exposes REST endpoints for graph queries, project/cluster browsing, memory, wiki, semantic search, and AI code review; hosts MassTransit message consumers that react to async events (indexing completion, batch analysis progress, memory claims).

7. **Test projects** (`CodeGraph.Tests`, `CodeGraph.Jobs.Tests`) — Unit and integration tests using in-memory fakes and recording test doubles for all major subsystems including extractors, services, stores, controllers, and job scheduling.

External dependencies include Neo4j (graph persistence), ONNX Runtime (embedding inference), MassTransit (message bus), multiple LLM provider APIs, Tree-sitter native libraries, and Node.js (TypeScript extractor subprocess). No external systems are known to depend on this repository based on the available evidence.

## Projects

### CodeGraph.Services

CodeGraph.Services is the core business logic layer of the CodeGraph system — a platform for indexing, analyzing, and querying source code repositories as a graph structure. It provides services for code extraction and indexing pipelines, multi-provider LLM-based analysis (OpenAI, Anthropic, Gemini, local), repository health vitals computation, security scanning, semantic search via ONNX embeddings, cross-repository dependency linking, AI-assisted code review, a memory/knowledge graph subsystem, wiki management, and an MCP (Model Context Protocol) server for LLM tool integration.

### CodeGraph.Data

CodeGraph.Data is the data access layer for the CodeGraph repository, defining the contracts (interfaces) and data transfer objects (entity classes) for persisting and querying all application data. It provides a comprehensive set of store interfaces covering graph nodes/edges, cross-repo relationships, sync state, file hashes, analysis batches, metrics, reviews, wiki content, memory graphs, vector embeddings, job scheduling, and exclusion rules. The project is implementation-agnostic — concrete backends (e.g., Neo4j) live in sibling projects and implement these interfaces.

### CodeGraph.Models

CodeGraph.Models is a shared data model library that defines the DTOs, message contracts, request/response objects, and domain entities used across the CodeGraph system. It serves as the common type system for inter-service communication (message bus consumers), memory graph operations, API requests/responses, and exception handling throughout the repository.

### CodeGraph.Api

CodeGraph.Api is an ASP.NET Core Web API that serves as the central orchestration and HTTP interface layer for the CodeGraph system — a platform that indexes, analyzes, and queries source code repositories as a graph. It exposes REST endpoints for querying code graph nodes, projects, clusters, memory, wiki, search, and AI-assisted code review, while also hosting MassTransit message consumers that react to asynchronous events such as repository indexing completion, analysis batch progress, and memory claim storage.

### CodeGraph.Tests

CodeGraph.Tests is the automated test suite for the CodeGraph repository. It provides unit and integration tests covering extractors (Roslyn, SQL, TreeSitter, NuGet, cross-repo linker), services (indexing pipeline, batch analysis, review services, security analyzer, vitals analyzer, memory/retrieval, repo providers, exclusion rules, wiki seeding), API controllers, and data store implementations. The project makes heavy use of in-memory fake/stub/recording implementations of core interfaces (IGraphStore, IMemoryGraphStore, IAnalysisModelProvider, etc.) to isolate system-under-test components from external dependencies.

### CodeGraph

CodeGraph is a code intelligence platform that analyzes software repositories by extracting structural graphs of codebases (supporting .NET/C# and TypeScript), storing relationships in a graph database, and providing analysis features including impact analysis, cluster/community detection, security reviews, health diagnostics, memory/knowledge graphs, and a wiki. The repository contains a backend (.NET jobs and services), a TypeScript extractor tool, and an Angular frontend web application that visualizes graphs, search results, reviews, and AI-assisted chat over the codebase.

### CodeGraph.Jobs.Tests

CodeGraph.Jobs.Tests is a test project that validates the job scheduling and dispatching infrastructure of the CodeGraph system. It contains unit and integration tests for job command dispatching, job schedule management, and specific job types (discover repositories, process batch analysis), along with in-memory fakes and recording test doubles for external service dependencies.

### CodeGraph.Jobs

CodeGraph.Jobs is a background job execution and scheduling system within the CodeGraph repository. It defines a set of named job types (repository discovery, batch analysis, community detection, link detection, MCP doc regeneration, and full re-indexing), a dispatcher that routes job execution requests to the correct typed handler, a CRUD scheduling service backed by cron expressions, and a hosted worker that polls for due schedules. The project acts as the operational engine that drives automated code-graph maintenance tasks.

### CodeGraph.Extractors.CSharp

CodeGraph.Extractors.CSharp is a Roslyn-based static analysis project that extracts structural and semantic metadata from C# source code and solutions. It walks syntax trees to identify classes, methods, interfaces, routes, dependency injection registrations, service bus events, HTTP client calls, NuGet references, and other code relationships, producing structured ExtractionResult models consumed by the broader CodeGraph system. It serves as the C# language-specific extraction engine within the repository's multi-language code graph pipeline.

### CodeGraph.Extractors.TreeSitter

CodeGraph.Extractors.TreeSitter is a code extraction plugin that uses the Tree-sitter parsing library to analyze source files in multiple languages (at minimum C and C++). It implements the ICodeExtractor interface to walk syntax trees, extract symbol definitions (classes, functions, etc.) and import/dependency relationships, and produce structured graph nodes and edges for the broader CodeGraph repository analysis system.

### CodeGraph.Data.Neo4j

CodeGraph.Data.Neo4j is the Neo4j-backed persistence layer for the CodeGraph repository. It implements multiple data store interfaces (graph, vector, memory, wiki, job scheduling, metrics, analysis, review) by translating domain model objects to and from Cypher queries executed against a Neo4j database. This project serves as the infrastructure adapter that all higher-level services depend on for durable storage of code graph data, AI analysis results, memory graphs, and operational metadata.

### CodeGraph.Extractors.Sql

CodeGraph.Extractors.Sql is a SQL code analysis component that parses SQL script files and extracts structural graph information from them. It identifies and models SQL database objects — such as tables, views, stored procedures, functions, and indexes — along with their relationships, producing an ExtractionResult that feeds into the broader CodeGraph repository analysis pipeline. It implements the shared ICodeExtractor interface, making it a pluggable SQL-specific extractor within the system.

### CodeGraph.Extractors.TypeScript

CodeGraph.Extractors.TypeScript is a C# project responsible for extracting code graph data from TypeScript source files and projects. It manages a Node.js-based TypeScript analysis server process, communicates with it to extract structural information (nodes, edges) and lint results from TypeScript codebases, and converts the raw output into CodeGraph model types for integration into the broader CodeGraph pipeline.

